# 00-3. 필드 타입별 기능 레퍼런스

> 작성일: 2026-06-29  
> 대상: 필드 타입을 추가·수정하거나, 특정 필드의 속성/동작을 파악해야 하는 에이전트  
> 선행 문서: `docs/ge_guide/builder/00-2.builder_widget_field_components.md` (필드 구조 전체)  
> 공통 속성(label/fieldKey/colSpan/required/hideCondition 등)은 **00-2 § 6. 빌더 측 공통 베이스 구조** 참조

---

## 목차

| 번호 | 필드 | 사용 위젯 |
|---|---|---|
| 0 | [공통 렌더러 규칙](#0-공통-렌더러-규칙) | 전체 |
| 1 | [InputField](#1-inputfield) | Search, Form, SubList |
| 2 | [SelectField](#2-selectfield) | Search, Form, SubList |
| 3 | [RadioField](#3-radiofield) | Search, Form, SubList |
| 4 | [CheckboxField](#4-checkboxfield) | Search, Form, SubList |
| 5 | [ButtonField](#5-buttonfield) | Search, Form |
| 6 | [DateField](#6-datefield) | Search, Form, SubList |
| 7 | [DateRangeField](#7-daterangefield) | Search, Form, SubList |
| 8 | [TimeField](#8-timefield) | Search, Form, SubList |
| 9 | [TextareaField](#9-textareafield-space-전용) | Space |
| 10 | [FormTextareaField](#10-formtextareafield) | Form |
| 11 | [EditorField](#11-editorfield) | Form |
| 12 | [FileField](#12-filefield) | Form, SubList |
| 13 | [ImageField](#13-imagefield) | Form |
| 14 | [VideoField](#14-videofield) | Form |
| 15 | [MediaField](#15-mediafield) | Form |
| 16 | [ColorField](#16-colorfield) | Search |
| 17 | [CategoryField](#17-categoryfield) | Search |
| 18 | [ActionButtonField](#18-actionbuttonfield) | Space |
| 19 | [DateRangeStatusSearchField](#19-daterangestatussearchfield) | Search |
| A | [공통 — DataGenerationSection](#a-공통--datagenerationsection) | Input, FormTextarea, Editor |
| B | [공통 — _FieldOptions](#b-공통--_fieldoptions) | Select, Radio, Checkbox, Button, Color |
| C | [공통 — SlugSelectField](#c-공통--slugselectfield) | Category, ActionButton 내부 |

---

## 0. 공통 렌더러 규칙

모든 필드에 공통으로 적용되는 렌더러 동작 원칙이다. 개별 필드를 보기 전에 반드시 이 섹션을 먼저 읽는다.

---

### 0-1. Preview vs Live 모드

| 구분 | preview 모드 (빌더 미리보기) | live 모드 (운영 페이지) |
|---|---|---|
| 입력 | `pointer-events-none` — 클릭·입력 모두 차단 | 실제 입력·클릭 가능 |
| 옵션 | 처음 3~4개만 샘플 표시 | 전체 옵션 로드 |
| 공통코드 | `field.options` 슬라이스로 대체 | `codeGroupCode` → API로 전체 로드 |
| 파일 업로드 | `canAdd = false`, 버튼 비활성 | 실제 업로드 가능 |
| onChange | 없음 (또는 undefined) | 활성 |
| 읽기전용 | 구분 없음 (전체 비활성) | `field.readonly=true`면 `bg-slate-50` 스타일 적용 |

```typescript
// FieldRenderer.tsx
const isDisabled = isPreview || forceDisabled;
const isReadOnly = !isPreview && !!field.readonly;
```

---

### 0-2. Value 형태 전체 매핑표

| 필드 타입 | 저장 값 형태 | 저장 키 | 비고 |
|---|---|---|---|
| `input` | `string` | `fieldId` | 단순 텍스트 |
| `select` | `string` | `fieldId` | 옵션의 value 문자열 |
| `radio` | `string` | `fieldId` | 옵션의 value 문자열 |
| `checkbox` | `"v1,v2,v3"` (쉼표 구분 string) | `fieldId` | 복수 선택을 단일 문자열로 결합 |
| `button` (단일) | `string` | `fieldId` | 선택 value |
| `button` (다중) | `"v1,v2"` (쉼표 구분 string) | `fieldId` | `multiSelect=true`일 때 |
| `date` | `"YYYY-MM-DD"` | `fieldId` | dateSubType에 따라 포맷 상이 |
| `dateRange` | `string` × 2 | `fieldId_from`, `fieldId_to` | 시작/종료 분리 저장 |
| `time` | `"HH:MM"` | `fieldId` | |
| `textarea` | `string` | `fieldId` | 여러 줄 텍스트 |
| `editor` | HTML string | `fieldId` | 태그 포함 |
| `file` / `image` / `video` / `media` | `number[]` | `fieldId` | `page_files.id` 배열 |
| `color` | `"#RRGGBB"` | `fieldId` | HEX 문자열 |
| `category` | `string` | `fieldId` | 최종 선택 depth의 ID |
| `hidden` | — | — | 렌더링 없음, 저장 시 `defaultValue` 포함 |
| `action-button` | — | — | onChange 없음, 클릭 이벤트만 |

---

### 0-3. 검색 API 파라미터 규칙

SearchRenderer에서 수집한 `values` 맵을 기반으로 API 쿼리를 구성한다.

| 필드 타입 | values 키 | API 파라미터 | 예시 |
|---|---|---|---|
| `input`, `select`, `radio` | `fieldId` | `eq_fieldKey` | `eq_status=active` |
| `checkbox`, `button`(다중) | `fieldId` | `in_fieldKey` | `in_tags=tag1,tag2` |
| `date` | `fieldId` | `fieldKey_gte` / `fieldKey_lte` | `createdAt_gte=2024-01-01` |
| `dateRange` | `fieldId_from`, `fieldId_to` | `fieldKey_gte` / `fieldKey_lte` | |
| `category` | `fieldId` | `eq_fieldKey` | `eq_categoryId=5` |
| `dateRangeStatus` | `fieldId` | `fieldKey_status` | `period_status=in_range` |
| `input`·`select` 등 (OR) | `fieldId` | `fieldKey1\|fieldKey2` | `title\|content=홍길동` |

> ⚠️ 파라미터 키는 `fieldId`(렌더러 내부 상태 키)가 아닌 `fieldKey`(데이터 컬럼명) 기준으로 API에 전달된다. 혼동 주의.

#### OR 검색 — fieldKey에 `|` 사용

하나의 검색어로 **여러 컬럼을 동시에(OR) 부분 일치 검색**하려면 검색 필드의 `fieldKey`에 `|`로 컬럼을 나열한다. FE 변경 없이 **fieldKey 설정만으로** 동작한다.

```
검색 필드 설정:  fieldKey = "title|content"
FE → API 전달:   ?title|content=홍길동
```

**BE 처리** (`bo-api … service/PageDataService.java` — `appendWhereConditions()` + `bindSearchParams()`):

파라미터 키에 `|`가 포함되면 OR 다중 필드 ILIKE 블록으로 분기한다. **최상위 컬럼과 1단계 중첩(`form1.title` 등) 객체를 동시에 탐색**한다.

```sql
-- ?title|content=홍길동
AND (
    data_json->>'title'   ILIKE '%홍길동%'
    OR data_json->>'content' ILIKE '%홍길동%'
    OR EXISTS (
        SELECT 1 FROM jsonb_each(data_json) kv
        WHERE jsonb_typeof(kv.value) = 'object'
        AND ( kv.value->>'title'   ILIKE '%홍길동%'
           OR kv.value->>'content' ILIKE '%홍길동%' )
    )
)
```

| 항목 | 규칙 |
|---|---|
| 매칭 방식 | 각 필드 `ILIKE '%값%'` 부분 일치, 필드 간 **OR** 결합 |
| 탐색 범위 | 최상위 키 + **1단계 중첩** 객체 (`jsonb_each`로 form 컨테이너 내부까지) |
| 필드명 제약 | 각 컬럼명은 `[a-zA-Z0-9_]+` 만 허용 — 하나라도 위반 시 조건 생략 (SQL Injection 방지) |
| 파라미터 바인딩 | `p_or_{fields}` (`\|` → `__` 치환) 단일 파라미터로 모든 필드에 `%값%` 공통 바인딩 |

> 단순 단일 컬럼(`|` 없음) 검색도 동일하게 최상위 + 1단계 중첩을 ILIKE로 탐색한다. `|`는 거기에 OR 다중 컬럼만 추가하는 확장이다.

---

### 0-4. 공통코드(codeGroupCode) 처리 방식

```typescript
// FieldRenderer.tsx — resolveOptions()
const resolveOptions = (field, codeGroups) => {
    if (field.codeGroupCode) {
        // live: codeGroups 배열에서 groupCode 일치하는 그룹 찾아 details 반환
        return codeGroups
            .find(g => g.groupCode === field.codeGroupCode)
            ?.details.filter(d => d.active)
            .map(d => `${d.nameMsgKey ?? d.name}:${d.code}`) ?? [];
    }
    return field.options ?? [];
};
```

- **preview 모드**: `field.options?.slice(0, 4)` — 빌더에서 수동 입력한 샘플 4개만 표시
- **live 모드**: `codeGroupCode`로 실시간 조회 — `nameMsgKey` 있으면 `t(msgKey)`로 번역
- **옵션 포맷**: `"텍스트:값"` 문자열 → `parseOpt()` 함수로 `{ text, value }` 분리

---

### 0-5. hideCondition / disableCondition 문법

```
형식:  "fieldKey=값"               단일 조건 (일치 시 숨김/비활성)
       "fieldKey!=값"              단일 조건 (불일치 시 숨김/비활성)
       "key1=v1,key2=v2"          AND 복수 조건 (모두 일치 시 적용)
예:    "status=active,type=form"
```

**적용 흐름** (SearchRenderer / FormRenderer):
1. `evalFieldCondition(hideCondition, keyToId, values)` 호출
2. 조건 충족 → 해당 필드 `return null` (렌더링 스킵)
3. `disableCondition` 충족 → `forceDisabled=true`를 FieldRenderer에 전달
4. **preview 모드에서는 조건 평가 안 함** (항상 표시)
5. **hideCondition 충족된 필드는 유효성 검사 건너뜀** (required여도 오류 없음)

**FormRenderer cross-form 참조**:
- 현재 폼 필드 외에 다른 폼·탭의 fieldKey도 참조 가능
- URL 파라미터도 조건값으로 사용 가능 (`urlParams`)

---

### 0-6. 유효성 검사 흐름 (validateFormFields)

저장 버튼 클릭 시 `utils.ts`의 `validateFormFields()`가 순서대로 검사한다.

```
1. hideCondition 충족 필드 → 건너뜀
2. required → 빈 값이면 toast.warning 후 false 반환
3. minLength / maxLength → 글자 수 초과/미달
4. pattern → 정규식 불일치 + patternDesc 안내
5. 파일(file/image/video/media):
   - maxFileCount: 개수 초과
   - maxFileSizeMB: 개당 용량 초과
   - maxTotalSizeMB: 전체 합산 용량 초과
6. dateRange: 종료일 < 시작일
7. checkbox: minSelect / maxSelect 위반
```

> 검증 실패 시 즉시 `false` 반환 + toast 노출. 통과 시 `true` 반환.

---

## 1. InputField

**파일:** `builder/fields/InputField.tsx`  
**용도:** 단일행 텍스트 입력  
**사용 위젯:** Search, Form, SubList

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `placeholder` | string | - | 입력 힌트 텍스트 |
| `placeholderMsgKey` | string | - | 다국어 힌트 키 |
| `defaultValue` | string | - | 기본값 |
| `defaultValueMsgKey` | string | - | 다국어 기본값 키 |
| `minLength` | number | - | 최소 글자 수 |
| `maxLength` | number | - | 최대 글자 수 |
| `showCharCount` | boolean | false | 글자수 표시 여부 |
| `pattern` | string | - | 정규식 (유효성 검사용) |
| `patternDesc` | string | - | 정규식 설명 (사용자 안내) |
| `dataGenerations` | DataGenerationEntry[] | - | 다중 데이터 자동생성 세트 |

### 세부 설정 UI

1. **_FieldBase** — 라벨, Key, ColSpan, 필수항목, 읽기전용/검색제외, HIDE/Disable 조건
2. **Placeholder** — 텍스트 직접 입력 또는 MessageKeySelector (i18n 모드)
3. **기본값** — 텍스트 직접 입력 또는 MessageKeySelector
4. **유효성 검사** — 최소/최대 글자(2열), 글자수 표시 토글, 정규식 + 설명
5. **데이터 자동생성** — [DataGenerationSection](#a-공통--datagenerationsection) 참조

### 렌더러 동작

```tsx
// FieldRenderer.tsx — input 렌더링
<input
    type="text"
    value={value ?? ''}
    placeholder={field.placeholderMsgKey ? t(field.placeholderMsgKey) : field.placeholder}
    maxLength={field.showCharCount && field.maxLength ? field.maxLength : undefined}
    disabled={isDisabled}
    readOnly={isReadOnly}
    onChange={e => onChange?.(e.target.value)}
/>
{/* 글자수 표시 ON + maxLength 있으면 */}
{field.showCharCount && field.maxLength && (
    <span>{(value ?? '').length}/{field.maxLength}</span>
)}
```

**데이터 자동생성 적용 (FormRenderer)**:
- 이 필드 onChange 시 `dataGenerations` 배열 순회
- 각 세트의 변환 로직 적용 후 `generationKey` 경로의 대상 필드에 자동 입력
- 변환 순서: `dataReplacement(하이픈)` → `caseChange(대/소문자)` → `appendText` → `truncateLength`
- `onlyIfEmpty=true`면 대상 필드가 빈값인 경우에만 적용

### 특수 기능

- **레거시 마이그레이션**: `generationKey` 단일 속성으로 저장된 구버전 데이터는 첫 로드 시 `dataGenerations[0]`으로 자동 변환

### 예시

```json
{
  "type": "input",
  "label": "상품명",
  "fieldKey": "name",
  "colSpan": 6,
  "required": true,
  "maxLength": 100,
  "showCharCount": true,
  "dataGenerations": [
    { "generationKey": "slug", "dataReplacement": "hyphen", "caseChange": "lower", "onlyIfEmpty": true }
  ]
}
```

---

## 2. SelectField

**파일:** `builder/fields/SelectField.tsx`  
**용도:** 드롭다운 단일선택  
**사용 위젯:** Search, Form, SubList

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `options` | `{ label: string; value: string }[]` | [] | 수동 옵션 목록 |
| `codeGroupCode` | string | - | 공통코드 그룹 코드 |
| `defaultOptionValue` | string | - | 기본 선택값 |
| `placeholder` | string | - | 드롭다운 힌트 텍스트 |
| `placeholderMsgKey` | string | - | 다국어 힌트 키 |
| `optionSlug` | string | - | SLUG 옵션 소스 — 연결할 PAGE_DATA 타입 SLUG |
| `optionValueKey` | string | - | SLUG 데이터에서 value로 쓸 필드 key (flat key, 예: `code`) |
| `optionTextKey` | string | - | SLUG 데이터에서 text로 쓸 필드 key (flat key, 예: `prdNm`) |
| `optionOrderKey` | string | - | SLUG 데이터 정렬 기준 필드 key (flat key, Value/Text 키와 동일 기준) |
| `optionOrderDir` | `'ASC' \| 'DESC'` | - | 정렬 방향 (미설정 시 정렬 안 함) |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **Placeholder** — 텍스트 또는 i18n
3. **_FieldOptions** — 수동/공통코드/SLUG 탭 + 기본값 selectbox
   - SLUG 탭은 SelectField 전용 (RadioField·CheckboxField에는 없음)

### 렌더러 동작

```tsx
// SLUG 모드 (live 전용): /page-data/{optionSlug}?size=9999 fetch
// flattenPageDataItem()으로 중첩 flat 후 row[optionValueKey] / row[optionTextKey] 추출
// optionOrderKey 설정 시 FE에서 flat key 기준으로 정렬 (Value/Text 키와 동일 기준)
// → Value 키, Text 키, 정렬 Key 모두 같은 flat key 방식으로 입력 (예: 'prdNm')
// 수동/공통코드 모드: resolveOptions() → codeGroupCode 있으면 codeGroups에서, 없으면 field.options
```

- **preview**: `field.options?.slice(0, 4)` — 수동 입력 샘플 최대 4개 / SLUG 모드는 빈 options
- **live (수동/공통코드)**: `codeGroupCode` 있으면 `resolveOptions()`로 전체 옵션 로드
- **live (SLUG)**: `flattenPageDataItem()` 결과에서 flat key로 직접 접근 → `row[optionValueKey]`, `row[optionTextKey]`

### 특수 기능

- 수동/공통코드/SLUG **택일** — 탭 전환 시 나머지 값 전부 초기화
- SLUG key는 `flattenPageDataItem` 기준 flat key 사용 — contentKey 접두어 없이 필드명만 입력 (예: `form.prdNm` 아닌 `prdNm`)
- Value 키, Text 키, 정렬 Key 세 가지 모두 동일한 flat key 방식 — 입력 규칙이 통일됨

---

## 3. RadioField

**파일:** `builder/fields/RadioField.tsx`  
**용도:** 라디오 버튼 단일선택  
**사용 위젯:** Search, Form, SubList

### 전용 속성

SelectField와 동일 (`options`, `codeGroupCode`, `defaultOptionValue`)

### 세부 설정 UI

SelectField와 동일 (Placeholder 없음)

### 렌더러 동작

```tsx
<div className="flex items-center gap-4 py-2">
    {opts.map(opt => {
        const { text, value: val } = parseOpt(opt);
        return (
            <label key={val} className="flex items-center gap-1.5 cursor-pointer">
                <input
                    type="radio"
                    value={val}
                    checked={value === val}
                    disabled={isDisabled}
                    onChange={() => onChange?.(val)}
                />
                <span>{t(text)}</span>
            </label>
        );
    })}
</div>
```

- SelectField와 동일한 옵션 처리 방식 — 표시 UI만 radio 버튼으로 다름

---

## 4. CheckboxField

**파일:** `builder/fields/CheckboxField.tsx`  
**용도:** 체크박스 복수선택  
**사용 위젯:** Search, Form, SubList

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `options` | `{ label: string; value: string }[]` | [] | 수동 옵션 목록 |
| `codeGroupCode` | string | - | 공통코드 그룹 코드 |
| `defaultOptionValue` | string | - | 기본 선택값 |
| `minSelect` | number | - | 최소 선택 개수 |
| `maxSelect` | number | - | 최대 선택 개수 |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **_FieldOptions** — 수동/공통코드 탭 + 기본값
3. **유효성 검사** — 최소/최대 선택 개수 (2열 입력)

### 렌더러 동작

**Value 형태**: 선택된 값들을 `","` 로 결합한 단일 문자열  
예: 3개 선택 → `"tag1,tag2,tag3"`

```tsx
const selected = (value ?? '').split(',').filter(Boolean);

const handleChange = (val: string, checked: boolean) => {
    const next = checked
        ? [...selected, val]
        : selected.filter(v => v !== val);
    onChange?.(next.join(','));  // 쉼표 결합 후 상위 전달
};

{opts.map(opt => {
    const { text, value: val } = parseOpt(opt);
    return (
        <label key={val}>
            <input
                type="checkbox"
                value={val}
                checked={selected.includes(val)}
                disabled={isDisabled}
                onChange={e => handleChange(val, e.target.checked)}
            />
            <span>{t(text)}</span>
        </label>
    );
})}
```

**API 파라미터**: `in_fieldKey=v1,v2,v3` 형식으로 전달

---

## 5. ButtonField

**파일:** `builder/fields/ButtonField.tsx`  
**용도:** 선택 버튼 그룹  
**사용 위젯:** Search, Form

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `options` | `{ label: string; value: string }[]` | [] | 수동 옵션 목록 |
| `codeGroupCode` | string | - | 공통코드 그룹 코드 |
| `multiSelect` | boolean | false | true: 다중선택 toggle |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **_FieldOptions** — 수동/공통코드 탭 (기본값 selectbox 없음)
3. **다중선택 토글** — ToggleRow

### 렌더러 동작

**단일선택 (`multiSelect=false`)**:
- 선택 버튼 클릭 → `onChange?.(val)` 단일 문자열 전달
- 현재 선택값과 동일한 버튼 클릭 시 → `onChange?.('')` 선택 해제

**다중선택 (`multiSelect=true`)**:
```tsx
// value: "v1,v2" 쉼표 결합 문자열
const selected = (value ?? '').split(',').filter(Boolean);
const next = selected.includes(val)
    ? selected.filter(v => v !== val)  // 해제
    : [...selected, val];              // 추가
onChange?.(next.join(','));
```

- `preview` 모드에서는 `onClick={undefined}` — 클릭 이벤트 없음

---

## 6. DateField

**파일:** `builder/fields/DateField.tsx`  
**용도:** 단일 날짜/년월/날짜시간 선택  
**사용 위젯:** Search, Form, SubList

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `dateSubType` | `'date' \| 'yearMonth' \| 'datetime' \| 'time' \| 'timeSec'` | `'date'` | 날짜 선택 단위 |
| `defaultDateOffset` | number | 0 | 오늘 기준 N일 전 (0=오늘) |
| `defaultDate` | string | - | 고정 기본값 (offset=0일 때 직접 입력) |
| `defaultToday` | boolean | false | 오늘 날짜 자동 적용 |
| `disablePast` | boolean | false | 오늘 이전 날짜 비활성화 |

### 세부 설정 UI

1. **날짜 타입 선택** — select (date/yearMonth/datetime/time/timeSec)
2. **_FieldBase** — 공통
3. **기본값 섹션:**
   - `date/yearMonth/datetime`: `±` 버튼 + 숫자 input (offset 제어) + 날짜 input + 오늘 날짜·이전 날짜 토글
   - `time/timeSec`: 직접 시간 입력 (`<input type="time">`) — offset·오늘날짜 토글 없음, timeSec은 step=1(초 단위)

### dateSubType 분기

| dateSubType | input type | 저장 포맷 | offset 지원 | 비고 |
|---|---|---|---|---|
| `date` | `date` | `YYYY-MM-DD` | ✅ | 기본값 |
| `yearMonth` | `month` | `YYYY-MM` | ✅ | 년/월 선택만 |
| `datetime` | `datetime-local` | `YYYY-MM-DDTHH:mm` | ✅ | 날짜+시간 |
| `time` | `time` | `HH:mm` | ❌ | 시분 직접 입력 |
| `timeSec` | `time` (step=1) | `HH:mm:ss` | ❌ | 시분초 직접 입력 |

### 렌더러 동작

```tsx
// yearMonth 레거시 타입도 dateSubType='yearMonth'로 통합 처리
const dateSubType = field.type === 'yearMonth'
    ? 'yearMonth'
    : (field.dateSubType ?? 'date');

const isTimeBased = dateSubType === 'time' || dateSubType === 'timeSec';
const inputType = dateSubType === 'yearMonth' ? 'month'
    : dateSubType === 'datetime' ? 'datetime-local'
    : isTimeBased ? 'time'
    : 'date';

// disablePast: min 속성으로 오늘 날짜 설정
const minDate = field.disablePast ? getTodayString(dateSubType) : undefined;

<input type={inputType} value={value ?? ''} min={minDate}
    disabled={isDisabled} readOnly={isReadOnly}
    onChange={e => onChange?.(e.target.value)} />
```

**defaultToday 적용**: `defaultToday=true`면 렌더러에서 매번 오늘 날짜를 기본값으로 적용 (저장값이 비어있을 때)

---

## 7. DateRangeField

**파일:** `builder/fields/DateRangeField.tsx`  
**용도:** 날짜 범위 (시작일 ~ 종료일)  
**사용 위젯:** Search, Form, SubList

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `rangeSubType` | `'date' \| 'yearMonth' \| 'datetime' \| 'time' \| 'timeSec'` | `'date'` | 범위 선택 단위 |
| `label2` | string | - | 종료일 라벨 |
| `label2MsgKey` | string | - | 종료일 다국어 키 |
| `defaultStartDateOffset` | number | - | 시작일 offset (오늘 기준 N일 전) |
| `defaultStartDate` | string | - | 시작일 고정 기본값 |
| `disableStartPast` | boolean | false | 시작일 이전 날짜 비활성화 |
| `defaultEndDateOffset` | number | - | 종료일 offset |
| `defaultEndDate` | string | - | 종료일 고정 기본값 |
| `disableEndPast` | boolean | false | 종료일 이전 날짜 비활성화 |
| `maxRangeValue` | number | - | 최대 조회 기간 값 (0 또는 미설정: 무제한) |
| `maxRangeUnit` | `'day' \| 'week' \| 'month' \| 'year'` | `'day'` | 최대 조회 기간 단위 |
| `singleDateRange` | boolean | false | 단일 컬럼을 범위로 필터링 |

### 세부 설정 UI

1. **범위 타입 선택** — select (date/yearMonth/datetime/time/timeSec)
2. **단일 날짜 범위 검색 토글**
3. **_FieldBase** — 라벨(시작), label2(종료), Key, ColSpan (minSpan=2 고정)
4. **시작일/종료일 기본값** — 각각 `±` 버튼 + 숫자 + 날짜 input + disablePast 토글
5. **최대 조회 기간** — 숫자 input + 단위 selectbox

### rangeSubType 분기

| rangeSubType | 저장 포맷 | offset 지원 | 비고 |
|---|---|---|---|
| `date` | `YYYY-MM-DD` | ✅ | |
| `yearMonth` | `YYYY-MM` | ✅ | |
| `datetime` | `YYYY-MM-DDTHH:mm` | ✅ | |
| `time` | `HH:mm` | ❌ | 시분 직접 입력 |
| `timeSec` | `HH:mm:ss` | ❌ | 시분초 직접 입력, step=1 |

### 렌더러 동작

**저장 키**: `fieldId_from`(시작), `fieldId_to`(종료) — 2개로 분리 저장  
**API 파라미터**: `fieldKey_gte`(시작), `fieldKey_lte`(종료) 변환 후 전달

```tsx
// 2개 input 나란히
<div className="flex items-center gap-2">
    <input type={inputType} value={fromValue}
        onChange={e => onFromChange?.(e.target.value)} />
    <span className="text-slate-400">~</span>
    <input type={inputType} value={toValue}
        onChange={e => onToChange?.(e.target.value)} />
</div>
```

**singleDateRange**: `fieldKey_gte`/`_lte` 대신 단일 컬럼(예: `date`)에 `date_gte`/`date_lte`를 붙여 범위 필터링  
**maxRangeValue**: 렌더러에서 종료일이 시작일 + N단위 초과 시 오류 표시 및 조정

---

## 8. TimeField

**파일:** `builder/fields/TimeField.tsx`  
**용도:** 시간 선택 HH:MM  
**사용 위젯:** Search, Form, SubList

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `defaultTime` | string | - | 기본 시간 (HH:MM) |
| `timeStep` | `1 \| 5 \| 10 \| 30` | 1 | 분 단위 간격 |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **기본값** — time input (HH:MM 직접 입력)
3. **Step** — 버튼 4개 (1분 / 5분 / 10분 / 30분)

### 렌더러 동작

```tsx
<input
    type="time"
    step={field.timeStep ? field.timeStep * 60 : undefined}
    value={value ?? ''}
    disabled={isDisabled}
    onChange={e => onChange?.(e.target.value)}
/>
```

- `timeStep` → `step` 속성으로 변환 (분 단위 × 60 = 초 단위)
- DateRangeField의 `rangeSubType='time'`과 별개 — 독립적 단독 시간 필드

---

## 9. TextareaField (Space 전용)

**파일:** `builder/fields/TextareaField.tsx`  
**용도:** Space 위젯 내 텍스트 콘텐츠 표시 블록  
**사용 위젯:** Space

> ⚠️ Form 입력용 textarea는 **FormTextareaField** 사용. 이 컴포넌트는 Space 위젯의 텍스트 아이템 전용.

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `content` | string | - | 표시할 텍스트 내용 |
| `fontSize` | number | 12 | 글자 크기 (px) |
| `bold` | boolean | false | 굵게 |
| `textColor` | string | `#334155` | 텍스트 색상 (HEX) |

### 세부 설정 UI

1. **_FieldBase** — 라벨 (fieldKey 선택, 필수 아님)
2. **내용** — textarea (3줄)
3. **스타일** — 글자 크기(px), 굵게 토글(`B`), 색상 selectbox(프리셋 8가지)

### 색상 프리셋

`#334155`(기본 진회색) / `#000000`(검정) / `#475569`(진회색) / `#94a3b8`(중간회색) / `#3b82f6`(파랑) / `#10b981`(초록) / `#ef4444`(빨강) / `#f97316`(주황)

### 렌더러 동작

```tsx
// FieldRenderer — onChange 없는 경우: Space 표시 전용
if (!onChange) {
    return (
        <p style={{
            fontSize: `${field.fontSize ?? 12}px`,
            fontWeight: field.bold ? 'bold' : undefined,
            color: field.textColor ?? '#334155',
        }}>
            {field.content}
        </p>
    );
}
// onChange 있는 경우: 편집 가능 textarea (FormRenderer 등)
```

- **렌더러에서 onChange 없음** → 인라인 CSS로 표시 전용 `<p>` 태그 렌더링
- 사용자 입력 불가 (Space 위젯의 의도적 설계)

---

## 10. FormTextareaField

**파일:** `builder/fields/FormTextareaField.tsx`  
**용도:** 폼 입력용 여러 줄 텍스트  
**사용 위젯:** Form

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `placeholder` | string | - | 입력 힌트 |
| `placeholderMsgKey` | string | - | 다국어 힌트 키 |
| `defaultValue` | string | - | 기본값 |
| `defaultValueMsgKey` | string | - | 다국어 기본값 키 |
| `rows` | number | 3 | 표시 행 수 (1~20) |
| `minLength` | number | - | 최소 글자 수 |
| `maxLength` | number | - | 최대 글자 수 |
| `showCharCount` | boolean | false | 글자수 표시 |
| `pattern` | string | - | 정규식 |
| `patternDesc` | string | - | 정규식 설명 |
| `dataGenerations` | DataGenerationEntry[] | - | 다중 데이터 자동생성 세트 |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **Placeholder** — 텍스트 또는 i18n
3. **기본값** — 텍스트 또는 i18n
4. **표시 행 수** — 숫자 input (1~20 범위)
5. **유효성 검사** — 최소/최대 글자, 글자수 표시, 정규식
6. **데이터 자동생성** — [DataGenerationSection](#a-공통--datagenerationsection) 참조

### 렌더러 동작

```tsx
// onChange 있는 경우
<div className="relative">
    <textarea
        rows={field.rows ?? 3}
        maxLength={field.showCharCount && field.maxLength ? field.maxLength : undefined}
        value={value ?? ''}
        disabled={isDisabled}
        readOnly={isReadOnly}
        onChange={e => onChange?.(e.target.value)}
    />
    {field.showCharCount && field.maxLength && (
        <span>{(value ?? '').length}/{field.maxLength}</span>
    )}
</div>
```

- `rows` 속성으로 textarea 물리적 높이 제어 (`rowSpan`과 독립적)
- InputField와 동일한 데이터 자동생성 적용

---

## 11. EditorField

**파일:** `builder/fields/EditorField.tsx`  
**용도:** 위지윅(WYSIWYG) HTML 에디터  
**사용 위젯:** Form

### 에디터 종류

에디터는 두 가지 중 선택할 수 있으며, 빌더 설정 패널에서 `editorType`으로 지정한다.

| editorType | 컴포넌트 파일 | 설명 |
|---|---|---|
| `tiptap` (기본값) | `components/common/tiptap-editor.tsx` | 커스텀 툴바 WYSIWYG 에디터 |
| `toast` | `components/common/wysiwyg-editor.tsx` | TOAST UI Editor (과도기 병행 지원) |

> `editorType`을 지정하지 않으면 자동으로 `tiptap`이 사용된다.

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `editorType` | `'tiptap' \| 'toast'` | `'tiptap'` | 사용할 에디터 종류 |
| `placeholder` | string | - | 에디터 힌트 |
| `placeholderMsgKey` | string | - | 다국어 힌트 키 |
| `defaultValue` | string | - | 기본 HTML 내용 |
| `defaultValueMsgKey` | string | - | 다국어 기본값 키 |
| `minLength` | number | - | 최소 글자 수 (HTML 태그 제외 순수 텍스트 기준) |
| `maxLength` | number | - | 최대 글자 수 |
| `dataGenerations` | DataGenerationEntry[] | - | 다중 데이터 자동생성 세트 |

### 세부 설정 UI

1. **_FieldBase** — labelOptional 활성화
2. **에디터 종류** — tiptap / TOAST UI 라디오 선택 (`editorType` 저장)
3. **에디터 설정** — Placeholder, 기본값, 최소/최대 글자 (2열)
4. **데이터 자동생성** — `fieldType='editor'` 분기 (stripHtml + truncateLength만)

### TipTap 툴바 구성

| 그룹 | 버튼 |
|---|---|
| 텍스트 스타일 | Heading(H1~H6), Bold, Italic, Strike, Underline |
| 삽입 | 수평선 |
| 목록 | 글머리, 번호, 들여쓰기, 내어쓰기 |
| 미디어 | 테이블, 이미지, 링크 |
| 꾸미기 | 형광펜, 텍스트 정렬(4종), 글자색, 위첨자, 아래첨자 |
| 동영상 | YouTube 임베드 |

### 렌더러 동작

```typescript
// FieldRenderer.tsx — 두 에디터 모두 SSR 불가, dynamic import 필수
const TiptapEditor = dynamic(() => import('@/components/common/tiptap-editor'), { ssr: false });
const WysiwygEditor = dynamic(() => import('@/components/common/wysiwyg-editor'), { ssr: false });

// 높이 계산: rowSpan × ROW_HEIGHT - (라벨 있으면 44, 없으면 24)
// 이 값이 에디터 컴포넌트의 height prop = 툴바 포함 전체 높이
const editorHeight = `${rowSpan * ROW_HEIGHT - (field.label ? 44 : 24)}px`;

const editorType = field.editorType ?? 'tiptap';
if (editorType === 'toast') {
    return <WysiwygEditor initialValue={value} onChange={...} height={editorHeight} />;
}
return <TiptapEditor initialValue={value} onChange={...} height={editorHeight} />;
```

> ⚠️ **SSR 절대 금지**: `dynamic import + ssr: false` 필수. 서버 컴포넌트에서 직접 import 시 빌드 오류 발생  
> ⚠️ **height prop = 툴바 포함 전체 높이**: TipTap은 내부적으로 `flex-col`로 툴바/콘텐츠 영역을 분리하므로 별도 보정 불필요  
> ⚠️ **이미지 업로드 API**: `/page-files/upload` 사용 (`api` 인스턴스 baseURL이 `/api/v1`이므로 `/api/v1/...` 형태로 중복 입력 금지)

**DataGeneration editor 전용 변환**:
- `stripHtml`: HTML 태그 제거 후 순수 텍스트만 추출
- `truncateLength`: N자 미만으로 자르기
- `dataReplacement`, `caseChange`, `appendText` 사용 불가

---

## 12. FileField

**파일:** `builder/fields/FileField.tsx`  
**용도:** 파일 업로드  
**사용 위젯:** Form, SubList

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `maxFileCount` | number | 1 | 최대 파일 수 (1~20) |
| `maxFileSizeMB` | number | 10 | 개당 최대 용량 (MB, 1~100) |
| `maxTotalSizeMB` | number | 20 | 전체 최대 용량 (`maxFileSizeMB` 이상 자동 조정) |
| `fileTypeMode` | `'' \| 'doc' \| 'image' \| 'video' \| 'custom'` | `''` | 허용 파일 유형 |
| `allowedExtensions` | string[] | [] | 커스텀 확장자 목록 |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **파일 업로드 설정** — 최대 파일 수, 개당 최대(MB), 전체 최대(MB)
3. **허용 파일 유형** — 라디오 4개 (전체/문서/이미지/동영상)
4. **커스텀 확장자** — ExtensionTagInput (`fileTypeMode='custom'`일 때만)

### fileTypeMode 분기

| fileTypeMode | 허용 범위 | accept 속성값 |
|---|---|---|
| `''` | 모든 파일 | `*` |
| `'doc'` | 문서 (고정 목록) | `.pdf,.doc,.docx,...` |
| `'image'` | 이미지 (고정 목록) | `.jpg,.png,.gif,...` |
| `'video'` | 동영상 (고정 목록) | `.mp4,.avi,.mov,...` |
| `'custom'` | `allowedExtensions` 배열 직접 지정 | `.jpg,.custom,...` |

### 렌더러 동작

**Edge 브라우저 overflow 버그 우회**:
```tsx
// label>input[file] 패턴이 overflow-y-auto 컨테이너 내부에서 onChange 미발화하는 Edge 버그 우회
<input
    type="file"
    style={{ display: 'none' }}
    ref={inputRef}
    accept={resolveAccept(field)}
    multiple={field.maxFileCount > 1}
    onChange={handleFileChange}
/>
<button onClick={() => inputRef.current?.click()}>파일 선택</button>
```

**Value 형태**: `number[]` — 업로드된 `page_files.id` 배열  
**저장 후 처리**: 저장 API 완료 후 `PATCH /page-files/link` 호출하여 파일-데이터 연결 (미호출 시 orphan 파일)

---

## 13. ImageField

**파일:** `builder/fields/ImageField.tsx`  
**용도:** 이미지 업로드  
**사용 위젯:** Form

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `maxFileCount` | number | 1 | 최대 이미지 수 (1~20) |
| `maxFileSizeMB` | number | 10 | 개당 최대 용량 (MB, 1~50) |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **이미지 등록 설정** — 최대 이미지 수, 개당 최대(MB)
3. **허용 형식** — 이미지 확장자 고정 표시 (수정 불가)

### 렌더러 동작

**격자 자동계산** (복수 이미지):
```typescript
const cols = Math.max(1, Math.ceil(Math.sqrt(displayItems.length)));
const rows = Math.ceil(displayItems.length / cols);
const cellH = Math.floor((containerH - PAD_PX - GAP_PX * (rows - 1)) / rows);
// 컨테이너를 정사각형에 가깝게 채우는 격자 자동 배치
```

**파일 처리**: FileField와 동일한 Edge 버그 우회 방식 사용

---

## 14. VideoField

**파일:** `builder/fields/VideoField.tsx`  
**용도:** 동영상 입력 (URL 또는 파일 업로드)  
**사용 위젯:** Form

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `videoMode` | `'url' \| 'file'` | `'url'` | 입력 방식 |
| `maxFileCount` | number | 1 | 최대 파일 수 (파일 모드) |
| `maxFileSizeMB` | number | 500 | 개당 최대 (MB) |
| `maxTotalSizeMB` | number | 500 | 전체 최대 (MB) |
| `fileTypeMode` | `'video' \| '' \| 'custom'` | `'video'` | 허용 파일 유형 |
| `allowedExtensions` | string[] | [] | 커스텀 확장자 |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **URL / 파일 탭 토글** (videoMode 전환)
3. **파일 모드일 때만:** 최대 파일 수, 개당/전체 최대, 허용 형식, 커스텀 확장자

### 렌더러 동작

**탭 전환 자동 초기화**:
- URL → 파일: `maxFileCount=1`, `maxFileSizeMB=500`, `maxTotalSizeMB=500`, `fileTypeMode='video'` 자동 설정
- 파일 → URL: 파일 관련 속성 전체 초기화

**Edge 비디오 GPU 디코딩 타이밍 버그 우회**:
```tsx
useEffect(() => {
    const el = videoRef.current;
    el.src = url;
    const handleLoadedData = () => { el.currentTime = 0.001; };  // 첫 프레임 강제 렌더링
    el.addEventListener('loadeddata', handleLoadedData);
    return () => el.removeEventListener('loadeddata', handleLoadedData);
}, [url]);
```

---

## 15. MediaField

**파일:** `builder/fields/MediaField.tsx`  
**용도:** 이미지 + 동영상 통합 업로드 (1개)  
**사용 위젯:** Form

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `mediaImageMaxSizeMB` | number | 5 | 이미지 최대 크기 (MB, 1~100) |
| `mediaVideoMaxSizeMB` | number | 20 | 동영상 최대 크기 (MB, 1~2048) |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **이미지 섹션** — 최대 크기(MB), 허용 형식 고정 표시
3. **동영상 섹션** — 최대 크기(MB), 허용 형식 고정 표시

### 렌더러 동작

- **단일 업로드**: 이미지 또는 동영상 중 1개만 (동시 업로드 불가)
- 업로드된 파일 타입 감지 후 `<img>` 또는 `<video>` 자동 분기
- 허용 형식: 이미지 + 동영상 accept 합집합 (`image/*,video/*`)

---

## 16. ColorField

**파일:** `builder/fields/ColorField.tsx`  
**용도:** 프리셋 색상 선택  
**사용 위젯:** Search

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `options` | string[] | 기본 프리셋 8개 | HEX 색상 값 배열 |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **색상 목록** — 원형 버튼(ColorSwatch), 호버 시 X 삭제, 기본값 초기화 버튼
3. **색상 추가** — 미리보기 원 + HEX input + color picker + 추가 버튼

### 기본 프리셋 8개

`#4361ee`(인디고) / `#10b981`(에메랄드) / `#f59e0b`(앰버) / `#ef4444`(레드) / `#8b5cf6`(바이올렛) / `#06b6d4`(시안) / `#ec4899`(핑크) / `#94a3b8`(그레이)

### 렌더러 동작

```tsx
// FieldRenderer.tsx — ColorPresetSelector 컴포넌트
<ColorPresetSelector
    colors={field.options}
    selectedColor={value}
    disabled={isPreview}       // preview 모드에서 pointer-events-none
    onChange={onChange}
/>
// 원형 버튼 목록 표시, 선택된 색상은 outline 강조
// onChange: HEX 문자열 전달 (예: "#4361ee")
```

**HEX 유효성 검사**: `#RGB` 또는 `#RRGGBB` 형식만 허용  
**API 파라미터**: `eq_fieldKey=#4361ee`

---

## 17. CategoryField

**파일:** `builder/fields/CategoryField.tsx`  
**용도:** 계층형 카테고리 검색 필터 (1~4 depth)  
**사용 위젯:** Search

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `dbSlug` | string | - | 카테고리 데이터 PAGE_DATA slug (필수) |
| `relationSlugId` | string | - | 연동 slug-relation ID (선택) |
| `maxDepth` | `1 \| 2 \| 3 \| 4` | 1 | 카테고리 최대 depth |
| `depthLabels` | string[] | - | depth별 라벨 배열 |
| `depthLabelMsgKeys` | string[] | - | depth별 다국어 라벨 키 |
| `depthValueFields` | string[] | - | depth별 value 경로 (dot notation) |
| `depthTextFields` | string[] | - | depth별 텍스트 경로 (dot notation) |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **카테고리 Slug 선택** — SlugSelectField
3. **연동 Slug 선택** — SlugSelectField (선택 안 함 가능)
4. **최대 Depth** — 버튼형 1/2/3/4
5. **Depth별 설정 그리드** — 라벨 + Value 경로 + Text 경로 (depth 수만큼 행)

### 렌더러 동작 (CategorySearchField.tsx)

**캐스케이딩 드롭다운 흐름**:

```typescript
// 1. 마운트 시 depth=1 옵션 즉시 로드
loadDepthOptions(1, null);

// 2. depth N 선택 시
const handleSelect = (depth, selectedValue) => {
    setDepthValues(prev => {
        const next = [...prev];
        next[depth - 1] = selectedValue;
        // 하위 depth 선택값 + 옵션 전부 초기화
        for (let i = depth; i < maxDepth; i++) next[i] = '';
        return next;
    });
    onChange?.(selectedValue);         // 최종 선택값 상위 전달
    if (selectedValue && depth < maxDepth) {
        loadDepthOptions(depth + 1, selectedValue);  // 다음 depth 로드
    }
};
```

**API 호출**:
```typescript
// GET /page-data/{field.dbSlug}?eq_depth=1
// GET /page-data/{field.dbSlug}?eq_depth=2&eq_parentId=5
const params = { eq_depth: String(depth) };
if (parentValue) params.eq_parentId = parentValue;
```

**Value 경로 해석 (dot notation)**:
```typescript
// depthValueFields[0] = 'id'         → item.id
// depthValueFields[1] = 'dataJson.id' → item.dataJson.id
value: resolveAccessor(item, field.depthValueFields[depth - 1] ?? 'id')
text:  resolveAccessor(item, field.depthTextFields[depth - 1] ?? 'name')
```

**preview 모드**: 각 depth마다 샘플 3개 (`PREVIEW_OPTIONS`) 표시, API 호출 없음

---

## 18. ActionButtonField

**파일:** `builder/fields/ActionButtonField.tsx`  
**용도:** Space 위젯 버튼 아이템 설정  
**사용 위젯:** Space

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `color` | `'black' \| 'green' \| 'blue' \| 'yellow' \| 'red' \| 'gray' \| 'pink'` | `'black'` | 버튼 배경색 |
| `textColor` | string | `'white'` | 버튼 글자색 |
| `connType` | `'' \| 'content' \| 'popup' \| 'path' \| 'close' \| 'excel' \| 'datasave'` | `''` | 연결 방식 |
| `saveConfirm` | boolean | false | 클릭 시 confirm 창 표시 |
| `connectedContentWidgetIds` | string[] | - | 연결할 Form/SubList/MultiSelect widgetId 배열 |
| `contentAction` | `'save' \| 'delete'` | - | 저장/삭제 동작 |
| `goBackAfterAction` | boolean | false | 동작 후 이전 페이지 이동 |
| `dataSaveSlug` | string | - | datasave 연결: 저장 대상 slug |
| `params` | string | - | 추가 파라미터 (JSON 문자열) |
| `excelTableWidgetId` | string | - | excel 연결: 다운로드할 Table widgetId |
| `popupSlug` | string | - | popup 연결: 팝업으로 열 페이지 slug |
| `fileLayerSlug` | string | - | path 연결: 이동할 경로 |

### 세부 설정 UI

1. **_FieldBase** — saveConfirm 토글 포함 (3열 grid 배치)
2. **색상 설정** — 배경색 + 글자색 selectbox (2열)
3. **연결 섹션** — connType select + 타입별 추가 UI

### connType별 추가 설정 UI

| connType | 추가 설정 UI |
|---|---|
| `''` | (없음) |
| `'content'` | Form/SubList/MultiSelect 위젯 체크박스 → 저장/삭제 라디오 → 이전 페이지 이동 토글 |
| `'datasave'` | Form/SubList/Table/MultiSelect 체크박스 + Slug 선택 + params + 이전 페이지 토글 |
| `'excel'` | Table 위젯만 필터링한 selectbox |
| `'popup'` | SlugSelectField (Quick-Detail 템플릿) + params |
| `'path'` | 경로 text input + params |
| `'close'` | (없음) |

### 렌더러 동작 (SpaceRenderer.tsx)

```typescript
// 버튼 색상: 정적 맵 사용 (동적 Tailwind 클래스 금지)
const BG_COLOR_MAP = {
    black: 'bg-slate-900', green: 'bg-green-600', blue: 'bg-blue-600',
    yellow: 'bg-yellow-500', red: 'bg-red-600', gray: 'bg-slate-500', pink: 'bg-pink-500'
};

const handleButtonClick = async (field) => {
    // saveConfirm: 먼저 confirm 창 표시
    if (field.saveConfirm && !confirm('저장하시겠습니까?')) return;

    switch (field.connType) {
        case 'content':
            // onContentAction(widgetIds, 'save' | 'delete') 호출
            onContentAction?.(field.connectedContentWidgetIds, field.contentAction);
            if (field.goBackAfterAction) router.back();
            break;
        case 'close':
            onClose?.() ?? router.back();
            break;
        case 'popup':
            onPopupOpen?.(field.popupSlug, parseParams(field.params));
            break;
        case 'excel':
            onExcelDownload?.(field.excelTableWidgetId);
            break;
        case 'datasave':
            await onDataSave?.(field.connectedContentWidgetIds, field.dataSaveSlug, ...);
            if (field.goBackAfterAction) router.back();
            break;
        case 'path':
            router.push(field.fileLayerSlug);
            break;
    }
};
```

**연결 타입 변경 시 전체 초기화**: `connType` 변경 → 연결 속성 전부 `undefined`로 리셋

---

## 19. DateRangeStatusSearchField

**파일:** `builder/fields/DateRangeStatusSearchField.tsx`  
**용도:** 날짜 범위 기반 상태 필터 (3단계)  
**사용 위젯:** Search

### 전용 속성

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `linkedDateRangeKey` | string | - | 연결할 dateRange 필드의 accessor (필수) |
| `statusDisplayStyle` | `'select' \| 'radio'` | `'select'` | 렌더러 표시 방식 |
| `beforeText` | string | - | 날짜 이전 상태 텍스트 (예: '예정') |
| `beforeTextMsgKey` | string | - | 이전 상태 다국어 키 |
| `inRangeText` | string | - | 날짜 포함 상태 텍스트 (예: '진행중') |
| `inRangeTextMsgKey` | string | - | 포함 상태 다국어 키 |
| `afterText` | string | - | 날짜 이후 상태 텍스트 (예: '종료') |
| `afterTextMsgKey` | string | - | 이후 상태 다국어 키 |

### 세부 설정 UI

1. **_FieldBase** — 공통
2. **연결 dateRange Key** — text input (font-mono, required)
3. **표시 방식** — select/radio 토글
4. **3개 상태 텍스트** — 이전/포함/이후 (3열 grid, i18n 지원)

### 렌더러 동작

**상태 옵션 생성**:
```typescript
const opts = [
    { value: '',          text: '전체' },
    { value: 'before',    text: field.beforeText    ?? '이전' },
    { value: 'in_range',  text: field.inRangeText   ?? '진행중' },
    { value: 'after',     text: field.afterText     ?? '이후' },
];
```

**API 파라미터**: `fieldKey_status=before | in_range | after`  
**렌더러 검색 조건 해석**:
- `before`: 오늘 < `linkedDateRangeKey_from`
- `in_range`: `linkedDateRangeKey_from` ≤ 오늘 ≤ `linkedDateRangeKey_to`
- `after`: 오늘 > `linkedDateRangeKey_to`

**`statusDisplayStyle` 분기**:
- `'select'`: `<select>` 드롭다운으로 표시
- `'radio'`: 라디오 버튼 그룹으로 표시

**연동 필수**: `linkedDateRangeKey`가 없으면 렌더러에서 조건 평가 불가 → 항상 '전체' 처리

---

## 20. DateRangeStatusColumnField

**파일:** `builder/fields/DateRangeStatusColumnField.tsx`  
**용도:** 테이블 셀에서 날짜 범위 데이터를 "예정 / 진행중 / 종료" 텍스트로 자동 표시  
**사용 위젯:** Table

---

### 동작 개요

API가 반환한 `{accessor}_from` / `{accessor}_to` 두 값을 읽어, **현재 시각**과 비교하여 상태를 판정한다.  
비교 기준 포맷은 `linkedRangeSubType`이 결정한다.

```
API 응답 예시
  period_from: "2024-01-01"
  period_to:   "2024-12-31"

  → TableCellRenderer가 today(=YYYY-MM-DD)와 비교
  → "2024-01-01 ≤ today ≤ 2024-12-31" 이면 "진행중" 표시
```

---

### 빌더 설정 순서

#### STEP 1 — 같은 테이블에 dateRange 컬럼이 있어야 한다
데이터 accessor가 `period`라면 API는 `period_from` / `period_to`를 반환해야 한다.  
해당 dateRange 컬럼의 `rangeSubType`을 확인해둔다.

#### STEP 2 — dateRangeStatus 컬럼 추가
테이블 컬럼 추가 시 셀 타입 `dateRangeStatus` 선택

#### STEP 3 — 연결 설정 (필수)

| 항목 | 설명 |
|---|---|
| **연결 dateRange 컬럼 Key** | dateRange 컬럼의 accessor 입력 (예: `period`) |
| **날짜 범위 서브타입** | 연결된 dateRange 컬럼의 `rangeSubType`과 **동일하게** 선택 |
| **이전/포함/이후 텍스트** | 각 상태에 표시할 텍스트 입력 (기본값: 예정 / 진행중 / 종료) |

> ⚠️ `날짜 범위 서브타입`이 dateRange 컬럼의 `rangeSubType`과 다르면 비교 포맷이 맞지 않아 **항상 오판정**된다.

---

### 상태 판정 기준 (rangeSubType별)

`linkedRangeSubType`에 따라 현재 시각을 다른 포맷으로 변환한 뒤 `_from` / `_to`와 문자열 비교한다.

| rangeSubType | 비교 기준 | from/to 값 형태 |
|---|---|---|
| `date` (기본) | 오늘 날짜 `YYYY-MM-DD` | `2024-07-01` |
| `yearMonth` | 이번 년월 `YYYY-MM` | `2024-07` |
| `datetime` | 현재 날짜+시각 `YYYY-MM-DD HH:mm` | `2024-07-01 14:30` |
| `time` | 현재 시각 `HH:mm` | `14:30` |
| `timeSec` | 현재 시각 `HH:mm:ss` | `14:30:00` |

**판정 결과**:
- `현재 < from` → **이전 텍스트** (예: 예정)
- `from ≤ 현재 ≤ to` → **포함 텍스트** (예: 진행중)
- `현재 > to` → **이후 텍스트** (예: 종료)
- from / to 모두 없음 → `-`

---

### 전용 속성 참조

| 속성 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `linkedDateRangeKey` | string | - | 연결된 dateRange 컬럼의 accessor (필수) |
| `linkedRangeSubType` | `'date' \| 'yearMonth' \| 'datetime' \| 'time' \| 'timeSec'` | `'date'` | 상태 판정 포맷 — dateRange 컬럼의 rangeSubType과 일치시킬 것 |
| `beforeText` | string | `'예정'` | 날짜 이전 표시 텍스트 |
| `beforeTextMsgKey` | string | - | 이전 텍스트 다국어 키 |
| `inRangeText` | string | `'진행중'` | 날짜 포함 표시 텍스트 |
| `inRangeTextMsgKey` | string | - | 포함 텍스트 다국어 키 |
| `afterText` | string | `'종료'` | 날짜 이후 표시 텍스트 |
| `afterTextMsgKey` | string | - | 이후 텍스트 다국어 키 |

---

### preview 모드
실제 데이터 없이 [이전 텍스트, 포함 텍스트, 이후 텍스트]를 행 index % 3 으로 순환 표시한다.

---

## A. 공통 — DataGenerationSection

**파일:** `builder/fields/DataGenerationSection.tsx`  
**포함 필드:** InputField, FormTextareaField, EditorField

### 동작 원리

이 필드 값을 변환하여 **다른 필드에 자동 입력**하는 기능이다.  
예: 상품명 입력 → slug 필드에 URL-safe 값 자동 생성

### 변환 순서

```
원본 값
  → (1) stripHtml      — HTML 태그 제거 (editor 전용)
  → (2) dataReplacement — 공백·특수문자 → '-' 치환 (hyphen 선택 시)
  → (3) caseChange      — 대문자(upper) / 소문자(lower) 변환
  → (4) appendText      — 끝에 고정 문자열 추가
  → (5) truncateLength  — N자 미만으로 자르기
  → generationKey 경로의 대상 필드에 자동 입력
```

**예시**: `applyDataGeneration('Hello World!', 'hyphen', 'lower', '-doc', 20)`  
→ `'hello-world--doc'`

### DataGenerationEntry 속성

| 속성 | 타입 | 설명 |
|---|---|---|
| `generationKey` | string | 결과를 입력할 대상 필드 accessor (dot notation) |
| `onlyIfEmpty` | boolean | 대상 필드가 빈값인 경우에만 적용 |
| `dataReplacement` | `'none' \| 'hyphen'` | 공백·특수문자 → `-` 치환 |
| `caseChange` | `'none' \| 'upper' \| 'lower'` | 대/소문자 변환 |
| `appendText` | string | 변환 후 끝에 붙이는 텍스트 |
| `truncateLength` | number | N자 미만으로 자르기 |
| `stripHtml` | boolean | HTML 태그 제거 (editor만) |

### generationKey 경로 형식 (dot notation)

| 형식 | 예 | 의미 |
|---|---|---|
| `fieldKey` | `slug` | 동일 폼의 `slug` 필드 |
| `contentKey.fieldKey` | `form1.slug` | `contentKey=form1`인 폼의 `slug` |
| `tabKey.contentKey.fieldKey` | `tab1.form1.slug` | 탭 내부 폼의 `slug` |

### 세부 설정 UI

1. **섹션 헤더** — "데이터생성" + "추가" 버튼
2. **세트 목록** (GenerationEntryRow):
   - 생성 KEY input | 빈값일 때만 체크박스 (`onlyIfEmpty`)
   - `fieldType='input'`: 데이터변경 + 문자변경 + 텍스트추가 + 글자자르기
   - `fieldType='editor'`: HTML제거 + 글자자르기만

### 특수 기능

- **레거시 마이그레이션**: 구버전 `generationKey` 단일 속성 → 첫 로드 시 `dataGenerations[0]`으로 자동 변환
- **다중 세트**: "추가" 버튼으로 무제한 생성, 각 세트 독립적

---

## B. 공통 — _FieldOptions

**파일:** `builder/fields/_FieldOptions.tsx`  
**포함 필드:** SelectField, RadioField, CheckboxField, ButtonField, ColorField  
**SLUG 탭:** SelectField 전용 (`showSlugTab={true}` prop으로 활성화)

### 세 가지 모드 (택일)

| 모드 | 저장 속성 | 렌더러 처리 | 사용 가능 필드 |
|---|---|---|---|
| **수동** | `options: string[]` ("텍스트:값" 형식) | 배열 그대로 사용 | 전체 |
| **공통코드** | `codeGroupCode: 'GROUP_CODE'` | `resolveOptions()`로 실시간 로드 | 전체 |
| **SLUG** | `optionSlug`, `optionValueKey`, `optionTextKey`, `optionOrderKey`, `optionOrderDir` | `flattenPageDataItem()` 후 flat key 접근, 정렬은 FE에서 처리 | SelectField 전용 |

**탭 전환 시 나머지 값 전부 초기화**:
- 수동 선택: `codeGroupCode`, `optionSlug`, `optionValueKey`, `optionTextKey`, `optionOrderKey`, `optionOrderDir` 초기화
- 공통코드 선택: `options`, `optionSlug`, `optionValueKey`, `optionTextKey`, `optionOrderKey`, `optionOrderDir` 초기화
- SLUG 선택: `options`, `codeGroupCode` 초기화

### 세부 설정 UI

1. **수동/공통코드/SLUG 탭** — 탭 전환 시 초기화
2. **수동 탭** — OptionInputRows: "텍스트:값" 쌍 입력 + 기본값 selectbox
3. **공통코드 탭** — CodeGroupSelector + 기본값 selectbox
4. **SLUG 탭** (SelectField 전용) — SlugSelectField(연결 SLUG 선택) + Value 키 input + Text 키 input + 정렬 Key input + 정렬 방향 selectbox (ASC/DESC/없음)
   - ⚠️ Value 키 · Text 키 · 정렬 Key 세 가지 모두 `flattenPageDataItem` 기준 flat key로 입력 (예: `prdNm`)

---

## C. 공통 — SlugSelectField

**파일:** `builder/fields/SlugSelectField.tsx`  
**사용처:** CategoryField, ActionButtonField 내부

### Props

| prop | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `value` | string \| null | - | 현재 선택된 slug |
| `slugOptions` | `{ id: string; slug: string; name: string }[]` | - | 선택 가능한 slug 목록 |
| `label` | string | `'연결 Slug'` | 라벨 텍스트 |
| `required` | boolean | false | 필수 표시 |
| `emptyLabel` | string | `'선택 안 함'` | 빈값 옵션 텍스트 |
| `formatDisplay` | `(opt) => string` | - | 선택 후 표시 텍스트 커스터마이징 |

### 렌더러 동작

```
검색어 타이핑 → name/slug 실시간 필터링 → 드롭다운 표시 (name (slug) 형식)
선택 → formatDisplay? 커스텀 텍스트 : 기본 "name (slug)"
선택 해제 → ✕ 버튼 클릭 → value = null
Escape 키 → 입력창 원상복구 (선택값 유지)
외부 클릭 → 드롭다운 닫기
```

---

## 검증 체크리스트

필드 추가·수정 시 이 문서와 함께 확인:

- [ ] **00-2 § 10** — 새 필드 타입 추가 7단계 체크리스트
- [ ] `_FieldBase` props 올바르게 전달 (`colSpanMode`, `rowSpanConfig` 등)
- [ ] options 기반 필드라면 `_FieldOptions` 재사용
- [ ] 데이터 자동생성 필요하면 `DataGenerationSection` 재사용
- [ ] Slug 선택 필요하면 `SlugSelectField` 재사용
- [ ] `editor` 타입이면 SSR 금지 (`dynamic import + ssr: false`)
- [ ] i18n 지원 필드라면 `*MsgKey` 속성 + `MessageKeySelector` 분기
- [ ] connType 변경 로직 있다면 변경 시 관련 속성 초기화 처리
- [ ] 파일 업로드 필드라면 Edge 버그 우회 (`inputRef + onClick`) 적용
- [ ] 파일 저장 후 `PATCH /page-files/link` 호출 여부 확인
- [ ] value 형태가 쉼표 구분 string(checkbox/button 다중)인지 number[] (파일)인지 확인
- [ ] preview 모드에서 onChange=undefined, pointer-events-none 처리 확인
