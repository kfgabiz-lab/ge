# BO 로그인 보안성검토 매트릭스

> 작성일: 2026-08-06
> 대상: bo(관리자 프론트) + bo-api `AuthController`(`/api/v1/auth`) 로그인/인증 플로우 전체
> 특이사항: BO 로그인은 DB `menu` 테이블의 메뉴 트리에 속하지 않는 **별도 인증 플로우**다. 따라서 표준 매트릭스의 "메뉴" 열 대신 **로그인 플로우 6단계**를 열로 사용한다.
> 체크리스트 카테고리(A~J)는 `docs/보안성검토/ckecklist.md`와 동일한 분류 체계를 재사용하되, 세부 정의는 인증/토큰/세션 맥락으로 재작성했다. 로그인 플로우 특성상 **B(인증/인가)와 C(민감정보 노출)가 검토 비중의 핵심**이며, 나머지 카테고리는 보조 관점이다.
> 표기: `PASS`(이상없음) / `FAIL`(이슈발견, 상세는 "발견 이슈" 섹션에) / `-`(미분석) / `N/A`(해당사항 없음)

---

## 매트릭스

| 체크리스트 | ①ID/PW 로그인 | ②TOTP 등록(QR) | ③TOTP 검증 | ④토큰 갱신(Refresh) | ⑤로그아웃 | ⑥클라이언트 인증상태 관리 |
|---|---|---|---|---|---|---|
| A. 인젝션 / XSS | ~~FAIL~~ **PASS** | PASS | PASS | PASS | PASS | PASS |
| B. 인증/인가 (IDOR 포함) | FAIL | FAIL | FAIL | FAIL | FAIL | FAIL |
| C. 민감정보 노출 | ~~FAIL~~ **PASS** | FAIL | PASS | FAIL | PASS | FAIL |
| D. 서버측 요청위조/설정 보안 | FAIL | ~~FAIL~~ **PASS** | ~~FAIL~~ **PASS** | PASS | PASS | ~~FAIL~~ **PASS** |
| E. 오픈 리다이렉트/CSRF/Clickjacking | ~~FAIL~~ **PASS** | PASS | PASS | FAIL | FAIL | ~~FAIL~~ **PASS** |
| F. 입력 검증/비즈니스 로직 남용 | FAIL | FAIL | FAIL | FAIL | PASS | PASS |
| G. 조회/열거(Enumeration) 안전성 | ~~FAIL~~ **PASS** | PASS | FAIL | PASS | PASS | N/A |
| H. 서드파티/공급망 | FAIL | PASS | PASS | PASS | PASS | FAIL |
| I. 캐시/가용성 | FAIL | FAIL | PASS | FAIL | PASS | FAIL |
| J. 개인정보/컴플라이언스 | FAIL | PASS | FAIL | FAIL | FAIL | FAIL |

**집계: 총 60셀 — PASS 29 / FAIL 30 / N/A 1 (미분석 0) — 2026-08-08 수정 반영, 최초 분석 시점 대비 8셀 PASS 전환**

> 판정 근거는 bo(프론트)·bo-api(백엔드) 양측 리뷰 결과를 병합한 것이다(한쪽이라도 FAIL이면 FAIL). 디스패처가 Critical 항목 전건을 소스 직접 Read로 재확인했다.
> ⚠️ **모드 분기 전제**: `ls.redis-enabled` 값에 따라 인증 방식이 완전히 갈린다(`SecurityConfig.java:58`). `false`(local/developer) = JWT Stateless + tempToken, `true`(dev) = Redis 세션. 아래 이슈에는 어느 모드에 해당하는지 표기했다.

---

## 대상 파일 / 엔드포인트

| # | 플로우 단계 | bo-api 엔드포인트 | bo 프론트 파일 |
|---|---|---|---|
| ① | ID/PW 로그인 | `POST /api/v1/auth/login` | `bo/src/app/admin/login/page.tsx`, `bo/src/components/auth/login-form.tsx` |
| ② | TOTP 등록(QR) | `POST /api/v1/auth/totp/qr`, `POST /api/v1/auth/totp/registrations` | `bo/src/components/auth/totp-setup-form.tsx` |
| ③ | TOTP 검증 | `POST /api/v1/auth/totp/sessions` | `bo/src/components/auth/totp-verify-form.tsx` |
| ④ | 토큰 갱신(Refresh) | `POST /api/v1/auth/refresh` | `bo/src/lib/api.ts` (토큰 첨부/자동갱신 인터셉터) |
| ⑤ | 로그아웃 | `POST /api/v1/auth/logout` | `bo/src/components/auth/auth-provider.tsx`, `bo/src/store/auth-store.ts` |
| ⑥ | 클라이언트 인증상태 관리 | (해당 없음 — 클라이언트 전용) | `bo/src/store/auth-store.ts`, `bo/src/components/auth/auth-provider.tsx` |

> 각주 1: `bo/src/app/admin/_login/page.tsx`는 `login-form.tsx`를 그대로 노출하는 페이지지만, Next.js App Router 규칙상 언더스코어(`_`) 접두 폴더는 라우팅에서 제외되어 **실제로는 접근 불가능한 죽은 코드**다. 보안 이슈가 아니라 **정리 후보**로만 기록한다(검토 대상에서 제외).
> 각주 2: bo-api 측 실제 구현 클래스(`AuthService`, JWT 토큰 프로바이더, `SecurityConfig` 등)는 분석 단계에서 `AuthController`로부터 추적하여 확정한다. 현 시점에서는 엔드포인트 6개만 확정 상태.

---

## 카테고리(A~J) 정의 — BO 로그인 맥락

| 분류 | 정의 |
|---|---|
| A. 인젝션 / XSS | 로그인 ID·TOTP 코드 등 인증 입력값이 쿼리/로그/에러메시지 조합에 그대로 들어가는지, 로그인 실패 메시지에 입력값이 그대로 반사되어 렌더링되는지(Reflected XSS), TOTP QR(otpauth URI) 생성 시 사용자 제어 문자열이 이스케이프 없이 삽입되는지 |
| B. 인증/인가 (IDOR 포함) ★핵심 | 비밀번호 검증 로직(해시 알고리즘, 타이밍 안전 비교), **TOTP 단계 우회 가능성**(1차 로그인 토큰만으로 보호 자원 접근 가능한지), TOTP 등록 엔드포인트가 타 계정의 QR/등록을 수행할 수 있는지(IDOR), Refresh 토큰의 소유자 검증·재사용 탐지·회전(rotation) 여부, 로그아웃 후 토큰 무효화 실효성, 인증 관련 엔드포인트의 `permitAll` 범위가 과도한지 |
| C. 민감정보 노출 ★핵심 | 응답 바디에 password hash·TOTP secret·salt가 포함되는지, **TOTP secret이 QR 발급 응답/클라이언트 상태에 평문으로 남는지**, 토큰(Access/Refresh)의 저장 위치(localStorage vs HttpOnly 쿠키 vs 메모리)와 XSS 노출 위험, JWT 페이로드에 불필요한 개인정보가 담기는지, 인증 실패 로그·콘솔에 자격증명이 찍히는지, `NEXT_PUBLIC_` 환경변수에 인증 관련 시크릿 혼입 여부 |
| D. 서버측 요청위조/설정 보안 | 인증 엔드포인트에 대한 CORS 허용 출처 범위(`allowCredentials` 조합 포함), 인증 실패 시 스택트레이스/내부 경로 노출, 로그인 페이지에 적용되는 보안 헤더(HSTS, X-Content-Type-Options, X-Frame-Options/frame-ancestors, CSP) 존재 여부 |
| E. 오픈 리다이렉트/CSRF/Clickjacking | 로그인 성공 후 이동 경로(`redirect`/`returnUrl` 등 쿼리 파라미터)가 검증 없이 `router.push`/`window.location`에 쓰이는지(오픈 리다이렉트), 토큰을 쿠키로 보관한다면 CSRF 방어(SameSite, Origin 검증) 여부, 로그인 화면 iframe 삽입 차단 여부(로그인 Clickjacking) |
| F. 입력 검증/비즈니스 로직 남용 | **로그인 시도 Rate Limiting / 계정 잠금(브루트포스 방어)**, TOTP 코드 재시도 횟수 제한 및 코드 1회성 소비(replay 방지), TOTP 시간 윈도우(허용 skew) 과도 여부, 서버측 Bean Validation 존재 여부, 비밀번호 정책 강제 지점 |
| G. 조회/열거(Enumeration) 안전성 | 존재하지 않는 ID와 비밀번호 오류의 **응답 메시지·HTTP 상태·응답 시간 차이로 계정 존재 여부가 식별되는지**(User Enumeration), TOTP 등록 여부가 응답으로 구분되어 정찰 정보를 주는지 |
| H. 서드파티/공급망 | TOTP 라이브러리·JWT 라이브러리(bo-api `build.gradle`)와 QR 생성/인증 관련 npm 패키지(bo `package.json`)의 알려진 취약점 및 버전 노후화, lockfile 관리 |
| I. 캐시/가용성 | 로그인 페이지·인증 응답이 캐시되어(브라우저/프록시/Next.js 캐시) 다른 사용자에게 인증 정보가 섞여 나갈 가능성, 인증 응답의 `Cache-Control` 헤더, Refresh 토큰 갱신 실패 시 무한 재시도 루프로 인한 자기 DoS 가능성 |
| J. 개인정보/컴플라이언스 | 로그인 성공/실패 이력에 저장되는 정보 범위(비밀번호·토큰 평문 저장 금지), 접속 로그의 IP·User-Agent 수집 근거와 보존기간, 관리자 계정 PII 처리 |

---

## 발견 이슈

<!-- 형식: #번호 [파일:라인] 설명 — 카테고리 / 심각도(Critical|Warning|Info). 번호는 문서 전체에서 순차 부여(①~⑥ 단계 순서), 앞으로 이 번호로 항목을 지칭한다. -->

### ① ID/PW 로그인

- ✅ **#1 [bo/src/components/auth/login-form.tsx:57]** 로그인 폼 `defaultValues`에 실제 관리자 비밀번호 `"P@ssw0rd123"`가 하드코딩되어 있다. 이 값은 클라이언트 번들에 그대로 포함되므로, 로그인 페이지에 접근할 수 있는 누구나(비인증 포함) 비밀번호 필드에 자동 입력된 관리자 비밀번호를 획득할 수 있다. — C / **Critical** — ⚠️ **수정 이력 정정(2026-08-06)**: 최초 수정(`password: ""`)이 이후 시점에 코드에서 사라져 있던 것을 `#보안성검토수정`(#13/#48/#49) 작업 중 `security-fix-executor`가 재확인·보고함(원인 불명 — git 이력상 커밋된 적 없는 로컬 변경이었음). **재수정 완료**: `password: ""`로 다시 변경, `git diff`로 실제 반영 확인. bo 개발서버는 Fast Refresh로 자동 반영(별도 재기동 불필요)
- ✅ **#2 [bo-api/src/main/resources/application.yml:31]** JWT 서명 시크릿 `app.jwt.secret`이 소스에 평문 하드코딩(`bo-backoffice-ge-america-secret-key-256bit-secure-2026`)되어 형상관리에 커밋되어 있다. 환경변수 오버라이드 없이 이 값이 기본으로 사용되며, 시크릿을 아는 사람은 임의 이메일·임의 role의 accessToken을 위조해 전체 인가 체계를 우회할 수 있다. — C / **Critical** — ✅ **수정 완료(2026-08-06)**: `secret: ${JWT_SECRET}`로 환경변수 필수화 + 신규 랜덤 시크릿으로 교체, 재기동 검증 완료
- ✅ **#3 [bo-api/src/main/java/com/ge/bo/service/AuthService.java:160-165 vs 138-142]** 계정이 존재하고 비밀번호만 틀린 경우 `"비밀번호 N회 실패 하셨습니다. 5회 실패 시 계정 비활성화됩니다."`를, 존재하지 않는 이메일에는 `"ID or password가 일치하지 않습니다."`를 반환한다. 응답 메시지만으로 계정 존재 여부가 100% 식별된다(추가로 비활성 계정은 403 `ACCESS_DENIED`, 승인대기는 403 `PENDING_APPROVAL`로 상태까지 구분됨). 유효 관리자 계정 목록을 열거한 뒤 잠금 임계치 이하로 분산 시도하는 공격이 가능하다. — G / **Critical** — ✅ **수정 완료(2026-08-06)**: 이메일 미존재/비활성/비밀번호오류/잠금 4개 분기 응답을 `401 INVALID_CREDENTIALS` + 동일 메시지로 통일(승인대기 `PENDING_APPROVAL`은 인증 성공 후에만 도달하는 분기라 열거 벡터가 아니므로 유지). 실제 요청으로 응답 동일성 검증 완료
- ✅ **#4 [bo/next.config.ts:29-37, bo/src/middleware.ts]** 응답 보안 헤더에 `X-Frame-Options` 및 CSP `frame-ancestors`가 없다(nosniff/HSTS/Referrer-Policy/CORP/COOP만 설정). 로그인 화면을 외부 사이트 iframe에 삽입하는 Clickjacking(자격증명 탈취형 UI redressing)을 차단하지 못한다. — E / Warning — ✅ **수정 완료(2026-08-06)**: `next.config.ts` headers() + `middleware.ts` applySecurityHeaders()에 `X-Frame-Options: SAMEORIGIN`, `Content-Security-Policy: frame-ancestors 'self'` 추가(전 경로 적용이라 ②-D/③-D/⑥-D,E 동일 이슈도 함께 해결). fo에도 동일 적용. 재기동 후 응답헤더 확인 완료
- ✅ **#5 [bo-api/src/main/java/com/ge/bo/dto/LoginRequest.java:10-13, AuthController.java:38]** `LoginRequest`에 Bean Validation 애노테이션이 전혀 없고 컨트롤러에도 `@Valid`가 없다. 서버측 형식·길이 검증이 `AuthService.java:122-127`의 수동 null/blank 체크뿐이라 이메일 형식·최대 길이 제한이 서버에서 강제되지 않는다(계정 잠금 `AuthService.java:153-158`과 reCAPTCHA `AuthService.java:134`는 존재해 브루트포스 자체는 일부 방어됨). — F / Warning — ✅ **수정 완료(2026-08-06)**: `email`(NotBlank/Size(30)/Pattern 영숫자), `password`(NotBlank/Size(4~100)) 검증 추가 + 컨트롤러 `@Valid` 추가. `recaptchaToken`은 기존 전용 에러 처리 유지 위해 미적용. 실제 요청으로 400 검증 확인 완료. (①-F의 reCAPTCHA 테스트키/Rate Limiting 미비 Critical 2건은 미해결로 FAIL 유지)
- ✅ **#6 [bo-api/src/main/java/com/ge/bo/service/RecaptchaService.java:42]** `String.format(VERIFY_URL, secretKey, token)`으로 사용자 제어 값인 `token`을 URL 인코딩 없이 쿼리스트링에 직접 조합한다. `&`가 포함된 토큰으로 siteverify 요청에 임의 파라미터를 덧붙이는 파라미터 인젝션이 가능하다. — A / **Warning** — 🔄 **교차검증 완료(2026-08-06)**: 세션 에이전트(최초 Info 판정) + `java-security-reviewer`(독립 재분석) 교차검증 결과 Warning으로 정정. `ExternalApiClient`가 Spring `RestClient` 기본 인코딩모드(`TEMPLATE_AND_VALUES`)를 사용해 `&`/`=`를 인코딩하지 않음을 코드로 확인 — 파라미터 인젝션은 실제 성립. 단 SSRF(호스트 고정) 및 CRLF/요청분리(`HttpURLConnection`+`java.net.URI` 계층에서 차단)는 불가 확인. ✅ **수정 완료(2026-08-06)**: 쿼리스트링 → `MultiValueMap` 폼 바디 POST로 전환(#47과 함께 처리). **실제 인젝션 재현 테스트로 검증**: `recaptchaToken="abc&secret=hacked&foo=bar"` 전송 시 로그에 `response=[abc&secret=****&foo=bar]`로 `response` 필드 값 하나에 전체가 온전히 담김(별도 파라미터로 분리 안 됨) 확인
- **#7 [bo-api/src/main/java/com/ge/bo/entity/LoginLog.java:22-50]** 로그인 이력에 비밀번호·토큰은 저장하지 않아(양호) 이메일/IP/User-Agent/상태/실패사유만 남지만, 해당 로그의 보존기간 설정이나 자동 파기(purge) 로직이 코드베이스에 존재하지 않는다. IP·UA는 개인정보에 해당하므로 보존기간 정의가 필요하다. — J / **Warning** — ⬆️ **재평가(2026-08-06)**: `#보안성상세분석` 3자 교차검증(java-security-reviewer+nextjs-security-reviewer+세션 에이전트) 결과 Info→Warning. 단독 위험은 아니나 #13(접근권한 미제한)의 피해규모를 시간에 비례해 키우는 증폭요인으로 판단

- **#8 [bo/next.config.ts:6 + bo-api/src/main/resources/application-dev.yml:93, application-developer.yml:92, application-local.yml:80]** reCAPTCHA가 **구글 공개 테스트 키 쌍**으로 고정되어 봇 방어가 완전히 무력화되어 있다. 프론트 site key `6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI`와 서버 secret key `6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe`는 구글이 문서에 공개한 자동화 테스트용 키로, **어떤 토큰을 보내도 항상 `success: true`를 반환**한다. 따라서 `AuthService.java:134`의 `recaptchaService.verify()`는 사실상 no-op이다. 더구나 site key는 `.env`가 아니라 `next.config.ts`의 `env` 블록에 하드코딩되어 **모든 프로필 빌드에 동일하게 박히며**(운영 빌드에서 교체될 코드 경로가 없음), secret key는 dev 프로필에도 테스트 키가 들어가 있다. 추가로 SSO 모드(`ls.isApiLogin=true`, dev 기본값)는 `AuthService.java:129-131`의 `return ssoLogin(...)`이 `:134`의 reCAPTCHA 검증보다 **앞서 실행되어 캡차를 아예 건너뛴다.** — F / **Critical** — ⚠️ **구조 개선 완료 / 실키 교체는 별도 대기(2026-08-07)**: 이 항목은 **부분 해결(FULL FIX 아님)**이다. 사용자가 실제 reCAPTCHA 운영 키를 아직 보유하지 않아 **"테스트 키 → 실키 교체"는 이번 범위에서 명시적으로 제외**되었다. 따라서 *"어떤 토큰이든 `success:true`"* 라는 원 지적의 핵심(봇 방어 무력화)은 **여전히 유효**하며, 실키 발급 후 값 교체가 남아 있다. 이번에 처리한 것은 **교체를 가로막던 구조적 결함 2가지**다.
  - **① 환경변수 주입 가능화** — (a) `bo/next.config.ts:5-7`의 `env: { NEXT_PUBLIC_RECAPTCHA_SITE_KEY: "..." }` 블록 **삭제**. Next.js는 `NEXT_PUBLIC_` 접두 변수를 `.env` 파일에서 자동으로 클라이언트에 노출하므로 이 블록은 불필요할 뿐 아니라 **배포 시 주입되는 환경변수를 코드가 덮어써(override) 실키 교체 경로를 원천 차단**하고 있었다. 값은 `bo/.env.local`로 이관(회귀 방지를 위해 동일한 테스트 키 값 유지). (b) bo-api 3개 프로필의 `recapchaKey : "6LeIx..."` 하드코딩을 같은 파일에서 이미 쓰이는 컨벤션(`ctpFileDownCode: ${CTP_FILEDOWN_CODE:}`, `azure-search.api-key: ${AZURE_SEARCH_API_KEY:}`)과 동일한 `recapchaKey: ${RECAPTCHA_SECRET:6LeIx...}` 형태로 전환. **이제 실키 발급 시 코드 수정 없이 `bo/.env.local` 1줄 + `RECAPTCHA_SECRET` 환경변수만 바꾸면 된다.**
  - **② SSO 모드 캡차 우회 제거** — `AuthService.java`에서 `recaptchaService.verify(...)` 호출을 `if (isApiLogin) return ssoLogin(...)` 분기보다 **앞으로 이동**. 로컬/SSO 양쪽 모두 캡차를 통과해야 다음 단계로 진행된다.
  - **검증**: (a) bo 재기동 로그 `Environments: .env.local`, `curl /bo/admin/login` HTML의 script 34개를 전수 fetch해 클라이언트 청크 `/bo/_next/static/chunks/src_components_auth_5b8043d1._.js`에서 site key `6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI` 발견 → env 블록 제거 후에도 회귀 없음. (b) 미존재 계정 + `recaptchaToken:"dummy"` 로그인이 `RECAPTCHA_FAILED`가 아닌 **401 `INVALID_CREDENTIALS`** 로 통과 → `${RECAPTCHA_SECRET:...}` 플레이스홀더 정상 해석 확인. (c) `LS_ISAPILOGIN=true` 오버라이드 재기동(로그 `isApiLogin=true`) 후 토큰 **누락**→400 `RECAPTCHA_REQUIRED`, **빈문자열**→400 `RECAPTCHA_REQUIRED`, **토큰 있음**→`401 "SSO 서버 연결에 실패했습니다."`(캡차 통과 후 SSO 도달, 로컬에 SSO 서버 없음). ⚠️ *검증 방식 명시*: `application-dev.yml`이 사내 실 인프라(DB `10.153.11.120`)를 가리켜 로컬 구동이 위험하므로 **dev 프로필은 띄우지 않고** `local`에 `LS_ISAPILOGIN=true`만 임시 오버라이드해 재현했고, 검증 후 원상복구(`isApiLogin=false`)했다.
- ✅ **#9 [bo-api 전 소스(`RateLimit|bucket4j|resilience4j` grep 0건) + AuthService.java:153-158]** 로그인에 **IP 단위 Rate Limiting이 전혀 없다.** 존재하는 방어는 계정별 누적 실패 잠금(기본 5회)뿐이라, 위 reCAPTCHA 무력화와 결합하면 수천 개 계정에 3회씩 대입하는 계정 스프레이(password spraying)가 무제한 가능하다. — F / **Critical** — ⚠️ **전제 정정(2026-08-07)**: 이 서술이 근거로 삼은 "계정별 누적 실패 잠금"은 **작성 당시 실제로는 동작하지 않았다** — `LoginAdminService.incrementFailure()`가 카운터만 올리고 `setActive(false)`를 하지 않아 5회를 넘겨도 계정이 잠기지 않았다(`#54` 참조). 즉 당시 로그인 방어는 사실상 **전무**했다. `#54` 수정으로 계정 잠금은 실제 동작하게 되었으나, **계정 잠금과 rate limiting은 방어 축이 달라(수직 vs 수평) 서로 대체되지 않는다** — 계정 스프레이(다수 계정에 각 소량 시도)는 잠금 임계치를 절대 채우지 않으므로 여전히 무제한 가능하다. `#보안성상세분석`(java-security-reviewer 재확인, 2026-08-07)에서도 인프라 레벨(nginx/WAF/게이트웨이) 방어 근거가 저장소에 전혀 없음을 재확인 — **IP 단위 Rate Limiting 부재라는 이 항목의 지적 자체는 미해결로 유지.** — ✅ **수정 완료(2026-08-07)**: bucket4j(`com.bucket4j:bucket4j_jdk17-core:8.14.0`, Apache 2.0 — 구 좌표 `bucket4j-core`는 8.10.1에서 중단되고 8.11부터 JDK별 아티팩트로 분리됨) 기반 `LoginRateLimitFilter` 신규(`bo-api/src/main/java/com/ge/bo/security/LoginRateLimitFilter.java`). `POST /api/v1/auth/login` **정확히 이 경로만** 대상으로 IP당 **1분 20회**(토큰버킷 greedy refill) 제한, 초과 시 `filterChain.doFilter()` 없이 **429 `RATE_LIMIT_EXCEEDED`** 로 즉시 종료(`GlobalExceptionHandler`와 동일한 `status/error/message/timestamp` 필드 구성). IP는 **`request.getRemoteAddr()`만 사용하고 `X-Forwarded-For`는 참조하지 않는다** — 앞단에 신뢰할 프록시가 없는 현 배포 구조에서 헤더 신뢰는 곧 우회이므로 `#15`(XFF 무검증 신뢰)와 의도적으로 분리했고, **`#15`는 이번 범위에서 손대지 않아 여전히 미해결**이다. `SecurityConfig.filterChain()`의 **양쪽 분기 전부**(redis 세션 모드 = `UsernamePasswordAuthenticationFilter` 앞 / JWT 모드 = `JwtAuthenticationFilter` 앞)에 등록했으며, `@Component` Filter 빈이 서블릿 컨테이너에 중복 자동등록되는 것은 `FilterRegistrationBean.setEnabled(false)`로 차단했다. **검증**: (1) 25회 연속 호출 시 `1~21=401 → 22~25=429` 실발생, 429 바디 `{"status":429,"error":"RATE_LIMIT_EXCEEDED","message":"요청이 너무 많습니다. 잠시 후 다시 시도해주세요.","timestamp":"2026-08-07T15:39:20.525852"}` 캡처(21회까지 통과는 greedy refill 3초당 1토큰의 정상 동작), (2) **컨트롤러 미도달 증명** — 총 51회 요청 중 서버 로그 차단 24건 + `login_log` 신규 27행(psql 직접 조회, 24+27=51 정확 일치)으로 차단분은 로그를 남기지 않음 확인, (3) 429 응답에 `Access-Control-Allow-Origin` 포함(CorsFilter 뒤 배치라 BO 프론트가 바디를 읽을 수 있음), (4) **XFF 스푸핑 우회 불가** — 버킷 소진 상태에서 `X-Forwarded-For` 3종 변경해도 전부 429, (5) **대상 경로 한정** — 소진 상태에서 `GET /health` 30회 전부 200, `POST /auth/refresh` 401, `GET /auth/login` 405, `POST /auth/totp/sessions` 6회 400(429 없음), (6) **정상 로그인 미차단** — `comlbg` 정상 자격증명 200 + accessToken 발급, (7) **redis 세션 모드도 실기동 검증** — 로컬 Redis(6379)로 `LS_REDIS_ENABLED=true` 오버라이드 기동 후 동일하게 `1~21=401 → 22~25=429` 재현, 필터체인 로그에 `LoginRateLimitFilter` 등록 확인, (8) 회귀 — `#3`(3케이스 동일 `401 INVALID_CREDENTIALS`)·`#54`(오답 5회에 psql `failed 1→5`, `is_active=false` 전환)·`#5`(빈 이메일 `400 VALIDATION_FAILED`) 미파손. 테스트로 생긴 `login_log` 행과 계정 상태는 전부 원복(psql 스냅샷 대조). ⚠️ **한계**: (a) 로컬 요청은 전부 `::1`이라 **서로 다른 실제 IP별 독립 버킷 동작은 검증 불가**, (b) `ConcurrentHashMap`에 만료/축출이 없어 다수 IP 유입 시 맵이 계속 증가(후속 보완 대상), (c) 인메모리이므로 **다중 인스턴스 배포 시 실효 한도가 인스턴스 수만큼 곱해짐**(단일 프로세스 배포 전제로 승인된 설계) — ⚠️ **검증 정정(2026-08-08)**: 최초 `#9` 검증은 **8080(bo-api)에 직접 curl**한 것이어서, 실제 브라우저 경로에서만 드러나는 문제를 잡아내지 못한 한계가 있었음. 실제 배포 형태(bo Next.js 3001 → bo-api 8080 프록시, 동일 호스트)에서는 모든 사용자의 `request.getRemoteAddr()`가 bo Next.js 서버 주소(사실상 127.0.0.1/::1)로 잡히므로, `LoginRateLimitFilter`의 IP 버킷은 "출처별"이 아니라 **사이트 전체 공유 버킷**으로 동작한다. 이에 `#15` 수정과 묶어 `LoginRateLimitFilter.java:30`의 `MAX_ATTEMPTS_PER_MINUTE`를 **20 → 100**으로 상향(사이트 전체 공유 기준, 사용자에게 AskUserQuestion으로 직접 확인 후 명시 승인됨). 필터 구조·버킷 키(`getRemoteAddr()`)는 변경 없음. **재검증(전부 3001 경유)**: 순차 30회 호출 → 429 발생 0건(기존 20 기준이면 22번째부터 429였을 구간 통과), 순차 누적 115회 → 429 발생 0건(greedy refill 약 1.67건/초가 순차 속도를 따라잡음), **병렬 버스트 130회/3초 → 400 x65 / 429 x65**로 100회/분 한도가 실제로 강제됨을 확인.
- ✅ **#10 [bo-api/src/main/java/com/ge/bo/controller/AdminController.java(@PreAuthorize 0건) + service/AdminService.java:146]** 로그인 자격증명의 생명주기를 관리하는 관리자 API에 **인가 검증이 누락**되어 있다. `SecurityConfig.java:145` 주석은 "관리자 API — @PreAuthorize로 SUPER_ADMIN 제한"이라 명시하지만 `AdminController`에는 `@PreAuthorize`가 **단 한 건도 없어**(다른 11개 컨트롤러는 사용 중이므로 명백한 누락) `.authenticated()`만 적용된다. 즉 **최하위 권한 계정을 포함한 모든 로그인 사용자**가 `POST /api/v1/admins/{id}/reset-password`, `PATCH /api/v1/admins/{id}`(role 변경), `DELETE /api/v1/admins/{id}`를 호출할 수 있다. 여기에 `AdminService.java:146`의 `String tempPassword = "test12345";`가 결합된다 — 비밀번호 초기화가 **전 계정 공통 고정 문자열**을 설정하고 이를 응답 바디로 반환하므로, 임의의 인증 사용자가 SUPER_ADMIN 비밀번호를 알려진 값으로 바꿔 계정을 탈취할 수 있다. `getAdminById`(`:41`)에 있는 `is_system` 보호 필터가 `resetPassword`(`:141`)/`updateAdmin`(`:115`)/`deleteAdmin`(`:177`)에는 적용되지 않아 시스템 계정도 대상이 된다. — B / **Critical** — ✅ **수정 완료(2026-08-06)**: 5중 조치 — ① `AdminController` 클래스 레벨에 저장소 공통 컨벤션 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")` 적용, ② 단 `GET /admins/{id}/sites`만 `isSystemAdmin or isSelf(#id)` 메서드 레벨 예외(전 사용자가 쓰는 `header.tsx:61` SiteSelector 소비처 보존 — `PUT /{id}/sites`에는 예외 미부여), ③ `SecurityService.isSelf()` 신규 추가(`AdminRepository` 주입, `JwtAuthenticationFilter:43`이 principal에 email을 넣으므로 `authentication.getName()`=email 기준 비교), ④ `AdminService.createAdmin`/`updateAdmin`에 `isSystemRole()` 재사용한 시스템 역할 배정 차단(심층방어), ⑤ `resetPassword`의 고정 `"test12345"`를 `createAdmin`과 동일한 `UUID.randomUUID().toString().substring(0,12)`로 교체. 부수로 `SecurityConfig` 주석 2곳(redis/JWT 분기)의 "SUPER_ADMIN 제한"이라는 사실과 다른 서술을 실제 메커니즘(역할명이 아닌 `role.is_system=true` 기준)으로 정정하고, `bo/src/middleware.ts`의 `SYSTEM_ADMIN_PATHS`에 `/admin/settings/users`, `/admin/settings/roles`를 추가해 UI 계층도 정합화. **검증**: bo-api 재기동 후 curl 실검증 — (1) 시스템관리자(`comlbg`/`SYSTEM_ADMIN`, is_system=t) 토큰 → 목록·단건·타인 sites 전부 **200**, (2) 비시스템관리자(`compjj`/`SUPER_ADMIN`, is_system=f) 토큰 → 목록·단건·`reset-password`·`DELETE`·`PUT /{id}/sites` 5건 전부 **403** `{"error":"FORBIDDEN"}`, (3) isSelf 예외 — `compjj` 토큰으로 본인 `GET /admins/23/sites` **200**(사이트 1건 정상 반환), 타인 `GET /admins/1/sites`·`/admins/24/sites` **403**, (4) 시스템역할 배정 차단 — `PATCH /admins/23 {role:"SYSTEM_ADMIN"}` **400 INVALID_ROLE**, `POST /admins {role:"SYSTEM_ADMIN"}` **400**, `role:"OP_ADMIN"` 정상경로는 **200** 회귀 확인, (5) tempPassword 랜덤화 — `reset-password` 2회 호출 결과 `365daa5f-da1` / `ea69121e-d76`로 `test12345`가 아니며 매 호출 상이, (6) bo 재기동(3001) 후 `/bo/admin/settings/users`·`/roles`가 `bo_is_system` 쿠키 없으면 **307 → /admin/dashboard**, 쿠키 있으면 **200**, 비보호 경로(`menus`/`sites`/`dashboard`)는 **200** 회귀 정상
- ✅ **#55 [bo-api/src/main/java/com/ge/bo/service/TotpService.java:261-277]** 2차 인증(TOTP) 코드 검증에 시도 횟수 제한이 전혀 없었다. `verifyTotpCode()`는 오답 시 `TOTP_CODE_INVALID`만 반환하고 아무것도 누적하지 않으며, tempToken 유효시간 10분 동안 동일 토큰으로 무제한 재시도가 가능했다. 6자리 = 100만 조합에 검증기가 `setAllowedTimePeriodDiscrepancy(2)`로 5개 타임윈도우를 허용하므로 유효코드가 매 순간 5개씩 존재해, 비밀번호를 이미 확보한 공격자가 10분 내 자동화 요청으로 2FA를 뚫을 수 있었다(비밀번호 5회 잠금(`#54`)은 1차 인증에만 적용되어 2FA 단계를 보호하지 못함). — F / **Critical** — 🔍 **발견 경위**: `#54` 수정 후속으로 "1차 인증에는 잠금이 있는데 2차 인증에는 있는가"를 확인하다 발견 — ✅ **수정 완료(2026-08-07)**: `AdminUser.totpFailedAttempts` 컬럼 신규(L63-65), `TotpService`에 `MAX_TOTP_ATTEMPTS=3` 도입 — 코드 검증 **전에** 3회 초과면 `401 TOTP_MAX_ATTEMPTS`로 즉시 거부(정답이어도 거부), 오답 시 `LoginAdminService.incrementTotpFailure()`(`REQUIRES_NEW`)로 카운터 확정, 성공 시 4개 호출부(`confirm`/`confirmWithJwt`/`verify`/`verifyWithJwt`)에서 0으로 리셋. 계정 자체는 잠그지 않고 **해당 로그인 시도만 무효화**하는 설계라, `AuthService.tempLoginResponse()`(L412-414)에서 새 tempToken/세션 발급 시마다 `resetTotpFailure()`로 초기화(로컬/SSO 로그인 공용 경로). **검증**: 로컬 프로필 재기동 후 실제 TOTP 시크릿으로 코드를 계산해 curl 재현 — `/auth/totp/sessions` 오답 3회에 psql `totp_failed_attempts` 0→1→2→3 증가 확인, 4·5번째는 **정확한 코드(516493)를 넣어도** `401 TOTP_MAX_ATTEMPTS` 거부 + 카운터 추가 증가 없음 확인, 재로그인 시 0 리셋 후 정확한 코드로 `accessToken` 정상 발급 확인. `/auth/totp/registrations`(QR 최초등록) 경로도 동일하게 3회 제한·리셋·등록성공 재현. 회귀로 `#54`(5회 잠금 `t|1..t|4→f|5`)와 `#3`(미존재/비번오류/비활성 3케이스 모두 `401 INVALID_CREDENTIALS` 동일 메시지) 미파손 확인. ✅ **후속 보완(2026-08-07)**: 최초 구현은 `totp_failed_attempts`를 `int`(NOT NULL)로 선언해 `ddl-auto: update`가 기존 행 때문에 컬럼 추가에 실패하는 문제가 있었다(사용자 지적으로 발견). `AdminUser.totpFailedAttempts`를 `Integer`(nullable)로 변경하고, 값을 읽는 지점(`LoginAdminService.incrementTotpFailure()`, `TotpService.verifyTotpCode()` 임계치 체크) 2곳에 null-safe 처리(NULL은 0으로 취급)를 추가해 **NOT NULL 제약 없이도 정상 동작**하도록 수정. 이제 `ddl-auto: update`가 기존 행이 있어도 컬럼을 아무 문제 없이 추가할 수 있어 별도 수동 DDL이 필요 없다(dev의 `validate` 모드는 여전히 컬럼 존재 자체는 필요하므로 배포 전 `ALTER TABLE admin_user ADD COLUMN IF NOT EXISTS totp_failed_attempts integer;`만 실행하면 됨, DEFAULT/NOT NULL 불필요). **검증**: 로컬 DB에서 컬럼 NOT NULL 제약을 임시로 제거하고 테스트 계정의 `totp_failed_attempts`를 실제로 `NULL`로 만든 뒤 TOTP 오답을 제출 — 500 에러 없이 정상적으로 `400 TOTP_CODE_INVALID` 응답, DB 값이 `NULL→1`로 정상 증가함을 실측 확인(NULL을 0으로 처리하는 로직이 실제로 동작). 검증 후 제약·데이터 전부 원복
- **#11 [bo-api/src/main/java/com/ge/bo/config/SecurityConfig.java:88-97, 138-141]** permitAll 범위가 과도하다. `/api/v1/contents-batch/run-all`, `/catalog/run`, `/ssq/run`, `/certi/run` 4개 배치 트리거가 **redis 세션 모드·JWT 모드 양쪽 필터체인 전부**에서 무인증 전면 개방되어 있고(EAI 호출용이라는 주석 외에 IP 화이트리스트·서명·API 키 등 2차 검증이 코드에 없음, `ContentsBatchController`의 조회용 API 5개는 전부 `@PreAuthorize`가 있는데 이 4개 트리거만 없음), `/api/v1/cryptoTest/**`, `/api/v1/redisTest/**`도 무인증이다. — B / **Critical** — ⬆️ **재평가(2026-08-07)**: `#보안성상세분석`(java-security-reviewer+세션 에이전트 직접 확인)에서 Warning→Critical로 상향. 배치 트리거 4개는 **조회가 아니라 삭제(soft delete)·비노출 처리를 포함한 쓰기 경로**이며, 대상이 FO 공개 사이트가 소비하는 콘텐츠 테이블이라 피해 범위가 크다고 판단. ⚠️ **원문 서술 정정(2026-08-07, 최초 정정) 및 재정정(2026-08-07)**: 최초 정정 시 "cryptoTest는 permitAll 목록에 없어 항상 인증 필요"라고 적었으나 **이는 오류였다** — `#14` 심층분석 중 `SecurityConfig.java:134`를 다시 직접 읽어 확인한 결과 `.requestMatchers("/api/v1/cryptoTest/**").permitAll()`이 **JWT 모드(local/developer 프로필) 분기에 명확히 존재**한다. 즉 **local/developer 프로필에서는 `cryptoTest`가 실제로 익명 접근 가능**(로그인 없이 `/api/v1/cryptoTest/enc`, `/dec` 호출 가능)하고, redis 모드(dev/prd)에서만 permitAll 목록에 없어 인증이 필요하다 — **프로필에 따라 다르다**는 것이 정확한 서술이다. `redisTest`에 대한 정정(local/developer는 `@ConditionalOnProperty`로 빈 자체가 없어 404, dev/prd는 빈은 있지만 permitAll 목록에 없어 인증 필요)은 그대로 유효하다. `#57`(CryptoTestController 인가) 항목은 이 사실을 반영해 심각도 재검토가 필요함 — `#14` 처리 시 함께 정리 예정. 미착수. — 🔄 **재검토 완료 / 미해결 유지(2026-08-08)**: `#보안성검토`(디스패처 직접 Read + `java-security-reviewer` 교차검증)로 현재 코드 재확인. **핵심 지적은 그대로 미해결** — 배치 트리거 4개는 `SecurityConfig.java:94-97`(redis 분기)·`:137-140`(JWT 분기) **양쪽 전부 permitAll** 이고, `ContentsBatchController.java:50/59/68/81`의 트리거 4개 메서드에 `@PreAuthorize`가 없는 반면 조회용 5개(`:90/106/124/137/171`)에는 전부 붙어 있는 구조도 동일하다. 2차 검증 부재도 재확인 — 프로젝트의 `OncePerRequestFilter` 구현체는 `SiteContextFilter`/`TransactionLogFilter`/`JwtAuthenticationFilter`/`LoginRateLimitFilter` 4개뿐이고 `HandlerInterceptor`/`WebMvcConfigurer` 구현체는 0건이라 IP 화이트리스트·HMAC 서명·API 키를 걸 지점 자체가 없으며, `LoginRateLimitFilter.java:28-29, 38-42`는 `POST /api/v1/auth/login` 만 대상이라 배치 경로는 그대로 통과한다. ⚠️ **원문 서술 재정정(2026-08-08)**: 위 "cryptoTest도 무인증" 서술은 **이제 사실이 아니다** — `#57` 수정(2026-08-08)으로 JWT 분기의 `.requestMatchers("/api/v1/cryptoTest/**").permitAll()` 라인이 제거되어 **현재는 redis/JWT 두 분기 모두 `anyRequest().authenticated()` + 클래스 레벨 `@PreAuthorize` 적용 상태**다. `redisTest`만 JWT 분기(`SecurityConfig.java:133`)에 permitAll이 남아 있으나 local/developer는 `@ConditionalOnProperty`로 빈 자체가 없어 404다. 즉 **이 항목에 남은 실제 미해결 범위는 배치 트리거 4개뿐**이다. — 🔍 **`#보안성상세분석`(2026-08-08, 세션 에이전트 직접 오케스트레이션 — `java-security-reviewer`+`nextjs-security-reviewer` 블라인드 병렬 + 세션 에이전트 독립분석 3자 교차검증, security-review-dispatcher 미경유) 신규 발견**: `java-security-reviewer`가 `ContentsWriter.java`/`CatalogContentsBatchService.java` 직접 분석으로 **진짜 위험 지점을 구체화** — 익명 호출 한 번만으로 즉시 데이터가 삭제되는 건 아니다(soft delete는 이번 회차에 정상 처리된 문서 집합 기준으로만 발생, IF 대기행 없으면 삭제 0건). **실제 위험은 EAI가 소스 데이터를 적재하는 도중에 공격자가 이 무인증 엔드포인트를 호출하는 "끼어들기(race condition)"** — 이 경우 배치가 아직 덜 올라온 미완성 상태를 "이번 회차 최종본"으로 오인해, 아직 못 올라온 진짜 콘텐츠를 soft delete 처리하고 해당 IF 원본 행을 `if_result='P'`(처리완료)로 소비해버린다. EAI가 나중에 정상 호출해도 이미 소비된 행은 재처리 대상에서 빠져, **FO 공개 사이트의 실제 콘텐츠가 복구 불가 상태로 사라질 수 있다.** DoS 성격은 제한적(`ix_batch_log_running` 부분 유니크 인덱스로 소스당 동시 1건 직렬화돼 스레드 폭주는 없음)이나, CATALOG 배치가 매 실행마다 `contents_master` 전수조회+버전별 N+1을 수행해 반복 호출 시 DB 부하 누적은 가능(중간 수준). `nextjs-security-reviewer`: bo 화면의 "배치 실행" 버튼은 정상적으로 Bearer 토큰을 첨부해 호출하지만, 백엔드가 검사하지 않아 무의미하다는 것을 프론트 코드로 재확인(`bo/src/lib/api.ts`) — 화면 자체가 `/admin/system` 프리픽스라 미들웨어 쿠키 가드 대상이지만(`#40`과 동일하게 서버 인가와 무관), API 직접 호출 앞에서는 아무 완화 장치도 없다. 신규 발견 없음(같은 결론 재확인) 외에 **신규 발견 1건**(race condition 데이터 유실 경로) 추가. 조회용 5개는 여전히 `@PreAuthorize`로 보호돼 정보 노출 경로는 없음(과장 배제). 매트릭스 표 셀 변동 없음(①-B는 이미 FAIL). 코드 수정 없음.
- **#56 [bo-api/src/main/java/com/ge/bo/common/crypto/Aes256Utils.java:29, 51]** AES-256-CBC 암복호화에 **매번 동일한 고정 IV**(`aes256Properties.getConnectPortalIv()`)를 사용한다. CBC 모드에서 IV가 고정이면 같은 평문은 항상 같은 암호문으로 나오는 결정론적 암호화가 되어, 암호문만 비교해도 평문이 같은지 다른지 추론할 수 있다(패턴 분석 공격 표면). 이 키는 `ContactUsService`/`ContactUsInquiryService`가 고객 이메일·실명·문의 비밀번호를 외부 CTP로 전송하기 전 암호화하는 데 쓰인다. `#14`(같은 키가 git에 평문 커밋)와 별개로, **키를 안전하게 관리해도 남는 암호화 설계 결함**이다. — C / Warning — 🔍 **발견 경위**: `#11` `#보안성상세분석` 중 cryptoTest가 다루는 키의 실사용처를 추적하다 발견. 미착수
- **#57 [bo-api/src/main/java/com/ge/bo/common/crypto/CryptoTestController.java]** `@PreAuthorize`가 없다. 일부 프로필에서는 평문/암호문이 그대로 로그에 남는다(`log.info`). `@Profile`/`@ConditionalOnProperty` 가드도 없어 운영 환경에도 그대로 배포된다. — B / **Critical** — ⬆️ **재평가(2026-08-07)**: 최초 등록 시 "`authenticated()`에만 걸려 로그인만 하면 접근 가능"으로 Warning 판정했으나, `#14` 심층분석 중 `SecurityConfig.java:134`(JWT 모드=local/developer 분기)에 `/api/v1/cryptoTest/**`가 **permitAll로 등록**되어 있음을 재확인 — **local/developer 프로필에서는 로그인조차 필요 없이 완전 익명으로 PII 암호화 키(`#56`과 동일 키, CTP와 공유하는 대칭키) 암복호화 오라클을 사용할 수 있다.** dev/prd(redis 모드)는 permitAll 목록에 없어 인증은 필요하나 `@PreAuthorize`가 없어 최하위 권한 로그인 사용자도 접근 가능. 🔍 **발견 경위**: `#11` `#보안성상세분석` 중 최초 발견, `#14` 분석 중 익명접근 가능성 재확인으로 Warning→Critical 상향 — ✅ **수정 완료(2026-08-08)**: ① `CryptoTestController` 클래스 레벨에 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")` 추가, ② `SecurityConfig.java`의 JWT 모드(local/developer) 분기에서 `.requestMatchers("/api/v1/cryptoTest/**").permitAll()` 라인 제거(다른 보호 컨트롤러들과 동일하게 `anyRequest().authenticated()` + 메서드 레벨 인가로 편입). bo 프론트 소비처 없음(전수 grep 확인) 후 진행. **검증**: bo-api local 재기동 후 curl 3-way — 무토큰 **401**, 비시스템관리자(SUPER_ADMIN, 테스트계정 비밀번호 임시교체 후 원복) **403** `{"error":"FORBIDDEN"}`, 시스템관리자(comlbg) **200**(암호화 결과 정상 반환) — 동일 토큰으로 `/admins` 재확인 **200**(회귀 없음)
- **#58 [bo-api/src/main/java/com/ge/bo/common/redis/RedisTestController.java]** `@PreAuthorize` 없이 `authenticated()`에만 걸려있다(dev/prd의 redis 세션 모드 한정 — local/developer는 `@ConditionalOnProperty`로 빈 자체가 없어 해당없음). `RedisCacheService`가 키 네임스페이스 구분 없이 Redis에 접근하는데, 이 Redis는 로그인 세션도 함께 저장하는 저장소라 이론상 임의 권한 관리자가 `spring:session:sessions:*` 키를 조회/삭제/덮어써 타 관리자 세션을 조작할 수 있다. — B / Warning — 🔍 **발견 경위**: `#11` `#보안성상세분석` 중 발견. 미착수
- **#12 [bo-api/src/main/java/com/ge/bo/service/AuthService.java:153-158 + entity/AdminUser.java:63]** 계정 잠금에 **자동 해제 로직이 없다.** `lockedUntil` 컬럼은 존재하지만 로그인 경로에서 한 번도 읽지 않으며(`AuthService.login` 전체에 `getLockedUntil()` 호출 없음), 실패 5회 시 관리자가 수동 초기화할 때까지 영구 잠금된다. Rate Limiting 부재(위 항목)와 결합하면 **이메일만 아는 공격자가 임의 관리자 계정을 영구 잠글 수 있는 DoS**가 성립한다. — I / Warning — ⚠️ **전제 정정 + 현황(2026-08-07)**: 이 항목은 "실패 5회 시 영구 잠금된다"를 전제로 작성됐으나, 작성 시점에 **잠금 자체가 동작하지 않았다**(`#54`). 따라서 당시에는 이 DoS가 성립하지 않았다. `#54` 수정으로 잠금이 실제 동작하게 된 지금, **이 항목의 서술이 비로소 사실이 되었다** — `lockedUntil` 컬럼은 여전히 로그인 경로에서 한 번도 읽히지 않고, 자동 해제도 없어 **관리자가 `PATCH /api/v1/admins/{id}/status`로 수동 해제할 때까지 영구 잠금**이다. Rate Limiting 부재(`#9`)와 결합한 **"이메일만 알면 임의 관리자 계정을 영구 잠글 수 있는 DoS"는 해소되지 않았고 오히려 이제 실제로 성립한다.** 따라서 ①-I 셀은 FAIL 유지. 부수 확인: `AdminService.toggleStatus()`는 `is_active`만 되돌리고 `failed_login_attempts`는 초기화하지 않아(psql로 해제 후 `t|5` 확인), 해제 직후 1회만 더 실패해도 즉시 재잠금됨을 확인 — 이 후속 결함은 `#54` 보강 작업으로 별도 처리 예정.
- ✅ **#13 [bo-api/src/main/java/com/ge/bo/controller/LoginLogController.java, ErrorLogController.java, TransactionLogController.java(전부 @PreAuthorize 0건)]** `/api/v1/login-logs`가 `anyRequest().authenticated()`에만 걸려 있어 **모든 로그인 사용자가 전 관리자의 이메일·IP·User-Agent 접속이력을 조회**할 수 있다. `ErrorLogController`도 동일하며 스택트레이스 포함 상세 조회가 가능하고, `TransactionLogController`도 동일 패턴으로 요청 바디 원문(password류만 마스킹)이 노출된다. 이 저장소의 다른 20여개 관리 컨트롤러는 전부 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")`을 쓰는데 로그 조회 3곳만 이탈. — B, J / **Critical** — ⬆️ **재평가(2026-08-06)**: `#보안성상세분석`(java-security-reviewer + nextjs-security-reviewer + 세션 에이전트 3자 교차검증)에서 Warning→Critical로 상향, TransactionLogController 대상 추가 확인 — ✅ **수정 완료(2026-08-06)**: 3개 컨트롤러 클래스 레벨에 저장소 공통 컨벤션 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")` 적용(`ApiInfoController:28` 등과 동일). 소비처는 `bo/src/app/admin/logs/{access,error,transaction}` 전용 화면 6곳뿐이며 헤더/공통영역 등 예외 소비처 없음을 Grep으로 확인. **검증**: bo-api 재기동 후 curl 3-way 실검증 — (1) 토큰 없음 → 목록 3개 전부 **401**, (2) 비(非)시스템관리자 토큰(`SUPER_ADMIN`, `role.is_system=f`) → 목록·상세 6개 전부 **403** `{"error":"FORBIDDEN"}`, (3) 시스템관리자(`comlbg`/`SYSTEM_ADMIN`) → 목록·상세 6개 전부 **200** 정상 조회
- ✅ **#14 [bo-api/src/main/resources/application-dev.yml:8, 17, 87-88, 115]** JWT 시크릿(위 항목) 외에도 dev 프로필에 운영성 시크릿이 평문 커밋되어 있다: DB 비밀번호(:8, 값은 2026-08-08 문서 정리 시 마스킹 처리 — 아래 진행이력 참고), 메일 비밀번호(:17), Connect Portal 암복호화 키/IV(:87-88), Azure Blob SAS 토큰(:115). 4개 프로필 yml 전부 git 추적 대상이다. — C / **Critical** — ✅ **수정 완료(2026-08-08)**: `#보안성상세분석`으로 4개 시크릿 전부 코드수정 없이 `${VAR:}` 전환 가능함을 확인(java-security-reviewer+세션 에이전트). 우선순위 1번(Azure Blob SAS 토큰) 처리 완료 — `application-dev.yml:115`, `application-developer.yml:115`의 하드코딩 토큰을 `sas-token: ${AZURE_BLOB_SAS_TOKEN:}`로 전환(현재 두 프로필 다 `file-storage: local`이라 이 값 자체는 런타임에 미사용, 동작 영향 없음). **검증**: Python `yaml.safe_load()`로 두 파일 파싱 성공 + `sas-token` 값이 플레이스홀더로 정상 치환됨을 확인, bo-api local 프로필 재기동 정상(`Started BoApplication`). ⚠️ **파일에서 제거해도 이미 유출된 토큰 값 자체(2026-12-31까지 유효)는 Azure 측에서 별도로 폐기/재발급해야 무효화됨 — 이 부분은 코드 작업이 아니라 운영 담당자 조치 필요, 미완료.** 나머지 3개(DB 비밀번호/메일 비밀번호/Connect Portal 키·IV) 처리 완료(2026-08-08) — `application-dev.yml`(DB pw/mail pw/키/IV 4곳), `application-developer.yml`(mail pw/키/IV 3곳, DB pw는 이전에 이미 `${DB_PASSWORD:}`로 수정돼 있었음), `application-local.yml`(Connect Portal 키/IV만 2곳 — DB pw `"1234"`/mail pw `test`는 로컬 전용 가짜값이라 유지)를 전부 기존 `${VAR:}` 컨벤션으로 전환. **검증**: Python `yaml.safe_load()`로 3개 파일 파싱 성공 확인, bo-api local 재기동 정상(`Started BoApplication`) + 로그인 회귀 확인(`comlbg` 200 정상). ⚠️ **fail-late 특성 인지**: `Aes256Utils.validateKeyAndIv()`는 부팅 시점이 아니라 실제 암복호화 호출 시점에 키 길이를 검증하므로, `CONNECT_PORTAL_ENC_KEY`/`CONNECT_PORTAL_IV` 환경변수가 없는 상태에서 로컬 개발 시 Contact Us 문의 접수 기능(CTP 전송)을 실제로 쓰면 그때 처음 에러가 난다 — 부팅 실패로는 안 잡힘. ⚠️ **여전히 남은 것**: (1) 파일에서 지워도 DB/메일 비밀번호·Connect Portal 키 자체 값은 유효하므로 실제 로테이션은 별도 운영조치 필요(DB/메일은 인프라 담당자, Connect Portal 키는 CTP와 협의), (2) 실제 dev 서버 배포 파이프라인(Dockerfile/Jenkinsfile)에 환경변수 주입 단계가 코드로 없어 배포 담당자가 사전에 4개 환경변수(`DB_PASSWORD`/`MAIL_PASSWORD`/`CONNECT_PORTAL_ENC_KEY`/`CONNECT_PORTAL_IV`)를 서버에 미리 설정해둬야 함 — 안 하면 dev 서버가 DB 접속 실패로 기동 자체가 안 됨, (3) 문서 2건(`environment-profile-guide.md`, 보안성검토 매트릭스)의 평문 노출은 순위 4번으로 처리 완료(2026-08-08) — `environment-profile-guide.md`의 예시 yml 및 비교표 3곳을 실제 파일과 동일하게 `${DB_PASSWORD:}` 표기로 전환(가이드가 실제 파일과도 다시 일치하게 됨), 이 문서 자체와 `fo-메인화면-매트릭스.md`(#6 항목)에 인용된 평문 비밀번호도 마스킹(발견 기록·파일:줄번호 등 이력 정보는 보존, 값 텍스트만 제거). git 히스토리 재작성은 사용자가 "신경쓰지 말라"고 명시적으로 범위에서 제외 — ✅ **일관성 보완(2026-08-08)**: 사용자 요청으로 `application-local.yml`의 DB/메일 비밀번호도 형식만 통일 — `${DB_PASSWORD:1234}`, `${MAIL_USERNAME:test}`, `${MAIL_PASSWORD:test}`(가짜값을 기본값으로 그대로 유지해 로컬 개발 동작은 무변경, 4개 프로필 파일 전부 `${VAR:...}` 형식으로 구조적 일관성 확보). **검증**: `DB_PASSWORD` 환경변수를 일부러 안 넣고 재기동 → 기본값 `1234`로 DB 연결 정상, `Started BoApplication` + 로그인 200 확인 ⚠️ **범위 확정(2026-08-08, 사용자 확인)**: 시크릿 값 자체의 실제 로테이션(DB/메일 비밀번호 변경, Connect Portal 키 재협의, Azure SAS 재발급)은 코드 문제가 아닌 운영 조치이므로 이 매트릭스의 완료 판정 범위에서 제외 — 코드가 하드코딩에서 환경변수 참조로 전환됐다는 사실만으로 이 항목은 해결 처리한다
- ✅ **#15 [bo-api/src/main/java/com/ge/bo/controller/AuthController.java:59-65]** `extractClientIp()`가 `X-Forwarded-For` 헤더를 프록시 홉 검증 없이 무조건 신뢰한다. 클라이언트가 헤더를 위조하면 접속이력의 IP가 오염되어 감사증적 신뢰성이 떨어지고, IP 기반 통제를 도입하더라도 우회된다. — J / Info — ⬆️ **재평가+범위확대(2026-08-08)**: `#보안성상세분석`에서 동일 패턴이 10개 파일에 중복 구현된 것을 확인, `AuthController` 것은 실제로 로그인 Rate Limiting(`#9`)의 실효성과도 직결됨이 드러나 Info보다 실질 영향이 큼을 확인 — ✅ **수정 완료(2026-08-08)**: X-Forwarded-For 신뢰를 완전히 포기하고(A안), 10곳에 중복 구현돼 있던 `split(",")[0]` 파싱 로직을 전부 삭제한 뒤 `common/util/ClientIpUtils.resolve()`(내부 구현 = `request.getRemoteAddr()`) 정적 호출로 일원화. 대상 10개 파일: `controller/AuthController`, `filter/TransactionLogFilter`, `controller/ContactUsInquiryController`, `controller/FoTrainingController`, `controller/FoSearchKeywordController`, `controller/TrainingRegistrationController`, `service/ErrorLogService`, `common/excel/EntityExcelExportService`, `controller/PageDataController`, `service/TrainingApplicationService`. XFF를 신뢰하는 설정을 "추가"하지 않은 이유는, 같은 호스트의 bo(Next.js) rewrite(`next.config.ts:14`, `/api/v1/:path*` 전체)가 클라이언트 XFF를 검증 없이 verbatim 전달하므로 프록시를 신뢰하면 오히려 공격자 조작값을 그대로 채택하게 되기 때문. **검증**: bo-api·bo 재기동 후 **실제 브라우저 경로인 3001(bo Next.js) 프록시 경유**로 `X-Forwarded-For: 1.2.3.4, 9.9.9.9`를 실어 `POST /api/v1/auth/login` 호출 → psql `login_log` 조회 결과 `client_ip=0:0:0:0:0:0:0:1`(실제 소켓 주소)로 기록되어 위조값 미채택 확인. 동일 방식으로 `transaction_log.client_ip`(XFF `5.6.7.8` 무시), `search_keyword_log.created_ip`(동일), `error_log.client_ip`(XFF `203.0.113.77` 무시), `download_log.ip_address`(XFF `198.51.100.9` 무시) 4개 경로 추가 실증. 테스트 데이터는 검증 후 전량 삭제·원복. ⚠️ **미해결 잔여**: `entity/ContactUsInquiry.java:74` 등 11곳의 "X-Forwarded-For 우선" Javadoc 주석이 이제 사실과 다름(승인 범위 밖이라 미수정), `ContactUsInquiryController`/`FoTrainingController`/`TrainingRegistrationController`/`EntityExcelExportService`/`TrainingApplicationService`의 실제 다운로드/제출 경로는 코드는 동일 패턴으로 통일됐으나 런타임 실호출 검증은 미실시(외부연동·데이터 준비 제약)
- **#16 [bo-api/build.gradle:13]** `JavaLanguageVersion.of(25)`로 비-LTS 런타임을 강제하고 있다(프로젝트 요구사항은 Java 21). 보안 패치 수명이 짧은 런타임에 고정되어 있다. — H / Info
- **#17 [bo-api/src/main/java/com/ge/bo/config/SecurityConfig.java:61, 66 + application.yml:25]** (**redis 모드 = dev 한정**) 세션 쿠키 기반으로 동작하면서 CSRF를 비활성화했다. 쿠키 자동 전송 + CSRF 미적용 상태에서 `same-site: lax`에만 의존하는데, lax는 top-level GET만 차단하므로 CORS 화이트리스트에 등록된 오리진 중 하나에서 XSS가 발생하면 상태변경 요청이 그대로 통과한다. — E / Warning

### ② TOTP 등록(QR)

- **#18 [bo-api/src/main/java/com/ge/bo/entity/AdminUser.java:66-67]** TOTP 비밀키가 `@Column(name = "totp_secret", columnDefinition = "TEXT")`로 **평문 그대로 DB에 저장**된다(`TotpService.java:64`에서 `admin.setTotpSecret(secret)`). 컬럼 암호화·해시·KMS 연동이 전혀 없어, DB 덤프나 SQL Injection·백업 유출 시 전 관리자의 2차 인증이 즉시 무력화된다(공격자가 secret으로 유효한 OTP를 무한 생성 가능). 비밀번호는 BCrypt로 보호되는 반면 2FA 시드는 무방비라 2FA가 사실상 방어 계층 역할을 못 한다. — C / **Critical** — 🔄 **재검토 완료 / 미해결 유지(2026-08-08)**: `#보안성검토`(디스패처 직접 Read + `java-security-reviewer` 교차검증)로 재확인, 지적 내용 전부 그대로 성립. ⚠️ **라인번호 갱신**: `#55` 작업으로 `totpFailedAttempts` 컬럼(`AdminUser.java:63-65`)이 추가되면서 라인이 밀렸다 — 현재 `totpSecret` 선언은 **`AdminUser.java:69-71`**, secret 저장 지점은 **`TotpService.java:67-68`**, 검증 시 평문 사용은 **`TotpService.java:271`**. `@Convert`/`AttributeConverter`/jasypt는 `bo-api/src/main` 전체에서 **0건**이며, 프로젝트에 `common/crypto/Aes256Utils.java`가 존재함에도 `totpSecret` 경로에서는 호출되지 않는다(`totpSecret` 참조 지점 전수 확인). ➕ **부수 확인(신규)**: `TotpService.setup()`이 반환하는 `SetupResponse`(`TotpService.java:79-82`)가 QR URI와 **함께 `secret` Base32 원문도 응답 바디에 평문으로 내려준다** — 이는 `#23`(클립보드 잔류)의 상위 원인이기도 하며, QR만으로 등록이 가능하므로 응답에서 제거 가능한지 검토 대상. ⬆️ **재평가 — 이론적 위험 → 실제 발생한 노출로 격상(세션 에이전트 `#보안성상세분석`, 2026-08-08)**: `java-security-reviewer`+`nextjs-security-reviewer` 블라인드 병렬 + 세션 에이전트 독립분석 3자 교차검증 수행. `nextjs-security-reviewer`는 bo 프론트 경로(폼 state/스토어/로그/클립보드)에서는 이 위험을 증폭시키는 추가 유출 경로가 없음을 확인(Info, 정상 노출). `java-security-reviewer`가 저장소 루트의 `db_dump_20260731.sql`(git 커밋 `0e7e3c3f`)이 `admin_user` COPY 블록에 **관리자 18개 계정의 totp_secret 원문을 그대로 포함**(그중 2FA 활성 17건, `comlbg`/SYSTEM_ADMIN 포함)하고 있음을 발견 — 세션 에이전트가 직접 재확인: `git ls-files`로 추적 대상 확인, `git check-ignore -v`는 무결과(gitignore 미적용), `git branch -r --contains 0e7e3c3f` → **`ge/master`(github.com/kfgabiz-lab/ge.git) 포함**(로컬이 아니라 원격에 이미 push됨). 즉 "DB가 유출되면"이라는 가정형 위험이 아니라 **저장소 열람 권한자 전원이 이미 실제 관리자 17명의 유효한 TOTP seed에 접근 가능한, 이미 발생한 노출**이다. 같이 추적 중인 `dev_db_dump.sql`은 totp_secret 데이터 0건으로 해당 없음 확인. — **긴급 운영조치 필요(코드 수정과 별개)**: 노출된 17개 계정 전원 TOTP 강제 재등록(코드를 고쳐도 이미 유출된 seed 값 자체는 무효화되지 않음), `db_dump_20260731.sql` 저장소 제거 여부 및 git 이력 재작성 범위는 `#14` 선례와 마찬가지로 사용자 확인 필요.
- **#19 [bo-api/src/main/java/com/ge/bo/service/TotpService.java:52-64]** `setup()`이 `@Transactional` 안에서 6자리 코드 검증 **이전에** 새 secret을 생성해 `admin.setTotpSecret(secret)`으로 DB에 선반영한다. `POST /auth/totp/qr`는 tempToken만 있으면 호출 가능하므로, 10분 유효한 tempToken으로 반복 호출해 secret을 무한 재생성·덮어쓸 수 있다. 사용자가 인증앱에 등록을 마치는 도중 secret이 교체되면 등록이 실패하는 경합/방해 상황이 발생한다. 또한 `TotpDto.SetupRequest`(`TotpDto.java:18-20`)에 Bean Validation이 없다. — F / Warning
- **#20 [bo-api/src/main/java/com/ge/bo/service/TotpService.java:82-86, 124-128]** Javadoc은 `복구코드 10개 (1회성)` 발급을 명시하지만 실제 반환 DTO `TotpDto.VerifyResponse`(`TotpDto.java:68-71`)에는 복구코드 필드 자체가 없고 구현도 없다. 인증기기 분실 시 관리자 자가 복구 수단이 없어 계정 잠김(가용성) 위험이 있다. — I / Info

- **#21 [bo-api/src/main/java/com/ge/bo/service/AuthService.java:438-448]** (**redis 모드**) `startMfa()`가 세션에 `MFA_VERIFIED=false`를 심지만 **이 속성을 읽는 코드가 프로젝트 전체에 없다**(쓰기 1곳, 읽기 0곳). "1차만 통과한 세션"과 "2차까지 완료한 세션"을 구분하려는 의도로 만든 플래그가 죽어 있어, 향후 단계 구분이 필요해질 때 방어선이 존재하지 않는다(현재는 `createLoginSession`(`:492-510`) 이전에 SecurityContext가 비어 있어 실제 우회는 성립하지 않음). — B / Info
- ✅ **#22 [bo/next.config.ts:25-39, bo/src/middleware.ts:25-40]** ①-E와 동일한 CSP/`X-Frame-Options` 부재가 QR 등록 화면에도 그대로 적용된다. TOTP 시크릿과 QR 코드가 표시되는 화면이 iframe에 삽입될 수 있다. — D / Warning — ✅ **수정 완료(2026-08-06)**: ①-E와 동일 건, 전 경로 헤더 적용으로 함께 해결
- **#23 [bo/src/components/auth/totp-setup-form.tsx:43-47]** `copySecret()`이 TOTP 시크릿을 클립보드에 복사한 뒤 비우지 않는다(타이머는 아이콘 상태만 되돌림). 등록 완료 후에도 클립보드에 2FA 시드가 잔류해 클립보드 스니핑에 노출된다. — C / Info

### ③ TOTP 검증

- **#24 [bo-api/src/main/java/com/ge/bo/service/TotpService.java:206-233, 252-262]** OTP 검증에 **시도 횟수 제한이 전혀 없다.** `verifyWithJwt()`/`verify()` 어디에도 실패 카운터·잠금·지연이 없고(`AuthService.java:153-158`의 비밀번호 계정잠금은 1차 로그인에만 적용), `POST /api/v1/auth/totp/sessions`는 `SecurityConfig.java:131`에서 `permitAll`이며 reCAPTCHA도 이 단계에는 적용되지 않는다. 결과적으로 비밀번호를 확보한 공격자가 10분짜리 tempToken 하나로 6자리 코드를 **무제한 자동 대입**할 수 있다. 아래 두 요인이 이를 더 악화시킨다.
  - `verifyTotpCode()`가 `verifier.setAllowedTimePeriodDiscrepancy(2)`(`TotpService.java:256`)로 ±2 주기를 허용해 임의 시점에 유효한 코드가 5개(약 150초 창)나 존재한다 → 1회 시도 성공확률이 권장 설정(±1) 대비 크게 상승.
  - 검증에 성공한 코드를 소비/기록하지 않아 **같은 코드의 재사용(replay)이 가능**하다. 탈취된 OTP 1개를 유효 창 내에 반복 사용할 수 있다.
  이 조합으로 2차 인증이 실질적인 방어선 역할을 하지 못한다. — F / **Critical** — ⬇️ **재평가: 부분 해결(2026-08-08)**: `#보안성검토`(디스패처 직접 Read + `java-security-reviewer` 교차검증)로 `#55` 적용 이후 코드를 재확인한 결과, **이 항목이 지적한 3가지 중 1가지만 해결되고 2가지는 그대로**임을 확인했다. 문서에 `#24` 자체의 상태 갱신이 누락돼 있던 것을 여기서 정리한다. **심각도 Critical → Warning 하향**(무제한 대입이라는 Critical 근거가 사라졌으므로). ①-F/③-F 셀은 아래 잔여 2건 때문에 FAIL 유지.
  - ✅ **(1) 시도횟수 제한 — 해결됨(단, 절대 상한은 아님)**: `#55`(2026-08-07)로 `MAX_TOTP_ATTEMPTS = 3`(`TotpService.java:36`) 도입, 코드 검증 **이전에** 임계치를 체크해 초과 시 `401 TOTP_MAX_ATTEMPTS`로 거부(`TotpService.java:262-265`), 오답 시 `incrementTotpFailure`(`:272`). 원문의 "10분짜리 tempToken 하나로 무제한 자동 대입"은 **더 이상 성립하지 않는다.** ⚠️ 다만 카운터는 계정 잠금이 아니라 **해당 2FA 시도만 무효화**하는 설계라, 1차 로그인을 다시 통과하면 `AuthService.tempLoginResponse():423`의 `resetTotpFailure()`로 0으로 리셋된다. 즉 **비밀번호를 이미 확보한 공격자는 "재로그인 → 3회 시도"를 반복할 수 있고**, 이를 제한하는 것은 `LoginRateLimitFilter`(`:30` 100회/분, `:28-29,38-42`로 `POST /api/v1/auth/login` **한정** — `/auth/totp/sessions`는 rate limit 대상이 아님)뿐이다. 계정 잠금(`#54`)도 비밀번호가 맞으므로 걸리지 않는다. 무제한에서 **분당 최대 300회 수준**으로 낮아진 것이지 하드 상한이 생긴 것은 아니다.
  - ❌ **(2) 타임윈도우 ±2 — 미해결(그대로)**: `TotpService.java:269`의 `verifier.setAllowedTimePeriodDiscrepancy(2)` 변경 없음. period 30초(`:76`) 기준 **총 5창 = 약 150초** 동안 동일 코드가 유효하다(권장 ±1 = 90초 대비 넓음).
  - ❌ **(3) 코드 소비(replay 방지) — 미해결(그대로), 이 항목의 실질 잔여 핵심**: `verifyTotpCode()`(`TotpService.java:260-276`)의 **성공 경로는 아무 기록도 남기지 않는다**(실패 시에만 카운터 증가 후 예외). "마지막으로 사용한 타임스텝"을 저장하는 필드가 `AdminUser` 엔티티에 없고(TOTP 관련 필드는 `totpFailedAttempts`/`totpSecret`/`totpEnabled` 3개뿐), 사용된 코드를 담는 테이블·Redis 키·리포지토리도 존재하지 않는다(TOTP를 참조하는 9개 파일 전수 확인). 성공한 호출자 4곳(`:116-120`, `:148-152`, `:196-199`, `:229-232`)도 실패 카운터를 0으로 되돌릴 뿐이다. 따라서 **관리자가 방금 성공적으로 사용한 6자리 코드를 어깨너머·중간자·로그 등으로 확보하면 150초 이내에 그대로 재제출해 통과**할 수 있다. 게다가 replay는 성공 경로라 시도횟수 카운터에도 걸리지 않아 `#55`의 방어를 우회한다 — **(1)의 해결이 (3)을 전혀 커버하지 않는다는 점이 핵심.** ⚠️ 라인번호 갱신: 원문의 `206-233, 252-262`는 현재 `verify()`/`verifyWithJwt()` = `TotpService.java:169-241`, `verifyTotpCode()` = `TotpService.java:260-276`.
- **#25 [bo-api/src/main/java/com/ge/bo/security/JwtAuthenticationFilter.java:33-46]** 필터가 토큰의 `type`/`purpose` 클레임을 **전혀 확인하지 않는다.** 서명과 만료만 맞으면 종류를 가리지 않고 인증 주체로 승격시키려 시도한다. 현재 TOTP_PENDING·refresh·preview 토큰이 통과하지 못하는 이유는 설계상의 차단이 아니라, 이들 토큰에 `role` 클레임이 없어 `:41`의 `role.startsWith(...)`에서 NPE가 발생하고 `:57` catch에서 401로 떨어지는 **우연한 부작용**이다(`JwtTokenProvider.java:49-56, 61-69, 74-84` — 세 토큰 모두 `role` 없음). 누군가 tempToken에 role을 추가하거나 필터에 null-safe 처리를 넣는 순간 즉시 완전한 2FA 우회로 전환되는 구조 자체가 결함이다. — B / Warning
- ✅ **#26 [bo/next.config.ts:25-39]** ①-E와 동일한 프레이밍 차단 헤더 부재가 OTP 입력 화면에도 적용된다. — D / Warning — ✅ **수정 완료(2026-08-06)**: ①-E와 동일 건, 전 경로 헤더 적용으로 함께 해결
- **#27 [bo-api/src/main/java/com/ge/bo/service/TotpService.java:211-213 vs :259]** 미등록 계정은 `TOTP_NOT_ENABLED`, 등록 계정은 `TOTP_CODE_INVALID`로 응답이 구분되어 2FA 등록 여부가 열거된다(유효 tempToken이 선행되어야 하므로 영향은 제한적). — G / Info
- **#28 [bo-api/src/main/java/com/ge/bo/service/TotpService.java:207-233]** 2차 인증의 성공·실패가 `login_log`에 **전혀 기록되지 않는다.** 1차 로그인 시점에 이미 `SUCCESS`가 찍히므로(`AuthService.java:178`), 접속이력만으로는 "1차 통과 후 2차 실패"와 "완전 로그인"을 구분할 수 없다. 위 F의 TOTP 브루트포스가 실제로 시도되어도 흔적을 추적할 수 없다. — J / Info

### ④ 토큰 갱신(Refresh)

- ✅ **#29 [bo-api/src/main/java/com/ge/bo/service/AuthService.java:196-212 + security/JwtTokenProvider.java:49-56, 115-117, 129-137 + controller/AuthController.java:116-122]** **2차 인증(TOTP) 완전 우회.** `refreshWithJwt()`는 `validateToken()`(서명·만료만 검사) → `getEmailFromToken()`(**클레임 타입 검증 없음**) → `generateAccessToken()` 순으로 동작하며, refresh 토큰에는 `type`/`jti` 클레임이 아예 없어 토큰 종류를 구분할 방법 자체가 없다. 결과적으로 **1차 로그인(ID/PW)만 통과해 응답 바디로 받은 tempToken(`type=TOTP_PENDING`, 10분 유효)을 `refreshToken` 쿠키에 넣어 `POST /api/v1/auth/refresh`를 호출하면 정식 accessToken이 발급된다.** `/api/v1/auth/**`는 `permitAll`(`SecurityConfig.java:131`)이고 `@CookieValue`(`AuthController.java:118`)는 클라이언트가 임의 설정 가능하므로(httpOnly는 서버→브라우저 방향 보호일 뿐 curl에는 무의미) 공격 난이도가 사실상 0이다. 같은 이유로 5분짜리 previewToken(`JwtTokenProvider.java:74-84`)도 동일하게 악용된다.
  - *성립 조건*: `ls.redis-enabled=false` **AND** `ls.totp.enabled=true`. 현재 커밋된 프로필 조합(local/developer는 totp=false, dev는 redis=true)에서는 우연히 비껴가 있으나, **`application.yml:46`의 기본값이 `totp.enabled: true`이고 `ls.redis-enabled`는 미정의(=`@Value` 기본 false)** 이므로 두 키를 명시하지 않은 프로필(예: 신규 prd)을 추가하는 순간 곧바로 발현한다. 우회 대상 코드 경로(`confirmWithJwt`/`verifyWithJwt`)가 실사용 목적으로 완비되어 있어 잠재결함이 아닌 즉시 수정 대상이다. — B / **Critical** — 🔄 **재검토 완료 / 미해결 유지(2026-08-08)**: `#보안성검토`(디스패처 직접 Read + `java-security-reviewer` 교차검증)로 재확인, **코드 결함은 한 줄도 바뀌지 않았다.** `refreshWithJwt()`(`AuthService.java:204-209`)는 여전히 `validateToken()`(→ `JwtTokenProvider.java:129-137` → `:139-145`, **서명·만료만** 검사) → `getEmailFromToken()`(`JwtTokenProvider.java:115-117`, `getSubject()`만 반환) 순으로만 동작하고, `generateRefreshToken()`(`JwtTokenProvider.java:49-56`)은 `type`/`jti` 클레임을 넣지 않는다. **accessToken/refreshToken/tempToken/previewToken 4종이 동일 HMAC 키로 서명되면서 서로 구분할 수단이 없다**는 구조가 근본 원인이다. ➕ **비대칭성 확인**: 같은 클래스의 `getEmailFromTotpPendingToken()`(`JwtTokenProvider.java:104-110`)에는 `type=TOTP_PENDING` 명시 검증이 **이미 구현되어 있다** — 즉 refresh 경로에만 대칭 검증이 빠진 것이며, 수정 시 참고할 사내 구현이 이미 존재한다. 🔍 **프로필별 성립조건 실측 재확인(4종 전수)**: `application.yml`(base) = `redis-enabled` **키 없음**(→ `@Value` 기본 false) + `totp.enabled: true`(`:46`) → ⚠️ **성립** / `application-local.yml` = 키 없음(false) + `totp.enabled: false`(`:71`) → 미성립 / `application-developer.yml` = `redis-enabled: false`(`:80`) + `totp.enabled: false`(`:83`) → 미성립 / `application-dev.yml` = `redis-enabled: true`(`:84`) + totp 블록 없음(base 상속 true) → 미성립(`AuthController.java:114`가 redis 분기로 `authService.refresh()` 호출). ⚠️ **추가로 확인된 발현 경로**: `application.yml:5`가 `spring.profiles.active: ${SPRING_PROFILES_ACTIVE:developer}`이므로, **존재하지 않는 프로필명(예: `prd`, `prod`)으로 기동하면 base만 적용되어 `redis=false + totp=true` 조합이 그대로 성립**한다. 즉 "방어되고 있는" 것이 아니라 "현재 커밋된 프로필 조합에서 우연히 비껴가 있는" 상태라는 원문 판단이 그대로 유효하다. ➕ **previewToken 영향 정밀화(신규)**: previewToken(`JwtTokenProvider.java:74-84`)을 refreshToken 쿠키에 넣으면 동일하게 정식 accessToken이 발급되는 것은 맞으나(JWT 모드 한정, totp 설정 무관), **발급 대상이 이미 인증된 BO 관리자 본인**이므로(`PreviewTokenController.java:25`가 `SecurityContextHolder`에서 본인 email 추출) 자기 자신으로의 권한 상승은 없다 — 이 부분은 원문보다 낮게 평가해야 한다. 다만 previewToken은 **URL 쿼리파라미터로 전달**되고(`PageDataService.java:450, 1792-1793`의 `allParams.get("previewToken")`, FO 검증 API는 `FoPreviewTokenController.java:14`로 permitAll) 브라우저 히스토리·Referer·액세스로그에 남으므로, 유출된 5분짜리 최소권한 토큰이 **24시간 accessToken + 7일 refreshToken으로 증폭**된다는 점은 실질 위험으로 유효하다.
  - ✅ **수정 완료(2026-08-08)**: refresh 토큰에 종류 클레임을 부여하고 refresh 경로에서 이를 강제 검증하도록 변경. 같은 클래스의 기존 패턴(`getEmailFromTotpPendingToken()`의 `type` 불일치 시 `JwtException`)을 그대로 재사용했고 새 컨벤션은 도입하지 않았다. ① `JwtTokenProvider.generateRefreshToken()`에 `.claim("type", "REFRESH")` 추가, ② `JwtTokenProvider.getEmailFromRefreshToken()` 신규(= `type != "REFRESH"`면 `JwtException`), ③ `AuthService.refreshWithJwt()`가 `getEmailFromToken()` 대신 이 메서드를 호출하고 예외를 `BusinessException.unauthorized`(401)로 변환. ⚠️ **`getEmailFromToken()`은 손대지 않았다** — `JwtAuthenticationFilter.java:37`(accessToken 인증)·`TransactionLogFilter.java:124`가 공유하는 메서드라 여기에 REFRESH 강제를 넣으면 전체 인증이 깨진다. redis 세션 경로(`AuthService.refresh()`)와 `AuthController`의 분기는 미변경. **검증**(local 프로필 = `redis-enabled` 미정의/false + curl 실호출, 수정 전후 동일 시나리오 대조): **[수정 전]** preview 토큰을 `refreshToken` 쿠키로 `/auth/refresh` → **200 + SYSTEM_ADMIN accessToken 발급**(우회 실재현), accessToken을 넣어도 **200**. **[수정 후]** preview 토큰 → **401** `{"error":"UNAUTHORIZED"}`, accessToken → **401**, 수정 전 발급된 `type` 없는 구 refreshToken → **401**, 신규 정상 refreshToken(payload `{"sub":"comlbg","type":"REFRESH",...}` 확인) → **200 정상 발급**, 쿠키 없음 → **401**. **TOTP_PENDING 경로는 `LS_TOTP_ENABLED=true`로 임시 오버라이드 재기동해 별도 재현** — 1차 로그인 응답의 tempToken(payload `type=TOTP_PENDING` 확인)을 `refreshToken` 쿠키로 `/auth/refresh` → **401**, 이어서 DB `totp_secret`으로 실제 TOTP 코드를 계산해 `POST /auth/totp/sessions` 2차 인증 완료 → 발급된 `Set-Cookie: refreshToken`의 payload가 `type=REFRESH`이고 `/auth/refresh` **200** 정상(정상 2FA 플로우 무손상). 검증 후 오버라이드 제거하고 원래 local 프로필로 재기동해 회귀 확인(로그인 200 / accessToken 인증 API 200 / refresh 200 / logout 200 / preview 토큰 발급 200 / preview→refresh 401). ⚠️ **부수효과(의도된 동작)**: 이미 브라우저에 남아있는 기존 refreshToken에는 `type` 클레임이 없어 **다음 갱신 시점에 401 → 강제 재로그인**이 발생한다(최대 7일간 잔존 토큰 대상). 별도 마이그레이션 로직은 두지 않았다. ⚠️ **이 항목은 "토큰 종류 혼용" 결함만 해결한다** — 같은 셀의 `#30`(refresh 토큰 회전·재사용 탐지·서버측 저장소 전무)은 그대로 미해결이라 **④-B 셀은 FAIL 유지**.
- **#30 [bo-api/src/main/java/com/ge/bo/service/AuthService.java:194-224]** Refresh 토큰의 **회전(rotation)·재사용 탐지·서버측 저장소가 전무하다.** 토큰을 저장하는 테이블/Redis 키/엔티티가 존재하지 않고(`repository` 패키지에 RefreshToken 관련 리포지토리 없음), `refreshWithJwt`는 새 accessToken만 반환할 뿐 refreshToken을 재발급하지 않는다. 따라서 탈취된 refresh 토큰은 **7일간(`application.yml:33`) 무제한 재사용**되며 탐지도 불가능하다. 소유자 검증도 `sub` 클레임의 자기주장에만 의존해(`:200-202`) 기기 바인딩·세션 ID·IP/UA 대조가 전혀 없다. — B / **Critical** — 🔄 **재검토 완료 / 미해결 유지(2026-08-08)**: `#보안성검토`(디스패처 직접 Read + `java-security-reviewer` 교차검증)로 재확인, **전무 상태 그대로**. `refreshWithJwt()` 현재 전문(`AuthService.java:203-233`)이 새 accessToken만 반환(`:221-224`)하고 refreshToken을 재발급하지도 기존 것을 폐기하지도 않는다. 서버측 저장소 부재를 `bo-api/src/main` 전수 grep으로 재확인 — `blacklist`/`denylist`/`jti`/`tokenVersion`/`token_version`/`revoke` **전부 0건**, `RefreshToken` 엔티티·리포지토리 없음(코드 내 `RefreshToken` 문자열은 `AuthController.java:53,82,101` / `AuthService.java:250,260-261,272` / `JwtTokenProvider.java:49`의 메서드명뿐으로 전부 stateless 생성·쿠키 조작). `generateRefreshToken()`에 `jti` 클레임이 없어 **식별 기반 폐기 자체가 구조적으로 불가능**하다는 점이 `#36`과 공통 근본 원인이다. — 🔍 **`#보안성상세분석`(2026-08-08, 세션 에이전트 직접 오케스트레이션 — `java-security-reviewer`+`nextjs-security-reviewer` 블라인드 병렬 + 세션 에이전트 독립분석 3자 교차검증, security-review-dispatcher 미경유) 완화요소 재확인**: `java-security-reviewer`가 "전무 상태"라는 서술이 정확한지 재검증한 결과, **완화수단이 정말 0개는 아님**을 확인 — `refreshWithJwt()`가 매 호출마다 `admin.isActive()`를 DB 조회하므로 **계정 비활성화는 탈취된 토큰도 즉시 차단**한다(`AuthService.java:216-221`). 비밀번호 변경은 무관(애초에 저장소에 비밀번호 변경 엔드포인트 자체가 없음 — 별도 사안, 범위 밖). `nextjs-security-reviewer`: refreshToken은 `httpOnly`라 XSS로 프론트 코드에서 직접 탈취 불가능함을 재확인 — 탈취 자체가 네트워크 감청·기기 접근 등 별도 경로를 요구하는 전제 위에 있다(전제 자체가 이미 조건부라는 점을 명시). **사용자 검토 후 재평가**: "차단 수단이 없다"가 아니라 "차단이 필요한 상황(탈취)을 감지할 방법이 없다"가 이 항목의 정확한 남은 문제로 좁혀짐 — `/auth/refresh` 호출이 `login_log`에 전혀 기록되지 않아(`#35`) 재사용/이상 패턴을 알아챌 신호 자체가 없음. **사용자 결정: 패스**(수정 착수 없이 다음 Critical로 진행). 코드 수정 없음, 매트릭스 표 셀 변동 없음. 심각도는 사용자 확인 하에 재평가 여지 있음(현재 등급 유지, 재론 시 참고).
- **#31 [bo-api/src/main/java/com/ge/bo/service/AuthService.java:251-261, 263-272]** refreshToken 쿠키가 **`.secure(false)`로 하드코딩**되어 있다(`:255`, 주석에 `// 운영 환경에서는 true로 변경`). 프로필 분기나 프로퍼티 참조가 없어 **어떤 환경에서도 항상 false**다. 세션 쿠키는 dev에서 `secure: true`로 관리되는데(`application-dev.yml:50`) refresh 쿠키만 누락됐다. HTTP 평문 구간에서 7일짜리 refresh 토큰이 탈취될 수 있다. `clearRefreshTokenCookie`(`:263-272`)도 동일하다. *완화 요소*: `httpOnly(true)`, `sameSite("Strict")`, `path("/api/v1/auth")`는 올바르게 설정되어 있다. — E / Warning
- **#32 [bo-api/src/main/resources/application.yml:32 + JwtTokenProvider.java:41 + AuthService.java:399]** `app.jwt.expiration: 86400`의 주석은 "1시간 (초)"이나 실제 계산은 `jwtExpirationMs * 1000`이므로 액세스 토큰 수명은 **24시간**이다. 반면 응답의 `expiresIn`은 `3600`으로 하드코딩되어 프론트는 1시간으로 인식한다. **프론트가 폐기했다고 믿는 토큰이 서버에서는 23시간 더 유효**하며, 탈취 시 노출 창이 의도의 24배다. — C / Warning
- **#33 [bo-api/src/main/java/com/ge/bo/config/SecurityConfig.java:131]** `/api/v1/auth/refresh`가 permitAll이며 호출 빈도 제한이 없다. 유효한 refresh 토큰 1개로 accessToken을 무제한 대량 발급받을 수 있고, 매 호출마다 `findByEmail` + `roleRepository.findByCode` DB 조회가 2회 발생한다(`AuthService.java:201, 208`). — F / Warning
- **#34 [bo/src/lib/api.ts:62-65]** 재시도 요청 `return api(original)`이 **try 블록 안에 있다.** 토큰 갱신은 성공했는데 재시도 요청이 401 이외 사유(500/타임아웃/네트워크 단절/403)로 실패하면 catch가 이를 "refresh 실패"로 오인해 **강제 로그아웃 + 로그인 페이지 이동**을 실행한다. 일시적 백엔드 오류가 관리자 세션 강제 종료로 이어지는 자기 DoS 성격의 결함이다. 동시 다발 401이 모두 실패하면 각 요청이 개별적으로 `/auth/logout` POST와 `window.location.href` 대입을 중복 수행한다(무한 재시도 루프 자체는 `_retry` 플래그로 정상 차단됨). — I / Warning
- **#35 [bo-api/src/main/java/com/ge/bo/service/AuthService.java:194-224]** 토큰 갱신이 `login_log`에 기록되지 않는다. 24시간 accessToken + 7일 refresh 조합에서 실제 세션 지속 기간을 감사 로그로 재구성할 수 없다. — J / Info

### ⑤ 로그아웃

- **#36 [bo-api/src/main/java/com/ge/bo/service/AuthService.java:240-242, 263-272]** (**JWT 모드**) **로그아웃에 실효성이 없다.** `logoutWithJwt()`가 하는 일은 `Set-Cookie: refreshToken=; Max-Age=0` 헤더 한 줄, 즉 **브라우저에게 쿠키를 버리라고 요청하는 것**이 전부다.
  - **accessToken 무효화 없음** — 블랙리스트/버전 카운터/`jti` 저장소가 프로젝트에 존재하지 않는다. 로그아웃 후에도 탈취된 액세스 토큰은 **최대 24시간** 그대로 통과한다(`JwtAuthenticationFilter.java:36`은 서명·만료만 확인).
  - **refreshToken 무효화 없음** — 서버측 저장소가 없으므로 이미 복사된 refresh 토큰은 **7일간 계속 유효**하고, 로그아웃 후에도 `/api/v1/auth/refresh`로 새 accessToken을 계속 발급받을 수 있다.
  - 인증을 요구하지 않아(`/api/v1/auth/**` permitAll) 미인증 상태로도 200이 반환된다.
  즉 "로그아웃 API가 존재한다"는 사실만으로는 세션 종료가 보장되지 않는다. — B / **Critical**
  - *(**redis 모드**는 정상: `AuthService.java:231-233 → :555-562`의 `invalidateLoginSession()`이 `session.invalidate()`로 서버측 세션을 실제 파기한다.)*
  - 🔄 **재검토 완료 / 미해결 유지(2026-08-08)**: `#보안성검토`(디스패처 직접 Read + `java-security-reviewer` 교차검증)로 재확인, 지적 그대로 성립. 현재 `logoutWithJwt()`(`AuthService.java:249-251`) 본문은 `clearRefreshTokenCookie(response);` **단 한 줄**이고, `clearRefreshTokenCookie()`(`:272-281`)는 `maxAge(0)` 쿠키만 세팅한다. 진입점 `AuthController.java:120-129`도 JWT 모드에서 `logoutWithJwt(response)` 호출이 전부이며 **accessToken을 인자로 받지도 않아** 무효화 대상을 알 방법 자체가 없다. 블랙리스트/jti/토큰버전 부재는 `#30`과 동일 근거(전수 grep 0건). 노출 창 실측값 재확인 — accessToken **24시간**(`application.yml:32`, 주석은 "1시간 (초)"로 여전히 틀림 = `#32`), refreshToken **7일**(`application.yml:33`, 쿠키 maxAge도 `AuthService.java:47,266`의 `REFRESH_TOKEN_DAYS=7L`). ⚠️ **`#32`와의 결합 재확인**: 응답 `expiresIn`이 `3600L`로 하드코딩된 지점이 3곳(`AuthService.java:224`, `:408`, `TotpService.java:33`)이라 프론트는 1시간으로 인식하는데 서버 토큰은 24시간 유효 — **이 항목의 실제 노출 창이 프론트 기대치의 24배**다. redis 모드 정상 동작(`AuthController.java:122-123` → `AuthService.java:240-242` → `:567-574` `session.invalidate()`)도 재확인.
- **#37 [bo/src/store/auth-store.ts:39, 56, 74, 48, 79 + bo-api/AuthService.java:266]** 쿠키에 `Secure` 플래그가 없다. 프론트의 `bo_is_system` 쿠키는 `SameSite=Strict`만 지정하고 `Secure`를 누락했으며, 백엔드의 refresh 쿠키 만료 처리도 `.secure(false)`라 원본 쿠키가 `Secure`로 설정된 환경에서는 속성 불일치로 삭제가 실패할 수 있다. — E / Info
- **#38 [bo/src/components/layout/header.tsx:161-171 + bo/src/store/use-site-store.ts:57-59]** 로그아웃 시 캐시 소거가 `queryClient.removeQueries({ queryKey: ["menus","nav"] })` **한 종류뿐**이라 이전 관리자가 조회한 나머지 React Query 캐시가 같은 탭에 잔류한다. 또한 `activeSiteId`가 localStorage에 남아 다음 로그인 계정이 이전 사이트 컨텍스트를 승계하며, 이 값은 `X-Site-Id` 헤더로 서버에 전달된다. `header.tsx:65-71`의 "사이트 없음" 강제 로그아웃 경로와 `api.ts:66-72`의 강제 로그아웃 경로도 동일하게 캐시를 비우지 않는다. 계정 전환 시 이전 관리자 데이터(PII 포함)가 화면에 노출될 수 있다. — J / Info
- **#39 [bo-api/src/main/java/com/ge/bo/service/AuthService.java:231-242]** 로그아웃이 `login_log`에 기록되지 않는다. 로그인만 기록되고 세션 종료 시점 감사가 불가능하다. — J / Info

### ⑥ 클라이언트 인증상태 관리

- **#40 [bo/src/middleware.ts:75-81 + bo/src/store/auth-store.ts:39]** **시스템관리자 전용 경로 보호가 클라이언트가 조작 가능한 쿠키에만 의존한다.** 미들웨어는 `request.cookies.get("bo_is_system")?.value !== "true"`로 접근을 판정하는데, 이 쿠키는 `auth-store.ts:39`에서 `document.cookie`로 기록되는 **non-httpOnly 쿠키**다. 즉 로그인한 일반 관리자가 개발자도구에서 `document.cookie="bo_is_system=true; path=/"` 한 줄로 `/admin/system`, `/admin/database`, `/admin/settings/slug-registry`, `/admin/templates/make`, `/admin/templates/layer`(`:4-10`) 전 경로의 보호를 무력화할 수 있다. 더 심각한 것은 `:58` 주석이 "클라이언트 가드(SystemAdminGuard)가 2차 보호 담당"이라 명시하지만 **`SystemAdminGuard`가 코드베이스에 존재하지 않는다**는 점이다(전체 grep 0건). 유일한 추가 통제인 `sidebar.tsx:144-145`는 메뉴 표시 필터일 뿐 라우트 차단이 아니며, 판정 기준도 미들웨어(`isSystem` 쿠키)와 사이드바(`role === "SYSTEM_ADMIN"`)로 이원화되어 있다. **①-B에서 확인한 대로 서버측 `@PreAuthorize`마저 누락되어 있어, 이 UI 우회가 곧바로 실제 데이터 접근으로 이어진다.** — B / **Critical** — ⬇️ **재평가: 부분 해결(2026-08-08)**: `#보안성검토`(디스패처 직접 Read + `bo-security-reviewer` 교차검증)로 `#10`/`#51`/`#61` 적용 이후를 재확인.
  - ❌ **프론트 우회 메커니즘 자체 — 미해결(코드 한 줄도 안 바뀜)**: 미들웨어 판정은 여전히 `middleware.ts:82-83`의 `request.cookies.get("bo_is_system")?.value !== "true"` 단독이고(세션 검증·JWT 파싱·bo-api 조회 등 서버 신뢰 근거 **추가 0건**, httpOnly refreshToken 쿠키를 갖고 있음에도 판정에 쓰지 않음), 이 쿠키는 `auth-store.ts:39`(login)·`:56`(setAccessToken)·`:74`(initFromStorage) 3곳에서 `document.cookie`로 기록되는 **non-httpOnly + Secure 플래그 없음** 쿠키다. `SystemAdminGuard`는 전수 grep 결과 **`middleware.ts:62` 주석 1건**뿐이고 구현체가 없으며, 대체 계층도 없다 — `app/admin/layout.tsx`는 `{children}`만 반환하는 빈 래퍼, `auth-provider.tsx:45`는 `isLoggedIn`만 검사하고 `isSystem`은 보지 않는다. 즉 **미들웨어 쿠키 판정이 프론트의 유일한 라우트 보호 계층**이라는 구조는 그대로다. 사이드바 기준 이원화(`sidebar.tsx:144`의 `adminInfo?.role === "SYSTEM_ADMIN"` 하드코딩 vs 미들웨어·서버의 `role.is_system`)도 그대로 — `SecurityConfig` 주석이 "역할명이 아니라 `role.is_system` 기준"이라 못박은 규약을 사이드바만 위반 중.
  - ✅ **"전 경로 무력화" 서술은 이제 과장 — 7개 경로 중 5개는 서버측에서 차단됨**: `SYSTEM_ADMIN_PATHS` 7개(`middleware.ts:4-12`) 중 `/admin/settings/users`(`AdminController` 클래스 레벨), `/admin/settings/roles`(`RoleController`), `/admin/database`(`DatabaseController`), `/admin/settings/slug-registry`(`SlugRegistryController` 메서드별), `/admin/system` 대부분(`ApiInfoController`/`SlugRelationController`/`SlugEntityController`)은 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")`가 채워져 **쿠키를 조작해 화면에 진입해도 API가 403이라 데이터를 읽거나 바꿀 수 없다**(`SecurityService`는 클라이언트 값이 아닌 DB `role.is_system` 조회 기반). `/admin/templates/layer`는 서버 API 호출 자체가 없어(유일한 흔적이 `SampleDrawer.tsx:160`의 주석 처리된 호출) 실피해 없음.
  - ~~❌ **잔존 실피해 경로는 `/admin/templates/make` 하나 — 상세는 신규 항목 `#64` 참조.**~~ → ✅ **차단됨(2026-08-08, `#64` 수정 완료)**: 해당 3개 컨트롤러(`PageTemplateController`/`TsxGenerationController`/`ValidationRuleController`)에 `@PreAuthorize`가 채워져 쿠키를 조작해 화면에 진입해도 쓰기 API가 403이다.
  - ⚠️ **정정(2026-08-08, `#보안성상세분석` 재검증)**: 바로 위 "`SYSTEM_ADMIN_PATHS` 7개 경로 전부 서버측 차단"은 **과장이었다.** `#64`가 잠근 3개 컨트롤러 외에, `/admin/templates/make` 화면이 **함께 호출하는** `PageDataController`(page-data CRUD·그룹삭제·엑셀추출)/`FileMetaController`/`PageFileController`/`CodeController`(공통코드 CRUD) **4개는 `@PreAuthorize`가 단 한 곳도 없다**(세션 에이전트가 4개 파일 직접 Grep으로 재확인, `java-security-reviewer` 독립분석과 일치). 이 사실관계 자체는 유효하나, "인가 어노테이션 없음"을 곧바로 취약점으로 단정한 것은 성급했다는 사용자 지적에 따라 신규 항목 `#65`로 분리해 **보류 처리**했다 — 상세는 `#65` 참고.
  - 📌 **결론**: 심각도 **Critical → Warning 하향, 유지.** 쿠키 결함 자체(방어 계층이 조작 가능한 클라이언트 값 하나뿐)는 구조적으로 미해결이지만, `#64` 대상 3개 컨트롤러에 대해서는 실제 데이터 침해로 이어지던 경로가 **해결됨(2026-08-08)**. 다만 같은 화면이 호출하는 다른 4개 컨트롤러(`#65`)는 **쿠키와 무관하게 이미 열려 있던 별도의 더 심각한 구멍**이므로, `#40`의 잔존 위험도를 논할 때 `#65`의 존재로 착각해 안심하지 말 것. ⑥-B 셀은 `#43`과 이 항목의 구조적 결함이 남아 FAIL 유지(`#64`는 해소, `#65`는 ①-B 등 별도 셀에서 추적).
- **#41 [bo/.env.local:4]** `.gitignore`의 `.env*` 규칙에도 불구하고 `.env.local`이 **git에 추적 중**이다(`git ls-files .env.local` 확인 — 이미 추적된 파일에는 gitignore가 적용되지 않음). 파일 내 `NEXT_PUBLIC_GOOGLE_MAPS_KEY=AIzaSyBkHvBn_MG-VWQd2C8-UA3PJBniWPy5QNk`는 실제 키이며 서버 설정(`bo-api/application-developer.yml:91`)과 동일한 값이 저장소에 커밋되어 있다. 파일 상단 주석의 "git 미추적"이라는 서술은 사실과 다르다. — C / Warning
- ✅ **#42 [bo/next.config.ts:25-39, bo/src/middleware.ts:25-40]** 두 곳 어디에도 `Content-Security-Policy`와 `X-Frame-Options`(또는 CSP `frame-ancestors`)가 없다. `nosniff`/HSTS/`Referrer-Policy`/CORP/COOP는 설정되어 있으나 **COOP는 프레이밍을 막지 못하므로** 전 관리자 화면에 Clickjacking이 성립한다. — D, E / Warning — ✅ **수정 완료(2026-08-06)**: ①-E와 동일 건, 전 경로 헤더 적용으로 함께 해결
- **#43 [bo-api/src/main/java/com/ge/bo/config/SecurityConfig.java:128 vs :66-69, 203-222]** 인증상태 강제 수준이 모드별로 크게 다르다. redis 모드는 `sessionFixation().changeSessionId()` + `maximumSessions(1)`로 세션 고정 방어와 동시 세션 1개를 서버가 강제하지만, **JWT 모드(`STATELESS`)는 이 통제가 전부 미적용**이다. 동일 계정으로 발급된 토큰이 몇 개든 모두 유효하고 서버가 세션 수를 알 수 없다. 관리자 백오피스 기준 24시간 액세스 토큰 수명도 과도하다. — B / Warning
- **#44 [bo/package.json:44]** 프론트엔드 `dependencies`에 PostgreSQL 드라이버 `pg ^8.20.0`이 포함되어 있다. 서버 전용 코드에서만 사용되는지, 클라이언트 번들 경로로 유입될 여지가 없는지 확인이 필요하다. — H / Info
- **#45 [bo/src/middleware.ts:48, 87]** matcher가 `"/:path*"`로 전 경로에 적용되지만 `Cache-Control: no-store`는 `badRequest()` 응답에만 설정된다. 인증 화면 및 보호 화면 자체의 캐시 정책이 지정되어 있지 않다. — I / Info
- **#46 [bo/src/store/auth-store.ts:4-10]** 클라이언트 메모리에 `id/name/email/role/isSystem`이 상주한다. 영속 저장이 없다는 점은 양호하나, 화면 표시 목적이 없다면 `email` 등은 최소화 검토가 필요하다. — J / Info

### 추가 발견 (교차검증 과정에서 신규 발견, ① 단계 소속)

- ✅ **#48 [bo-api/src/main/java/com/ge/bo/entity/LoginLog.java:41-42 + service/LoginLogService.java:90-98 + controller/AuthController.java:59-65]** `extractClientIp()`가 `X-Forwarded-For` 헤더를 검증 없이 신뢰하는데, `LoginLog.clientIp` 컬럼(`length=50`)은 `userAgent`(500자)와 달리 **길이 절단 코드가 없다.** 51자 이상의 XFF 헤더를 보내면 INSERT가 실패하고 그 예외는 `log.warn`으로 삼켜진다(`LoginLogService.java:95-98`) — **공격자가 헤더 하나로 자신의 로그인 시도 전체를 접속이력에서 사라지게 만들 수 있다(감사 회피)**. 브루트포스 시도가 화면상 흔적 없이 진행 가능. — F / **Critical** — 🔍 **신규 발견(2026-08-06)**: `#7` `#보안성상세분석` 중 `nextjs-security-reviewer`가 발견 — ✅ **수정 완료(2026-08-06)**: `LoginLogService.saveAsync`에서 `userAgent` 500자 절단과 **동일한 substring 패턴**으로 `clientIp`도 컬럼 길이(50)에 맞춰 절단(신규 유틸 도입 없음). **검증**: 65자 `X-Forwarded-For` 헤더를 실어 `POST /api/v1/auth/login` 호출 후 psql 직접 조회 — `login_log` 신규 행(id=105, `login_email=sectest48`, `status=FAIL`, `fail_reason=USER_NOT_FOUND`)이 **정상 INSERT**되고 `length(client_ip)=50`으로 절단 저장됨을 확인. 즉 긴 XFF로 접속이력을 누락시키는 감사 회피가 더 이상 성립하지 않음
- ✅ **#49 [bo-api/src/main/java/com/ge/bo/config/SecurityConfig.java]** redis 세션 분기에서 `.exceptionHandling()`이 두 번 호출되어 뒤쪽 설정이 앞쪽을 덮어쓴다. 덮이는 앞쪽 핸들러의 JSON 텍스트가 닫는 `}`가 누락되어 깨진 상태다. 현재는 덮어쓰기 덕에 표면화되지 않지만, 설정 순서가 바뀌는 순간 인증 실패 응답이 파싱 불가한 JSON으로 나가게 된다. — D / Warning — 🔍 **신규 발견(2026-08-06)**: `#7` `#보안성상세분석` 중 `java-security-reviewer`가 발견 — ✅ **수정 완료(2026-08-06)**: redis 분기의 **앞쪽(덮이는) `.exceptionHandling()` 블록을 제거**하고, 실제 적용되던 뒤쪽 핸들러만 남김(동작 변화 없음). **검증**: bo-api 재기동 성공(`Started BoApplication in 9.812 seconds`, 8080 LISTENING) + 에러 응답 전 경로 정상 JSON 확인 — 로그인 실패 `{"error":"INVALID_CREDENTIALS",...}` 401, 미인증 접근 `{"code":"UNAUTHORIZED","message":"인증이 필요합니다."}` 401, 잘못된 토큰 401, `/health` 200. ⚠️ **검증 한계**: 로컬은 `ls.redis-enabled=false`(profile `local`)라 JWT 분기가 동작하므로, 수정한 **redis 분기 자체의 런타임 동작은 미검증**(컴파일·기동 성공까지만 확인). 원래 지적된 "덮이는 죽은 코드 제거"라 동작 변화가 없는 수정임
- ✅ **#51 [bo-api/src/main/java/com/ge/bo/controller/RoleController.java(@PreAuthorize 0건) + service/RoleService.java:87]** `#10`과 완전히 동일한 인가 누락이 **역할(Role) API에도 존재**한다. `SecurityConfig.java:99/136` 주석이 "역할 API — @PreAuthorize로 SUPER_ADMIN 제한"이라 명시하지만 `RoleController`에는 `@PreAuthorize`가 한 건도 없어 `.authenticated()`만 걸린다. 즉 **최하위 권한 계정을 포함한 모든 로그인 사용자**가 `POST/PATCH/DELETE /api/v1/roles`로 인가 체계의 근간인 역할 정의 자체를 조작할 수 있다. 여기에 `RoleService.createRole`(`:87`)의 `.isSystem(request.isSystem())`이 결합되어 **클라이언트가 요청 바디로 보낸 `is_system` 플래그를 서버가 그대로 신뢰**한다 — 임의의 인증 사용자가 `is_system=true`인 새 역할을 만들 수 있고, 이 플래그는 `SecurityService.isSystemAdmin()`이 시스템관리자 여부를 판정하는 **유일한 기준**이므로 곧바로 최고권한 상승으로 이어진다(`#40`의 `bo_is_system` 쿠키 조작과 결합하면 UI·API 양쪽 통제가 동시에 무력화). — B / **Critical** — 🔍 **신규 발견(2026-08-06)**: `#보안성상세분석`에서 `#10`의 `AdminController` 인가 누락을 추적하던 중 동일 패턴이 `RoleController`에도 있음을 확인 — ✅ **수정 완료(2026-08-06)**: ① `RoleController` 클래스 레벨에 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")` 적용(메서드 레벨 예외 없음), ② `RoleService.createRole`의 `.isSystem(request.isSystem())`을 `.isSystem(false)`로 고정해 신규 역할 생성 API로는 시스템 역할을 만들 수 없게 차단(bo 프론트의 `POST /roles` 호출 2곳 — `settings/roles/[id]`, `system/roles/[id]` — 모두 이 필드를 보내지 않음을 Grep으로 확인, 정상 사용처 없음). ③ 프론트 정합화로 `middleware.ts`의 `SYSTEM_ADMIN_PATHS`에 `/admin/settings/roles` 추가. **검증**: bo-api 재기동 후 curl 실검증 — (1) 시스템관리자 토큰 → `GET /roles`·`/roles/assignable`·`/roles/{id}` 전부 **200**, (2) 비시스템관리자 토큰 → 동일 3건 + `POST /roles` + `DELETE /roles/{id}` 전부 **403**, (3) 무토큰 → **401**, (4) `is_system` 강제 — 시스템관리자 토큰으로 `POST /api/v1/roles`에 `system:true`를 실어 보낸 뒤 psql 직접 조회 결과 신규 role(id=17, code=`SECTEST51`)의 `is_system`이 **`f`**로 저장됨(응답 바디도 `"system":false`). ⚠️ **검증 중 사실 정정**: 요청 바디의 실제 JSON 필드명은 `isSystem`이 아니라 **`system`**이다(Lombok `@Getter`가 `boolean isSystem` → `isSystem()`을 만들고 Jackson이 이를 `system` 프로퍼티로 해석). `isSystem`으로 보내면 `MALFORMED_JSON` 400으로 거부되므로, 원래 취약 경로는 `system:true`였다
- **#50 [bo-api/src/main/java/com/ge/bo/config/SecurityConfig.java — `expiredSessionStrategy` 블록]** `#49` 수정 작업 중 `security-fix-executor`가 발견. 실제로 닫는 `}`가 누락된 깨진 JSON은 `#49`가 지목했던 앞쪽 `.exceptionHandling()` 블록이 아니라 `sessionManagement`의 `expiredSessionStrategy`(다른 기기 로그인 시 세션 종료 메시지, redis 모드 한정) 쪽이었다. `#49` 승인 범위 밖이라 수정하지 않고 보고만 함. — D / Warning — 🔍 **신규 발견(2026-08-06)**: `#보안성검토수정`(#49) 작업 중 `security-fix-executor`가 발견, 미착수
- ✅ **#47 [bo-api/src/main/java/com/ge/bo/common/client/ExternalApiClient.java:100, 105, 109, 112]** `RecaptchaService.java:42`가 시크릿키를 쿼리스트링에 실어 만든 URL(`request.getUrl()`)이, 구글 API 호출 실패 시(4xx/5xx/네트워크오류/기타예외 4개 catch 분기 전부) WARN/ERROR 레벨로 **그대로 로그에 남는다**. `application-dev.yml:75-78` 등 로그레벨이 WARN이라 이 로그는 배포 환경에서도 살아있어, 구글 쪽에서 오류 응답 한 번만 나도 reCAPTCHA 시크릿키가 평문으로 로그 파일에 기록된다. — C / **Critical** — 🔍 **신규 발견(2026-08-06)**: `#6` 교차검증 중 `java-security-reviewer`가 `ExternalApiClient` 호출 경로를 추적하다 발견. 세션 에이전트 단독 분석에서는 놓쳤던 항목. ✅ **수정 완료(2026-08-06)**: #6과 동일 수정(시크릿을 URL→폼 바디로 이동)으로 해결 — WARN/ERROR 로그는 `request.getUrl()`만 찍고 body는 안 찍으므로 구조적으로 해결됨. **검증 상세**: `logback-spring.xml` 확인 결과 `local` 프로파일은 `%maskedMsg`(자체 마스킹 컨버터) 적용되지만, **dev 프로파일(JSON+파일 로그)은 `%msg`(마스킹 미적용, "PII 마스킹은 로그 수집 플랫폼에서 처리"라고 주석에 위임)** — 즉 dev 경로는 이 코드베이스 차원의 안전망이 없었으므로 원래 Critical 판정이 타당했음. 수정 후에는 마스킹 여부와 무관하게 시크릿이 URL에 실리지 않아 dev 프로파일에서도 안전함을 로그 직접 확인으로 검증
- ✅ **#52 [bo/src/middleware.ts]** `#10`/`#51` 수정으로 `SYSTEM_ADMIN_PATHS`에 `/admin/settings/users`,`/admin/settings/roles`가 추가됐는데, 보호 실패 시 리다이렉트가 `new URL("/admin/dashboard", request.url)`로 만들어져 **basePath(`/bo`)가 빠진 채 리다이렉트**된다(실제로는 `/bo/admin/dashboard`여야 함) — 비시스템관리자가 보호 경로 접근 시 대시보드로 돌아가는 게 아니라 404를 만난다. 보안 경계 자체(접근 차단)는 정상 동작하므로 취약점은 아니지만 UX 결함. — D / Warning — 🔍 **신규 발견(2026-08-06)**: `#10`/`#51` 부수 보고 검토 중 세션 에이전트가 발견 — ✅ **수정 완료(2026-08-06)**: `new URL(...)` 대신 `request.nextUrl.clone()` 사용(basePath 자동 포함)으로 교체. **검증**: `curl`로 `/bo/admin/settings/users` 무쿠키 요청 → `location: /bo/admin/dashboard` 확인, `bo_is_system=true` 쿠키로는 그대로 `200` 통과(회귀 없음)
- ✅ **#53 [bo/src/components/menus/menu-tree.tsx, bo-api/src/main/java/com/ge/bo/service/RoleService.java]** `#51`(RoleController 인가 적용)의 부수효과로 발견된 죽은 코드 2건: ① `menu-tree.tsx`의 `useRolesQuery()`가 이제 비시스템관리자에게 403을 유발하는데 그 결과값(`store.roles`)을 쓰는 곳이 없어 불필요한 호출이었음 ② `RoleService.getAllRoles()`의 "시스템관리자 여부에 따라 필터링" 분기가 `RoleController`의 클래스 레벨 인가로 이제 시스템관리자만 도달하므로 무의미해짐. 보안 이슈는 아니고 코드 정리 항목. — H / Info — 🔍 **신규 발견(2026-08-06)**: `#51` 작업 중 `security-fix-executor`가 보고 — ✅ **수정 완료(2026-08-06)**: `useRolesQuery` 호출/import 제거, `getAllRoles()`의 분기 로직 및 미사용 `isCurrentUserSystemAdmin()`/`SecurityContextHolder` import 제거. **검증**: bo-api 정상 기동(컴파일·DI 문제 없음) + `GET /roles` 무토큰 401 정상, bo 정상 기동(컴파일 에러 없음)
- ✅ **#54 [bo-api/src/main/java/com/ge/bo/service/LoginAdminService.java:20-27]** **계정 잠금이 실제로는 전혀 동작하지 않았다.** `incrementFailure()`가 `failedLoginAttempts` 숫자만 증가시키고 `admin.setActive(false)`를 호출하지 않아, 5회를 넘겨도 계정은 계속 활성 상태였다. `AuthService.java:155-158`(로컬)과 `:305-308`(SSO)의 `if (attempts >= maxFailedAttempts)` 분기는 그 시도의 응답만 바꿀 뿐 다음 시도를 막지 못했으므로, **브루트포스가 횟수 제한 없이 무제한 지속 가능**했다. `#9`(Rate Limiting 부재)가 "그래도 계정 잠금은 있다"를 전제로 심각도를 산정했던 것과 달리, 실제로는 로그인 무차별 대입에 대한 방어가 **아무것도 없었다.** — F / **Critical** — 🔍 **신규 발견(2026-08-07)**: `#보안성상세분석` 중 `#12`(자동 해제 부재)를 검증하려고 잠금 동작을 추적하다 발견 — ✅ **수정 완료(2026-08-07)**: `incrementFailure()`에 `if (attempts >= maxAttempts) { admin.setActive(false); }` 추가. 이 함수는 로컬/SSO 두 경로가 공유 호출하므로 한 곳 수정으로 양쪽 적용. 로그인 앞부분의 기존 `if (!admin.isActive())` 체크(`#3`으로 401 `INVALID_CREDENTIALS` 통일됨)가 자동으로 차단을 담당하므로 신규 분기 없음. **검증**: 테스트계정 `compjj`(id=23) password_hash 백업 후 알려진 해시로 교체 → 사전 정상 로그인 200 확인 → 틀린 비밀번호 5회 curl(psql로 매회 추적: `t|1`→`t|2`→`t|3`→`t|4`→**`f|5`**) → **6번째로 올바른 비밀번호 제출해도 401 `INVALID_CREDENTIALS`** → `login_log` 직접 조회로 차단 지점 확인(5번째 `ACCOUNT_DISABLED`, 6번째 **`ACCOUNT_INACTIVE`** = 비밀번호 검증 이전에 차단) → `PATCH /api/v1/admins/23/status {"isActive":true}`(SYSTEM_ADMIN 토큰) 200으로 해제 → 정상 로그인 200 복구 및 `failed_login_attempts` 0 초기화 확인 → 테스트 후 password_hash·is_active·attempts 원상 복구 완료 — ✅ **후속 수정 완료(2026-08-07)**: `AdminService`의 계정 정상화 경로 3곳(`toggleStatus` L173-175 / `updateAdmin` L131-133 / `resetPassword` L152)에 `setFailedLoginAttempts(0)` 추가 — 관리자가 잠긴 계정을 재활성화해도 카운터가 5로 남아 다음 1회 오입력에 즉시 재잠금되던 문제 해소. 초기화는 "활성화/정상화" 방향일 때만 수행(`isActive:false`는 카운터 보존). **검증**: 5회 오답으로 `f|5` 잠금 재현 → `PATCH /api/v1/admins/{id}/status {"isActive":true}` HTTP 200 → psql로 `t|0` 리셋 확인 → 정상 로그인 즉시 성공(재잠금 없음). 음성 케이스 2건(`toggleStatus` false: `t|3→f|3`, `updateAdmin` false: `t|4→f|4`)에서 카운터 보존 확인, `updateAdmin` true(`f|5→t|0`)·`resetPassword`(`f|5→f|0`) 리셋 확인
- ✅ **#59 [bo-api/src/main/java/com/ge/bo/controller/EmailSendHisController.java]** 클래스/메서드 어디에도 `@PreAuthorize`가 없어 `SecurityConfig.anyRequest().authenticated()`에만 걸려 있었다. 같은 "로그" 카테고리의 `LoginLogController`/`ErrorLogController`/`TransactionLogController`는 전부 클래스 레벨 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")`가 걸려 있는데 이것만 빠져, **모든 로그인 사용자(비시스템관리자 포함)가 이메일 발송 이력을 조회**할 수 있었고 특히 `POST /{id}/resend`는 조회가 아니라 **실제 메일 재발송을 트리거**하는 상태 변경 엔드포인트였다. — B / **High** — 🔍 **신규 발견(2026-08-07)**: `#13` `#보안성상세분석` 중 4개 "로그" 컨트롤러 인가를 대조하다 EmailSendHis만 누락된 것을 확인 — ✅ **수정 완료(2026-08-07)**: 다른 3개 로그 컨트롤러와 동일하게 클래스 레벨 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")` 적용, 클래스 주석도 실제 동작(시스템관리자 전용)에 맞게 정정. 소비처는 `bo/src/app/admin/logs/email/` 화면뿐이고 부모 메뉴가 시스템관리자용이라 정상 사용에 영향 없음. **검증**: bo-api 재기동 후 curl 3-way — GET 목록·GET /{id}·POST /{id}/resend 전부 무토큰 **401**, 비시스템관리자(SUPER_ADMIN) 토큰 **403**, 시스템관리자 토큰으로 GET 목록 **200**. ⚠️ **검증 한계**: `resend`의 시스템관리자 200 성공 케이스는 실제 메일 발송을 유발하므로 실행하지 않음(클래스 레벨 인가가 resend에도 균일 적용됨은 비시스템 403으로 입증).
- ✅ **#60 [bo-api/.../common/context/SiteContext.java + filter/SiteContextFilter.java + entity/{LoginLog,ErrorLog,TransactionLog}.java + 대응 Service 3종 + AuthService/GlobalExceptionHandler/TransactionLogFilter 호출부]** 감사 로그 3종(`login_log`/`error_log`/`transaction_log`)에 **요청 사이트 식별자(site_id)가 전혀 기록되지 않았다.** `SiteContext`는 timezone(`ZoneId`)만 ThreadLocal로 보관하고 원본 `X-Site-Id` 숫자값은 어디에도 남기지 않아, 멀티사이트 환경에서 "어느 사이트에서 발생한 로그인/오류/변경인지" 사후 추적이 불가능했다(`email_send_his`/`search_keyword_log`는 이미 site_id를 갖는 기존 컨벤션인데 이 3종만 누락). — G / Warning — 🔍 **신규 발견(2026-08-07)**: `#13` 심층분석 중 감사 로그의 사이트 귀속 추적 가능성을 점검하다 발견 — ✅ **수정 완료(2026-08-07)**: ① `SiteContext`에 `CURRENT_SITE_ID`(ThreadLocal&lt;Long&gt;) + `setSiteId/getSiteId` 추가(zone과 함께 clear), ② `SiteContextFilter`가 파싱한 `X-Site-Id`를 DB조회 없이 그대로 set + `@Order(HIGHEST_PRECEDENCE)`로 로깅 필터보다 바깥에서 감싸도록 조정, ③ 3개 엔티티에 **nullable** `site_id` 컬럼 추가(#55 교훈 반영 — `nullable=false` 미사용, `ddl-auto: update`로 기존 데이터 있는 테이블에 자동 생성 성공), ④ 3개 Service `saveAsync`에 `siteId` 파라미터 추가 + 호출부 30곳(AuthService 18·GlobalExceptionHandler 11·TransactionLogFilter 1)에서 요청 스레드의 `SiteContext.getSiteId()`를 추출해 전달(@Async 스레드 ThreadLocal 미전파 대응). **조회 API에는 site 필터링을 추가하지 않음**(기록까지만, 시스템관리자 전체 조회 동작 그대로 유지 — 필터링 강제 여부는 별도 정책 결정 후 진행 예정). **검증**: X-Site-Id 헤더 유무별 psql 직접 조회 — `login_log`(헤더7→site_id=7, 헤더99→99, 무헤더→NULL), `error_log`(헤더55→55, 무헤더→NULL), `transaction_log`(헤더77→77, 무헤더→NULL) 전부 확인. 무헤더 NULL 저장 시 예외/저장실패 WARN 0건. site_id 3컬럼 자동 생성 및 앱 정상 기동(9.956s) 확인.
- ✅ **#61 [bo-api/src/main/java/com/ge/bo/controller/DatabaseController.java]** `@PreAuthorize` 없이 `authenticated()`에만 걸려 **모든 로그인 사용자가 DB 스키마 메타데이터(테이블 목록·컬럼 정보·JPA 엔티티/필드 구조)를 조회**할 수 있었다. 내부 데이터 모델 구조가 비시스템관리자에게 노출되어 후속 공격의 정찰(reconnaissance) 정보를 제공했다. — B / **High** — 🔍 **신규 발견(2026-08-07)**: `#13` 심층분석 중 인가 누락 컨트롤러를 전수 점검하다 발견(`#10`/`#51`과 동일 패턴) — ✅ **수정 완료(2026-08-07)**: 클래스 레벨 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")` 적용. 소비처는 `use-database-store.ts`/`use-entity-store.ts`뿐이고 해당 메뉴(id=42/44, 부모 "DB 시스템" `is_system=t`)가 원래 시스템관리자 전용이라 영향 없음. **검증**: `/database/tables`·`/database/entities` 각각 무토큰 **401** / 비시스템관리자 **403** / 시스템관리자 **200** 확인.
- ✅ **#63 [bo-api/src/main/java/com/ge/bo/service/ErrorLogService.java, exception/GlobalExceptionHandler.java]** 보안 이슈는 아니고 **가용성/데이터 무결성 버그**. `ErrorLogService.saveAsync()`가 `@Async`(비동기 스레드)인데 그 내부에서 `HttpServletRequest`를 직접 참조해 `method`/`requestUrl`/`clientIp`를 그 시점에 추출하고 있었다. Tomcat은 응답이 끝난 request 객체를 스레드풀 재사용을 위해 회수(recycle)하므로, 비동기 스레드 실행이 지연되면(레이스 컨디션) `The request object has been recycled and is no longer associated with this facade` 예외가 발생해 해당 오류로그 저장이 **조용히 실패**한다(예외를 삼키고 WARN만 남기므로 클라이언트 응답에는 영향 없어 발견이 어려움). 매번 재현되는 게 아니라 타이밍에 따라 간헐적으로만 발생 — `#15`(`X-Forwarded-For` 정리) 코드 변경과 무관한 기존 버그였으나, `#15` 수정 후 주석 정리(`X-Forwarded-For 우선` → `getRemoteAddr() 기준`) 작업의 런타임 재검증 과정에서 실제로 재현되어 발견됨. — I / Warning — 🔍 **발견 경위(2026-08-08)**: 사용자가 "코드로직 변경도 검증해야지"라고 재검증을 요구해 재기동 후 실호출 테스트하던 중 서버 로그에서 `WARN ErrorLogService - 오류로그 저장 실패` 실측 — ✅ **수정 완료(2026-08-08)**: 같은 파일에 이미 있던 `loginUser`/`siteId` 패턴(요청 스레드에서 미리 추출해 파라미터로 전달)과 동일하게, `saveAsync()` 시그니처를 `HttpServletRequest` 대신 `method`/`requestUrl`(String)/`clientIp`(String) 3개 파라미터로 변경. `GlobalExceptionHandler`의 11개 호출부 전부(`extractLoginUser()`/`currentSiteId()`와 동일한 방식으로 신설한 `extractFullUrl()` + `request.getMethod()` + `ClientIpUtils.resolve(request)`)에서 요청 스레드에서 값을 미리 추출해 전달하도록 수정, `ErrorLogService`의 `getFullUrl()` 사설 메서드 및 `HttpServletRequest`/`ClientIpUtils` import 제거. **검증**: `gradlew compileJava` 성공 → bo-api 재기동 → 잘못된 JSON으로 5회 연속 `POST /auth/login` 호출(전부 `400 MALFORMED_JSON` 정상 응답) → 서버 로그에 `저장 실패` WARN **0건** → psql로 `error_log`에 5건 전부 `method=POST`, `request_url=/api/v1/auth/login`, `client_ip=0:0:0:0:0:0:0:1`로 정상 저장됨을 직접 확인(수정 전에는 같은 패턴의 요청에서 간헐적으로 저장 실패가 발생했었음). 테스트 데이터 삭제 완료. 동일 비동기 request-참조 패턴이 `LoginLogService`/`TransactionLogService`에는 없음을 grep으로 확인(대상은 `ErrorLogService` 1곳뿐).
- ✅ **#64 [bo-api/src/main/java/com/ge/bo/controller/PageTemplateController.java, TsxGenerationController.java, ValidationRuleController.java (3개 파일 전부 @PreAuthorize 0건)]** `#10`/`#51`/`#61`과 **완전히 동일한 인가 누락**이 페이지 템플릿 계열 3개 컨트롤러에 남아 있다. `SecurityConfig`의 `.anyRequest().authenticated()`(`:109`/`:152`)에만 걸리므로 **최하위 권한 계정을 포함한 모든 로그인 사용자**가 아래를 호출할 수 있다.
  - `PageTemplateController`(`/api/v1/page-templates`) — `:31` GET, `:37` GET/{id}, `:43` GET/by-slug/{slug}, `:51` **POST /generate**, `:59` **POST**, `:65` **PUT/{id}**, `:73` **DELETE/{id}**
  - `TsxGenerationController`(`/api/v1/tsx-generation`) — `:27` **POST(TSX 코드 생성)**, `:41` GET, `:53` GET/{id}, `:61` **DELETE/{id}**
  - `ValidationRuleController`(`/api/v1/validation-rules`) — `:27` GET, `:34` **POST**, `:40` **PUT/{id}**, `:47` **DELETE/{id}**

  이 3개는 `middleware.ts`의 `SYSTEM_ADMIN_PATHS`에 포함된 `/admin/templates/make` 화면이 소비하는 API다. 즉 `#40`이 지적한 쿠키 조작 우회가 **실제 데이터 조작으로 이어지는 유일한 잔존 경로**이며, 더 정확히는 **미들웨어 보호가 화면 라우트에만 걸리고 API에는 걸리지 않으므로 쿠키 조작조차 불필요하다** — 일반 관리자 토큰으로 API를 직접 호출하면 그대로 성립한다. 페이지 템플릿·생성 코드의 생성/수정/삭제가 가능하므로 BO 운영 화면 정의 자체를 훼손할 수 있다. — B / **Critical** — 🔍 **신규 발견(2026-08-08)**: `#보안성검토`의 `#40` 재검증 중 `bo-security-reviewer`가 `SYSTEM_ADMIN_PATHS` 7개 경로의 호출 API를 전수 추적하다 발견, 디스패처가 3개 파일 직접 Grep으로 `@PreAuthorize` 0건 확인. ⚠️ **수정 시 주의(디스패처 직접 확인, 소비처 추적 결과)**: `#10`/`#51`처럼 **클래스 레벨 일괄 적용을 하면 정상 기능이 깨진다.** `GET /page-templates`와 `GET /page-templates/by-slug/{slug}`는 시스템관리자 전용이 아닌 일반 운영 화면이 소비하고 있다 — `menus/menu-detail.tsx:52, 811`(메뉴 상세의 템플릿 선택), `admin/widget/[slug]/page.tsx:63`, `admin/widgetSub/[slug]/page.tsx:51`. 반면 쓰기 계열(POST/PUT/DELETE/generate)과 `tsx-generation`·`validation-rules`의 소비처는 `admin/templates/make/**`뿐이다. 따라서 **읽기는 `authenticated()` 유지, 쓰기만 `isSystemAdmin` 부여**하는 메서드 레벨 적용이 맞다. 미착수.
  - ⬆️ **`#보안성상세분석` 재검증(세션 에이전트 직접 오케스트레이션 — `java-security-reviewer`+`nextjs-security-reviewer` 블라인드 병렬 + 세션 에이전트 독립분석, security-review-dispatcher 미경유, 2026-08-08)**: 세션 에이전트가 3개 컨트롤러 소스를 직접 Read로 재확인(`@PreAuthorize` 0건 재확인) 및 `menu-detail.tsx`/`widget(Sub)/[slug]/page.tsx`가 GET만 호출함을 직접 grep으로 재검증. **Critical 등급 확정 + 근거 구체화**:
    - **`POST /page-templates/generate`가 가장 심각**: `tsxCode` 요청 바디를 검증 없이 그대로 파일로 쓰는데(`PageTemplateFileService.java:49-78`), output-dir이 3개 프로필 전부 `../bo/src/app/admin/generated`(`application-{local,developer,dev}.yml`)로 **bo Next.js 앱 자신의 소스 트리 내부**다 — 파일시스템을 공유하는 배치라면 다음 컴파일 시점에 `/admin/generated/{slug}` 라우트로 살아나는 **저장형 코드 주입**이 된다. 이 엔드포인트는 DB를 거치지 않아(`PageTemplateService.java:158-164`) `@CreatedBy` 감사 흔적조차 없다(`create`/`update`는 있음). `tsxCode`에 `@Size` 제한도 없어(`PageTemplateGenerateRequest.java:24-26`) 요청 1건당 최대 200MB(`application.yml:14`) 쓰기가 가능.
    - **경로 탈출(path traversal)은 이중 방어로 확실히 차단되어 있음을 확인**(`slug`/`fileName` 화이트리스트 정규식 + `toAbsolutePath().normalize()`+`startsWith(baseDir)` 경계검증) — 위험은 "임의 경로"가 아니라 "고정 디렉터리 내 임의 내용·무제한 크기"임을 명확히 함(과장 방지).
    - **High 추가 확인**: `DELETE /page-templates/{id}`는 DB행 삭제 후 실제 파일과 **부모 디렉터리까지 삭제**(실패는 조용히 삼킴, `PageTemplateFileService.java:87-108`) / `validation-rules` 변조는 `PageDataService.checkValidationRules()`(`:1624-1634`)가 소비하는 unique·maxCount 검증을 화면 오류 없이 무력화 / `tsx-generation` GET은 타인의 `configJson`·`tsxCode` 전문을 노출, DELETE는 복구불가.
    - **프론트 소비처 전수 매핑 완료**(nextjs-security-reviewer): 쓰기(POST/PUT/DELETE) 호출은 예외 없이 `/admin/templates/make/*` 하위에서만 발생(일반 화면에서 쓰기 호출 0건, 과장 아님) — 단 이 보호는 **UI 진입 차단일 뿐**, 실제 요청 경로(`/api/v1/*`)는 `middleware.ts`가 `/admin/...` 접두사만 매칭해 API 자체에는 걸리지 않으므로 로그인 토큰만 있으면 화면을 열지 않고도 API 직접 호출로 성립(기존 서술과 일치, 근거 구체화). 추가로 `GET /page-templates`(전체 목록, `configJson` 포함)가 `/admin/settings/menus`(비보호 화면)에서도 호출됨을 재확인.
    - Info 참고(수정 시 우선순위 아님): 응답에 서버 파일 절대경로 노출(`filePath`), `deleteFile()`에 `writeFile()`과 동일한 경계 재검증 없음(현재는 악용 불가, 심층방어 관점 참고).
    코드 수정 없음.
  - ✅ **수정 완료(2026-08-08)**: 3개 컨트롤러에 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")`를 **소비처 분석에 따라 혼합 적용**. ① `PageTemplateController` — 쓰기 4개(`generate`/`create`/`update`/`delete`)에만 **메서드 레벨** 부여(`SlugRegistryController` 선례와 동일 방식), GET 3개(`getAll`/`getById`/`getBySlug`)는 `menu-detail.tsx:52,811`·`widget/[slug]/page.tsx:63`·`widgetSub/[slug]/page.tsx:51` 등 시스템관리자 전용이 아닌 운영 화면이 소비하므로 `authenticated()` **그대로 유지**. ② `TsxGenerationController`, ③ `ValidationRuleController` — 소비처가 `admin/templates/make/**` 뿐임을 재확인(전 참조 16개 파일 전부 해당 경로 하위)하고 **클래스 레벨** 일괄 부여(GET 포함 전 엔드포인트). 신규 유틸 없이 기존 `SecurityService` 빈 재사용. **검증**: bo-api 재기동(`Started BoApplication in 10.603 seconds`, 8080 LISTENING) 후 무토큰 / 비시스템관리자(`comjejb`, role `SUPERRRRRRRRRRRRRRRR`, `isSystem:false` 응답으로 확인) / 시스템관리자(`comlbg`) **curl 3-way 전 엔드포인트 실행** — (1) 잠근 쓰기 4개: `POST /page-templates/generate` 401/403/**200**, `POST /page-templates` 401/403/**201**, `PUT /page-templates/{id}` 401/403/**200**, `DELETE /page-templates/{id}` 401/403/**204**. (2) **열어둔 GET 3개 회귀 확인**: `GET /page-templates`·`/page-templates/7`·`/page-templates/by-slug/prdGrp-list`(+`?type=PAGE`) 전부 **비시스템관리자도 200** — 정상 화면 안 깨짐. (3) `TsxGenerationController` 전 엔드포인트(GET 목록·GET 필터·GET/{id}·POST·DELETE) 401/403/2xx. (4) `ValidationRuleController` 전 엔드포인트(GET·POST·PUT·DELETE) 401/403/2xx. (5) 회귀: `/admins`·`/roles`·`/roles/assignable`·`/database/tables`(#10·#51·#61) 전부 401/403/200 유지. 테스트로 만든 `page_template`(id=137)·`tsx_generation`(id=1)·`validation_rule`(id=10) 및 `generate`가 실제로 쓴 파일 `bo/src/app/admin/generated/sectest64/page.tsx`(디렉터리째) 전부 삭제 확인, 테스트 계정 `password_hash` 원복 완료. ⚠️ **검증 중 확인된 사실**: `@Valid @RequestBody` 검증이 `@PreAuthorize`보다 먼저 실행되므로, 바디가 유효하지 않으면 비시스템관리자에게도 403이 아닌 400이 먼저 반환된다(요청은 거부되므로 보안상 문제 없음). 위 403 결과는 전부 **유효한 바디**로 재측정한 값이다. ⚠️ **미조치(승인 범위 밖)**: 같은 항목에 함께 기록된 Info/Warning 3건 — 응답의 서버 절대경로(`filePath`) 노출, `tsxCode` 업로드 크기 무제한, `deleteFile()` 경계 재검증 부재 — 는 이번 수정 대상이 아니어서 손대지 않음
- **#65 [bo-api/src/main/java/com/ge/bo/controller/PageDataController.java, FileMetaController.java, PageFileController.java, CodeController.java (4개 파일 전부 @PreAuthorize 0건)]** 페이지 실데이터(page-data), 첨부파일(file-meta/page-files), 전사 공통코드(codes) API에 **인가 어노테이션이 단 한 곳도 없다.** `SecurityConfig`의 `.anyRequest().authenticated()`에만 걸리므로 **최하위 권한 계정을 포함한 모든 로그인 사용자**가 아래를 호출할 수 있다.
  - `PageDataController`(`/api/v1/page-data/**`) — 임의 slug 지정 목록/단건 조회, 생성, 전체수정, 필드부분수정(PATCH), PK기반 삭제, **그룹 일괄삭제**, **엑셀/CSV 전량 추출(export)**
  - `FileMetaController`, `PageFileController` — 첨부파일 업로드/조회/다운로드/삭제
  - `CodeController`(`/api/v1/codes/**`) — 전사 공통코드 그룹·상세 CRUD 전체(다른 화면들이 참조하는 코드값 자체를 변경 가능)

  `#40`(쿠키 조작) 재검증 중 `/admin/templates/make` 화면의 실제 호출 API를 추적하다 발견됐지만, **`#40`과는 별개의 독립 결함**이다 — 쿠키를 조작하지 않아도, `/admin/templates/make` 화면에 들어가지 않아도, **로그인된 계정이면 이 4개 API를 직접 호출하는 것만으로 그대로 성립**한다. — B / ~~**Critical**~~ — 🔍 **신규 발견(2026-08-08)**: `#보안성상세분석 #40`(java-security-reviewer 독립분석, 세션 에이전트가 4개 파일 전부 직접 Grep으로 `@PreAuthorize` 0건 재확인) 중 발견.
  - ❌ **판정 철회(2026-08-08, 사용자 지적 + 세션 에이전트 근거 재확인) — 결함 아님으로 결론**: 최초 등록 시 "`@PreAuthorize` 없음 = Critical"을 기계적으로 적용했으나, 실제 근거를 확인한 결과 이는 결함이 아니라 **의도된 설계**로 판정한다. 근거:
    1. `SecurityConfig.java` 전체에 이 4개 컨트롤러를 언급하는 주석이 **0건**이다 — `#10`(AdminController)처럼 "관리자 API로 제한하려 했는데 코드에 못 걸었다"는 명시적 의도-구현 불일치 흔적이 전혀 없다.
    2. `CodeController`(`/codes`)는 `SYSTEM_ADMIN_PATHS`에 **없는** 일반 화면 `/admin/settings/codes`(`bo/src/store/use-code-store.ts:69,89,99,111,122,135,147`)에서 조회·등록·수정·삭제 전체를 직접 호출한다 — "일반 로그인 관리자가 공통코드를 CRUD하는 것" 자체가 이 화면의 존재 목적이다.
    3. `PageDataController`는 `WidgetRenderer.tsx`/`FieldRenderer.tsx`를 통해 시스템관리자 전용이 아닌 일반 운영 화면(메뉴, `manage/search` 등) 전반에서 콘텐츠 조회·수정에 쓰인다 — 시스템관리자로 제한하면 일반 운영자가 콘텐츠를 다루지 못해 앱이 정상 동작하지 않는다.
    최초 판정은 "인가 어노테이션 없음"이라는 사실 하나만으로 심각도를 매기고, `page-templates` GET·`slug-registry/active`처럼 이 매트릭스에도 이미 있는 "의도적으로 열어둔" 선례와 구분하지 않은 성급한 결론이었다. **#65는 결함 아님으로 종결한다. 추가 조치 없음, 재론 금지.**
- ✅ **#62 [bo-api/src/main/java/com/ge/bo/exception/GlobalExceptionHandler.java — handleHttpMessageNotReadableException]** JSON 파싱 실패(400) 응답만 유일하게 `body.put("detail", ex.getMessage())`로 **예외 원문을 클라이언트에 그대로 반환**했다. Jackson 예외 메시지에는 파싱 실패 위치·기대 토큰 등 내부 파서 정보가 담겨, 다른 핸들러들이 전부 한글 고정 메시지만 반환하는 정책과 어긋났다(정보 노출). — C / Warning — 🔍 **신규 발견(2026-08-07)**: `#13` 심층분석 중 GlobalExceptionHandler 핸들러별 응답 바디를 대조하다 이 핸들러만 원문 노출하는 것을 확인 — ✅ **수정 완료(2026-08-07)**: `body.put("detail", ...)` 라인 제거(클라이언트 응답에서만 삭제), `log.error(...)` 원문 로깅은 유지. **검증**: 잘못된 JSON 바디 POST → 응답에 `detail` 필드 없음(`{"error":"MALFORMED_JSON","message":"요청 데이터 형식이 올바르지 않습니다.","status":400,...}`), 서버 로그에는 `com.fasterxml.jackson.core.JsonParseException: Unexpected character...` 원문이 여전히 기록됨 확인.

---

## 공격 체인 (개별 이슈보다 이 조합이 핵심)

아래 세 결함이 동시에 존재해 **한 줄기 체인으로 시스템 전체 장악**이 가능하다.

1. **진입** — 로그인 폼에 하드코딩된 비밀번호(`login-form.tsx:57`)와 무력화된 reCAPTCHA(`next.config.ts:6`) + Rate Limiting 부재로 계정 확보 장벽이 사실상 없다.
2. ~~**권한 상승** — 낮은 권한 계정 하나만 있으면 `@PreAuthorize` 누락(`AdminController`)으로 `POST /api/v1/admins/{id}/reset-password`를 호출해 SUPER_ADMIN 비밀번호를 알려진 고정값 `test12345`(`AdminService.java:146`)로 초기화할 수 있다.~~ → ✅ **차단됨(2026-08-06, #10·#51)**: `AdminController`/`RoleController` 클래스 레벨 `@PreAuthorize` 적용으로 비시스템관리자 호출은 403, `tempPassword`도 매 호출 랜덤값으로 교체되어 이 고리는 끊겼다(비시스템관리자 토큰으로 `reset-password` 403 실검증).
3. **2차 인증 우회** — 그 계정으로 1차 로그인 후 받은 tempToken을 `refreshToken` 쿠키로 제출해 `/api/v1/auth/refresh`를 호출하면(`AuthService.java:203-233`) TOTP 없이 정식 accessToken이 발급된다. → 🔄 **미해결 유지(2026-08-08 재확인)**: 코드 결함 그대로. 커밋된 4개 프로필 조합에서는 비껴가 있으나 base-only 기동(미정의 프로필명) 시 즉시 발현.

> ~~🔄 **2026-08-08 재검토 시 확인된 별도 체인**: 위 체인과 무관하게, **일반 관리자 계정 하나만 있으면 `#64`(page-templates / tsx-generation / validation-rules 인가 부재)로 BO 페이지 템플릿과 생성 코드를 직접 생성·수정·삭제할 수 있다.** 2단계(권한 상승)를 거칠 필요조차 없고 UI 우회(`#40`)도 필요 없다 — API 직접 호출로 성립한다. 현재 남은 인가 공백 중 가장 짧은 경로다.~~ → ✅ **차단됨(2026-08-08, `#64` 수정 완료)**: 3개 컨트롤러의 쓰기 계열에 `@PreAuthorize`가 적용되어 비시스템관리자 토큰으로는 전부 403이다(실검증). 파일쓰기 경로 `POST /page-templates/generate` 포함.

여기에 JWT 시크릿 하드코딩(`application.yml:31`)이 겹치면, 소스 접근만으로 위 과정을 건너뛰고 임의 role의 토큰을 직접 위조하는 것도 가능하다.

---

## 검토 결과 양호했던 항목 (근거 확인 완료)

- **비밀번호 해시**: `BCryptPasswordEncoder(12)`(`SecurityConfig.java:167`) + `passwordEncoder.matches()`(`AuthService.java:152`) — 평문/MD5/SHA 미사용, 내부 비교는 상수시간
- **JWT 라이브러리 사용법**: jjwt 0.12.6의 `verifyWith().parseSignedClaims()`(`JwtTokenProvider.java:139-145`) — `alg:none` 거부 및 알고리즘 혼동 방어 정상, 시크릿 길이도 HS256 최소요건 충족
- **CORS**: 와일드카드 없는 명시 화이트리스트로 `allowCredentials(true)`와 안전하게 조합(`SecurityConfig.java:173-176`)
- **TOTP 등록 IDOR 없음**: `SetupRequest`에 식별자 필드 자체가 없고(`TotpDto.java:18-21`) 주체는 토큰/세션에서만 도출(`TotpService.java:280-296`). `getEmailFromTotpPendingToken`은 `type=TOTP_PENDING`을 명시 검증(`JwtTokenProvider.java:104-110`)
- **otpauth URI 인코딩**: 라이브러리가 label/secret/issuer 전부 `URLEncoder.encode` 처리(라이브러리 소스 직접 확인)
- **TOTP 코드 비교**: `timeSafeStringComparison()`이 XOR 누적 + 조기 return 없는 전 버킷 순회로 타이밍 사이드채널 차단
- **Access Token 메모리 전용 보관**: `auth-store.ts:29-36`, localStorage/sessionStorage 저장 grep 0건, zustand `persist` 미사용
- **AuthProvider 가드**: 미인증 시 `null` 렌더로 보호 화면을 한 프레임도 노출하지 않음(`auth-provider.tsx:56-70`), 루트 레이아웃 경유로 전 페이지 적용
- **refresh race 방어**: `_retry` 플래그 + 단일비행 `refreshPromise`로 동시 401 중복 갱신/재귀 차단(`lib/api.ts:12, 49-61`)
- **오픈 리다이렉트 없음**: 로그인/2FA 성공 후 이동 경로가 전부 고정 리터럴, `returnUrl` 계열 파라미터 미사용
- **자격증명 로그 미기록**: `/api/v1/auth/` 요청 바디가 트랜잭션 로그에서 제외(`TransactionLogFilter.java:39, 53`), `login_log`에 비밀번호·토큰 미저장(`LoginLog.java:20-62`)
- **스택트레이스 미노출**: 500 응답 바디에 고정 문구만 반환(`GlobalExceptionHandler.java:267-280`)

---

## 검토 한계 (UNPROVEN — 코드 정적분석으로 확정 불가)

- **의존성 CVE 대조**: 버전이 전부 최신 계열이고 lockfile로 고정되어 있음은 확인했으나, `npm audit`/OSV 등 SCA 도구를 실행하지 않았으므로 알려진 취약점 유무를 단정할 수 없다. 별도 SCA 실행 권고.
- **배포 환경 실제 헤더/캐시 정책**: 프록시·CDN·WAF 계층에서 추가되는 헤더는 코드에 없으므로 배포 구성 확인 필요.
- **운영(prd) 프로필**: 저장소에 `application-prd.yml`이 없다. `ls.redis-enabled` / `ls.totp.enabled` 조합에 따라 ④-B(2FA 우회)의 발현 여부가 갈리므로 운영 프로필 확정 시 재검토가 반드시 필요하다. ⚠️ **위험도 구체화(2026-08-08 재검증)**: 이건 "미래에 prd를 추가할 때"만의 문제가 아니다. `application.yml:5`의 `spring.profiles.active: ${SPRING_PROFILES_ACTIVE:developer}` 특성상 **존재하지 않는 프로필명으로 기동하면 오류 없이 base(`application.yml`)만 적용**되고, base의 조합이 정확히 `redis-enabled` 미정의(=false) + `totp.enabled: true`(`:46`)이므로 `#29`의 2FA 우회가 **그 순간 바로 성립**한다. 즉 `SPRING_PROFILES_ACTIVE=prd`처럼 오타 하나 또는 프로필 파일 누락만으로 발현되는 상태다. ✅ **해소(2026-08-08)**: `#29` 수정으로 refresh 경로가 `type=REFRESH` 클레임을 강제하게 되어, **프로필 조합과 무관하게** tempToken/previewToken의 refresh 승격이 차단됐다(프로필 조합에 의존하던 발현 조건 자체가 사라짐). 다만 운영 프로필 확정 시 `#30`(refresh 토큰 회전·폐기 부재) 등 나머지 ④-B 항목은 여전히 재검토 대상이다.

---

## 진행 이력

| 일자 | 내용 |
|------|------|
| 2026-08-06 | 매트릭스 최초 작성 (로그인 플로우 6단계 × 10개 카테고리 = 60셀, 전부 미분석) |
| 2026-08-06 | 60셀 전수 분석 완료. bo-security-reviewer(프론트) + java-security-reviewer(백엔드) 결과를 디스패처가 병합, Critical 전건은 소스 직접 Read로 재확인. 결과: PASS 21 / FAIL 38 / N/A 1, Critical 13건 / Warning 17건 / Info 16건 (총 46건) |
| 2026-08-06 | 5건 수정 완료 및 재기동 검증: ①-C(로그인폼 비밀번호 하드코딩, JWT secret 하드코딩), ①-G(User Enumeration), ①-E/②-D/③-D/⑥-D,E(X-Frame-Options·CSP frame-ancestors 부재, fo에도 동일 적용), ①-F(LoginRequest Bean Validation 부재). 6셀 FAIL→PASS 전환, 집계 PASS 27 / FAIL 32 / N/A 1 |
| 2026-08-06 | #6(RecaptchaService 파라미터 인젝션) 세션 에이전트 + java-security-reviewer 교차검증 → Info에서 Warning으로 정정. 교차검증 과정에서 #47(reCAPTCHA 시크릿키 평문 로그 노출, Critical) 신규 발견해 추가. 갱신된 집계: Critical 14건 / Warning 18건 / Info 15건 (총 47건) |
| 2026-08-06 | #6·#47 수정 완료(쿼리스트링→폼 바디 POST 전환) 및 실제 인젝션 재현 테스트 + `logback-spring.xml` 마스킹 정책 확인으로 검증. ①-A FAIL→PASS 전환. 집계 PASS 28 / FAIL 31 / N/A 1 |
| 2026-08-06 | `#보안성상세분석 #7` 3자 교차검증(java-security-reviewer+nextjs-security-reviewer+세션 에이전트) 수행. #7 Info→Warning, #13 Warning→Critical(TransactionLogController 대상 추가) 재평가. 신규 발견 #48(clientIp 미절단 감사회피, Critical), #49(SecurityConfig 중복 exceptionHandling, Warning) 추가. `#보안성검토수정`으로 #13·#48·#49 수정 착수 |
| 2026-08-06 | `#보안성검토수정`(#13·#48·#49) `security-fix-executor` 완료: 실제 curl 3-way(비인증/비시스템관리자/시스템관리자) + psql 직접 조회 + 재기동으로 검증. 과정 중 **#1이 실제로는 미수정 상태(원인 불명, 되돌아감)임을 재발견** → 즉시 재수정 및 git diff로 확인. **#50 신규 발견**(SecurityConfig `expiredSessionStrategy` 깨진 JSON, D/Warning, 미착수). **운영 이슈 확인**: `JWT_SECRET` 환경변수가 영속 저장되지 않아 bo-api 재기동마다 값이 사라짐 — 별도 조치 필요 |
| 2026-08-06 | #13·#48·#49 수정 완료 및 bo-api 재기동 후 실검증(curl 401/403/200 3-way, psql `login_log` 직접 조회, 에러응답 JSON 확인). **매트릭스 표 셀 변동 없음** — ①-B는 #10·#11, ①-J는 #7·#15, ①-F는 #8·#9가 각각 미해결로 남아 FAIL 유지. ①-D는 해당 카테고리 이슈가 #49뿐이라 PASS 전환 후보이나, 문서상 #49의 단계 소속 표기가 모호(⑥ 섹션 말미에 기재)하여 판단 보류 |
| 2026-08-07 | `#보안성상세분석 #10` 3자 교차검증 결과 `RoleController`/`RoleService`에 동일 인가 누락 확인, `#51` 신규 추가. `#보안성검토수정`(#10·#51, 8개 항목)을 `security-fix-executor`로 실행 — 전부 curl/psql 실검증 완료, 공격체인 2단계 차단. 부수 발견 6건 보고(그중 3건은 별도 승인 후 세션 에이전트가 직접 수정): `#52`(middleware.ts basePath 리다이렉트 버그), `#53`(menu-tree.tsx·RoleService 죽은 코드 2건) 신규 추가 및 수정 완료. 나머지 3건(RoleDto 필드명 불일치로 "시스템" 필터 미동작, compjj 비밀번호 재설정 필요, `#50` 미착수)은 범위 밖/운영 조치로 남겨둠 |
| 2026-08-06 | `#보안성상세분석`으로 **#51 신규 발견**(RoleController 인가 누락 + `createRole`의 클라이언트 `is_system` 신뢰, B/Critical) 후 `#보안성검토수정`으로 **#10·#51 수정 완료**. 8개 조치(AdminController/RoleController 클래스 레벨 `@PreAuthorize`, `SecurityService.isSelf` 신규, `RoleService.createRole` isSystem 고정, `AdminService` 시스템역할 배정 차단, `resetPassword` 랜덤화, `SecurityConfig` 주석 정정, `middleware.ts` 보호경로 2개 추가). bo-api·bo 각각 재기동 후 curl 실검증(시스템관리자 200 / 비시스템관리자 403 / 무토큰 401 / isSelf 예외 200·403 / 시스템역할 배정 400 / tempPassword 2회 상이 / 미들웨어 307·200) + psql 직접 조회. **매트릭스 표 셀 변동 없음** — ①-B는 **#11(permitAll 과도)** 이, ⑥-B는 **#40(`bo_is_system` 쿠키 조작)·#43** 이 각각 미해결로 남아 FAIL 유지. 단 #40은 서버측 `@PreAuthorize`가 채워지면서 "UI 우회가 곧바로 실제 데이터 접근으로 이어진다"는 결합 위험은 해소됨(쿠키 조작 자체는 여전히 가능) |
| 2026-08-07 | `#보안성검토수정`(#8·#54) `security-fix-executor` 실행. **#8은 부분 해결** — 실키 미보유로 테스트 키 교체는 범위 제외, 대신 교체를 막던 구조(`next.config.ts` env 블록 override, yml 하드코딩) 제거 + SSO 모드 캡차 우회 제거. **#54 신규 발견 및 수정**(`incrementFailure`가 `setActive(false)`를 안 해 계정 잠금이 통째로 무동작, F/Critical) — 이로 인해 **#9·#12의 기존 서술이 잘못된 전제 위에 쓰여 있었음을 정정**. 검증은 curl 실호출 + psql 직접 조회(admin_user 상태 5단계 추적, login_log fail_reason 확인) + 3회 재기동으로 수행, 테스트 계정 상태는 원복. **매트릭스 표 셀 변동 없음** — ①-F는 #8(실키 미교체)·#9(Rate Limiting 부재)가, ①-I는 #12(잠금 자동해제 부재 DoS, #54로 잠금이 실동작하게 되면서 오히려 실제 성립)가 각각 미해결로 남아 FAIL 유지 |
| 2026-08-07 | `#보안성상세분석 #9` 3자 교차검증(java-security-reviewer+세션 에이전트 직접 재확인) 수행. rate limiting 구현 0건(인프라 레벨 포함) 재확인, 계정 잠금(#54)이 rate limiting을 대체하지 못함(방어 축이 다름 — 계정 스프레이는 여전히 무제한) 확인해 #9 Critical 유지. 과정 중 신규 발견 2건: **#54 후속 결함**(`AdminService.toggleStatus`/`resetPassword`/`updateAdmin`이 `failedLoginAttempts` 미초기화 → 계정 재활성화해도 즉시 재잠금), **#55 신규**(TOTP 2차인증 코드 시도 횟수 제한 전무, F/Critical). 사용자 승인으로 두 항목 모두 DB 컬럼(`totp_failed_attempts`) 기반 통일 설계로 수정 착수(진행중) |
| 2026-08-07 | `#보안성검토수정`(#54 후속·#55) `security-fix-executor` 실행. **#54F 후속 수정 완료**(`AdminService` 정상화 3경로에 실패 카운터 초기화 — 재활성화 직후 1회 오입력에 즉시 재잠금되던 결함 해소). **#55 신규 발견 및 수정**(TOTP 2차 인증에 시도횟수 제한 부재, F/Critical) — `totp_failed_attempts` 컬럼 신규 + 3회 초과 시 `401 TOTP_MAX_ATTEMPTS` 차단 + 새 tempToken 발급 시 리셋. 검증은 실제 TOTP 코드 계산 후 curl 실호출 + psql 직접 조회(`sessions`/`registrations` 양 경로, 정확한 코드도 거부되는지 포함) + 회귀 2건(#54·#3) 확인, 전용 테스트 계정 사용 후 삭제·원복. **작업 중 승인안 전제 2건이 사실과 다름을 실측 확인**: (a) `ddl-auto: update`가 `NOT NULL` 컬럼 추가에 실패해 **각 환경 수동 DDL 필요**, (b) `TotpService` 내 `save()`는 예외 롤백으로 미반영되어 `LoginAdminService`의 기존 `REQUIRES_NEW` 패턴(`incrementTotpFailure`/`resetTotpFailure`)으로 보정. **매트릭스 표 셀 변동 없음** — ①-F는 #8(실키 미교체)·#9(Rate Limiting 부재)가 미해결로 남아 FAIL 유지. 미검증 잔여: redis 세션 모드/SSO 경로/BO 화면 |
| 2026-08-07 | `#보안성검토수정`(#9) `security-fix-executor` 실행. **#9 수정 완료** — bucket4j 8.14.0 기반 `LoginRateLimitFilter` 신규(`POST /api/v1/auth/login` 한정, IP당 1분 20회, 초과 시 429 `RATE_LIMIT_EXCEEDED`)를 `SecurityConfig` **양쪽 분기(redis/JWT) 전부**에 등록. IP는 `getRemoteAddr()`만 사용해 `#15`(XFF 무검증)와 분리했고 **`#15`는 미착수·미해결 유지**. 검증은 25회 버스트로 429 실발생 캡처 + 서버 차단로그 24건 vs `login_log` 27행 대조(합계 51 = 총 요청수)로 **컨트롤러 미도달 증명** + XFF 스푸핑 우회 불가 + 대상 경로 한정(health/refresh/totp 미영향) + 정상 로그인 200 + **로컬 Redis로 redis 세션 모드까지 실기동 재현**, 회귀 3건(#3·#54·#5) 확인, DB 원복. **매트릭스 표 셀 변동 없음** — ①-F는 **#8(reCAPTCHA 실키 미교체)** 가 남아 FAIL 유지. 부수 확인: `gradlew checkstyleMain`은 **이번 변경 이전부터 이미 실패 상태**(기존 코드 error 8342건/203파일)로, "checkstyle 통과해야 빌드된다"는 전제는 사실이 아님(신규 파일 자체는 위반 0건). 잔여 한계: 실제 서로 다른 IP별 동작·다중 인스턴스 합산 한도·맵 축출 부재 |
| 2026-08-07 | `#보안성상세분석 #13`(java-security-reviewer+세션 에이전트) 수행 — 기존 3개 로그 컨트롤러 인가는 견고함을 재확인하고, 인접 영역에서 4건 신규 발견: `#59`(EmailSendHisController 인가 전무, resend 쓰기API 포함), `#60`(감사로그 3종 site_id 미기록), `#61`(DatabaseController 인가 전무), `#62`(JSON 파싱 예외 원문노출). 마스킹 우회(`confirmPassword`)는 추후 로그 보존정책 일괄정리 시 처리하기로 보류. `#보안성검토수정`으로 4건 전부 수정 완료 — bo-api 재기동 후 curl 3-way(401/403/200) + psql 직접조회(X-Site-Id 헤더값 7/99/55/77 실제 기록, 무헤더 NULL 무오류)로 검증, 회귀 4건(#13·#3·#51·#10) 정상. site_id는 "기록"까지만 진행하고 조회 시 사이트별 필터링 강제는 별도 정책 결정 후 진행 예정. **매트릭스 표 셀 재확인 완료(2026-08-07)**: ①-G는 해당 항목이 #3·#60 둘뿐이고 둘 다 수정완료라 이미 `~~FAIL~~ PASS`로 표시돼 있던 것을 재확인(변경 없음). ①-B는 #11(Critical, 미해결)·#57·#58(Warning, 미해결)이 남아 FAIL 유지. ①-C는 #14(Critical, 미해결)·#56(Warning, 미해결)이 남아 FAIL 유지. 표 자체는 변경 없음(이미 정확했음) |
| 2026-08-08 | `#보안성상세분석 #15`(java-security-reviewer+세션 에이전트) 수행 — X-Forwarded-For 무검증 신뢰가 `AuthController` 1곳이 아니라 10개 파일에 중복 구현되어 있음을 확인. 추가로 `#9`(Rate Limiting) 최초 검증이 8080(bo-api) 직접호출이었던 탓에, 실제 브라우저 경로(bo Next.js 3001→bo-api 8080 프록시, 동일호스트)에서는 `getRemoteAddr()`가 항상 프록시 서버 주소로 잡혀 IP별 버킷이 사실상 **사이트 전체 공유 버킷**이 된다는 사실을 발견(자체 발견한 검증 한계 정정). 프록시가 XFF를 검증 없이 그대로 전달해 "프록시 신뢰" 설정도 역효과(공격자 위조값을 그대로 채택)라 판단, "A안"(XFF 신뢰 완전 포기 + Rate Limit 임계치를 사이트 전체 공유 기준으로 상향)을 사용자 승인(AskUserQuestion으로 임계치 옵션 직접 확인). `#보안성검토수정`으로 `#15`(10개 파일 XFF 파싱 제거 → `ClientIpUtils.resolve()` 일원화) + `#9`(`LoginRateLimitFilter` 임계치 20→100) 수정 완료 — **반드시 3001(bo) 경유로 재검증**, 위조 XFF가 `login_log`/`transaction_log`/`search_keyword_log`/`error_log`/`download_log` 5개 경로 전부에서 미채택됨을 psql로 실증, 병렬 130회 버스트에서 429 65건 발생으로 100/분 한도 동작 확인, 회귀(#13·#3·#54) 정상. 미검증 잔여: ContactUsInquiry/FoTraining/TrainingRegistration/EntityExcelExport/TrainingApplication의 실제 제출·다운로드 경로(코드는 동일 패턴 통일, 외부연동·데이터 준비 제약으로 실호출 생략), 11곳의 "X-Forwarded-For 우선" Javadoc 주석이 이제 사실과 달라졌으나 승인범위 밖이라 미수정 |
| 2026-08-08 | `#보안성검토`(읽기전용) — **미해결 Critical 전건(#8·#11·#18·#24·#29·#30·#36·#40) 재검증**. 디스패처가 소스 직접 Read로 재확인 + `java-security-reviewer`(백엔드 5건)·`bo-security-reviewer`(프론트 #40) 블라인드 교차검증 병행. 결과: **미해결 유지 5건**(#11·#18·#29·#30·#36 — 서술·근거 전부 현재 코드와 일치, 라인번호만 갱신), **부분 해결 재평가 2건**(#24 시도횟수 제한은 `#55`로 해결됐으나 타임윈도우 ±2·replay 방지는 미해결 → Critical→**Warning** 하향 / #40 프론트 우회 구조는 100% 미해결이나 `SYSTEM_ADMIN_PATHS` 7경로 중 5개는 서버측 `@PreAuthorize`로 실피해 차단됨 → Critical→**Warning** 하향), **범위 확정 1건**(#8 실키 미보유로 기존 결정 유지). **신규 발견 `#64`**(PageTemplateController·TsxGenerationController·ValidationRuleController 3개 인가 전무, B/**Critical**) — `#40`의 `SYSTEM_ADMIN_PATHS` 호출 API 전수 추적 중 발견, 쿠키 조작조차 불필요한 API 직접 호출 경로. **문서 정정 2건**: (a) `#11`의 "cryptoTest 무인증" 서술은 `#57` 수정으로 사실이 아니게 됨(현재 두 분기 모두 인증 필요, redisTest만 JWT 분기 permitAll 잔존), (b) `#18`·`#24`·`#29`·`#30`·`#36`의 라인번호를 `#55`/`#60` 등 이후 수정으로 밀린 현재 값으로 갱신. **매트릭스 표 셀 변동 없음** — ①-B는 #11·#58, ①-F는 #8·#24, ③-F는 #24(윈도우·replay), ⑥-B는 #40·#43·#64가 각각 미해결로 남아 전부 FAIL 유지. 집계 PASS 29 / FAIL 30 / N/A 1 그대로. 갱신된 미해결 Critical: **7건**(#8·#11·#18·#29·#30·#36·#64). 코드 수정 없음 |
| 2026-08-08 | `#14` 완료 판정 범위 확정(사용자 결정): 시크릿 값 자체의 실제 로테이션(DB/메일 비밀번호, Connect Portal 키, Azure SAS 재발급)은 코드 문제가 아닌 운영 조치이므로, 코드가 하드코딩→환경변수 참조로 전환된 시점에 이 항목은 해결로 판정한다(운영 로테이션 여부는 판정에서 제외). `#14`를 `🔄 부분 진행 중` → `✅ 수정 완료`로 정정. ①-C(민감정보 노출) 셀 FAIL→PASS 전환(해당 셀의 남은 이슈였던 #2도 이미 ✅). 집계 갱신: PASS 28→29 / FAIL 31→30 |
| 2026-08-08 | `#보안성상세분석 #18`(세션 에이전트 직접 오케스트레이션 — `java-security-reviewer`+`nextjs-security-reviewer` 블라인드 병렬 + 세션 에이전트 독립분석 3자 교차검증, security-review-dispatcher 미경유) 수행. `nextjs-security-reviewer`: bo 프론트 경로(state/스토어/로그/클립보드)는 이 위험을 증폭시키지 않음(Info, 정상노출) 확인. `java-security-reviewer`: `AdminUser.java`/`TotpService.java` 재확인(설계결함 그대로) 외에 **저장소 루트 `db_dump_20260731.sql`에 관리자 18개 계정(2FA 활성 17건)의 totp_secret 원문이 그대로 포함**되어 있음을 신규 발견. 세션 에이전트가 `git ls-files`/`git check-ignore`/`git branch -r --contains 0e7e3c3f`로 직접 재검증 — **원격 `ge/master`(github.com/kfgabiz-lab/ge.git)에 이미 push된 상태를 확인**(이론적 위험이 아니라 이미 발생한 실제 노출). 같이 추적 중인 `dev_db_dump.sql`은 totp_secret 데이터 0건으로 무관함 확인. `#18` 항목 서술에 이 사실과 긴급 운영조치 필요성(노출 계정 전원 TOTP 강제 재등록, 덤프파일 저장소 제거 여부는 `#14` 선례처럼 사용자 확인 필요) 추가. **매트릭스 표 셀 변동 없음**(②-C는 이미 FAIL). 코드 수정 없음 |
| 2026-08-08 | `#보안성상세분석 #64`(세션 에이전트 직접 오케스트레이션 — `java-security-reviewer`+`nextjs-security-reviewer` 블라인드 병렬 + 세션 에이전트 독립분석 3자 교차검증, security-review-dispatcher 미경유) 수행. 세션 에이전트가 3개 컨트롤러 직접 Read로 `@PreAuthorize` 0건 재확인 + 프론트 소비처 일부(`menu-detail.tsx`, `widget(Sub)/[slug]`) 직접 grep 선확인. `java-security-reviewer`: **`POST /page-templates/generate`가 최고 위험** — output-dir이 3개 프로필 전부 bo 앱 자신의 소스 트리(`../bo/src/app/admin/generated`)라 임의 내용 파일쓰기가 잠재적 저장형 코드주입으로 이어지고, 이 경로는 DB 미경유라 감사 흔적도 없음(Critical 확정). 경로탈출 자체는 화이트리스트+경계검증으로 이중 차단됨을 확인(과장 아님). `DELETE /page-templates/{id}`(파일+디렉터리 삭제)/`validation-rules` 변조(unique·maxCount 무결성 검증 무력화)/`tsx-generation` 이력 노출·삭제는 High로 별도 판정. `nextjs-security-reviewer`: 쓰기 호출 전건이 `/admin/templates/make/*`에서만 발생함을 전수 매핑으로 재확인(과장 아님) — 단 이 보호는 UI 진입 차단일 뿐 `/api/v1/*` 요청 자체는 미들웨어가 걸러주지 않아 API 직접호출로 우회됨(기존 서술과 일치, 근거 구체화). `#64` 항목에 3자 분석 결과 및 수정 시 우선순위(파일쓰기 계열 최우선) 추가. **매트릭스 표 셀 변동 없음**(⑥-B는 이미 FAIL). 코드 수정 없음 |
| 2026-08-08 | `#보안성검토수정`(#64) `security-fix-executor` 실행. **#64 수정 완료** — 3개 컨트롤러에 `@PreAuthorize("@securityService.isSystemAdmin(authentication)")` 적용하되 **소비처 분석에 따라 혼합 방식** 채택: `PageTemplateController`는 쓰기 4개(`generate`/`create`/`update`/`delete`)만 메서드 레벨로 잠그고 GET 3개는 일반 운영 화면(`menu-detail.tsx`, `widget(Sub)/[slug]`)이 소비하므로 `authenticated()` 유지(`SlugRegistryController` 선례 방식), `TsxGenerationController`·`ValidationRuleController`는 소비처가 `admin/templates/make/**` 뿐임을 재확인 후 클래스 레벨 일괄 적용. 신규 유틸 없이 기존 `SecurityService` 빈 재사용. **검증**: bo-api 재기동 후 대상 3개 컨트롤러 **전 엔드포인트 curl 3-way**(무토큰 401 / 비시스템관리자 403 / 시스템관리자 200·201·204) + **열어둔 GET 3개가 비시스템관리자에게도 200임을 회귀 확인**(정상 화면 미파손 입증) + 기존 수정건 회귀 4건(#10 `/admins`, #51 `/roles`·`/roles/assignable`, #61 `/database/tables`) 정상. 테스트로 생성한 DB 3행 및 `generate`가 실제로 쓴 파일(`bo/src/app/admin/generated/sectest64/`) 전부 삭제, 테스트 계정 비밀번호 원복. 부수 확인: `@Valid @RequestBody` 검증이 `@PreAuthorize`보다 먼저 동작해 무효 바디는 403 이전에 400으로 거부됨(보안 영향 없음, 403 측정은 유효 바디로 재실행). **매트릭스 표 셀 변동 없음** — ⑥-B는 **#40(`bo_is_system` 쿠키 단독 의존 구조적 결함, Warning)·#43(JWT 모드 세션통제 부재, Warning)** 이 미해결로 남아 FAIL 유지. 집계 PASS 29 / FAIL 30 / N/A 1 그대로. 미해결 Critical 7건 → **6건**(#8·#11·#18·#29·#30·#36) |
| 2026-08-08 | `#보안성상세분석 #40`(세션 에이전트 직접 오케스트레이션 — `java-security-reviewer`+`nextjs-security-reviewer` 블라인드 병렬 + 세션 에이전트 독립분석 3자 교차검증, security-review-dispatcher 미경유) 수행. 세션 에이전트가 `middleware.ts`의 `SYSTEM_ADMIN_PATHS` 7개 경로가 실제로 호출하는 백엔드 컨트롤러를 전수 추적(`ApiInfoController`/`SlugRelationController`/`SlugRegistryController` 보호 재확인, `/admin/templates/layer`는 실제 API 호출 0건 재확인, `/admin/system/contents-batch`의 `POST run-all`이 `SecurityConfig`에서 **permitAll**임을 직접 재확인 — 이는 `#11`과 동일 사안이라 쿠키 위험도와 무관함을 확인). `java-security-reviewer`: **`SecurityService.isSystemAdmin`은 서명검증된 JWT role 클레임→DB `role.is_system` 조회 기반이라 쿠키가 서버 인가에 전혀 관여하지 않음**을 코드로 재확인, 지정 10개 컨트롤러 전 엔드포인트를 표로 판정한 결과 `contents-batch` run 4종(permitAll, `#11`과 동일)을 제외하면 전부 서버측 차단(N) — **단 `/admin/templates/make` 화면이 함께 호출하는 `PageDataController`/`FileMetaController`/`PageFileController`/`CodeController` 4개가 `@PreAuthorize` 전무**함을 신규 발견, 인증만 되면 임의 slug 데이터 조회·생성·수정·그룹삭제·엑셀 전량추출이 그대로 성공함을 확인(세션 에이전트가 4개 파일 직접 Grep으로 재검증 완료). `nextjs-security-reviewer`: `SystemAdminGuard` 최종 재확인 결과 여전히 존재하지 않음(`middleware.ts:62` 주석 1건뿐), 7개 경로의 18개 `page.tsx` 전부 `'use client'` CSR이고 서버사이드 데이터 프리로드 경로 0건이라 **쿠키 조작만으로 화면 진입해도 실제 민감 데이터는 렌더링되지 않음**(403 시 빈 화면/에러 토스트만) — 잔존 위험은 관리 UI 구조·버튼 노출(정찰 정보)에 국한. **결론**: `#40` 자체(쿠키 단독 의존 구조)는 Warning 유지(코드 변경 없음), 단 기존 서술 "`SYSTEM_ADMIN_PATHS` 7개 전부 서버측 차단"은 부정확했음을 정정 — **신규 `#65`**(PageData/FileMeta/PageFile/Code 4개 컨트롤러 `@PreAuthorize` 없음)를 별도 항목으로 등록했으나, "인가 어노테이션 없음 = Critical"로 기계적으로 판정한 데 대해 **사용자가 즉시 지적**: `AdminController`/`RoleController`/`DatabaseController`처럼 일반 사용자 접근이 명백히 부적절한 것과, `PageDataController`(콘텐츠 데이터)처럼 로그인 운영자라면 누구나 다루는 게 화면의 존재 목적인 것을 구분하지 않은 성급한 판정이었음을 인정 — `#65`는 이후 재확인 결과 **결함 아님(의도된 설계)으로 종결**(근거: `SecurityConfig` 주석 0건, `CodeController`는 비보호 화면 `/admin/settings/codes`가 CRUD 전체 직접 사용, `PageDataController`는 일반 운영 화면 전반의 콘텐츠 렌더링에 필수 — 상세는 `#65` 참고). 추가 조치 없음, 재론 금지. 매트릭스 표 셀 변동 없음. 코드 수정 없음. 미해결 Critical 6건 유지(변동 없음) |
| 2026-08-08 | `#보안성검토수정`(#29) `security-fix-executor` 실행. **#29 수정 완료** — refresh 토큰에 `type=REFRESH` 클레임 부여(`JwtTokenProvider.generateRefreshToken`) + `getEmailFromRefreshToken()` 신규(기존 `getEmailFromTotpPendingToken` 검증 패턴 그대로 재사용) + `AuthService.refreshWithJwt()`가 이를 호출해 타입 불일치 시 401. **공유 메서드 `getEmailFromToken()`은 미변경**(`JwtAuthenticationFilter`·`TransactionLogFilter`가 accessToken 파싱에 사용 중 — 소비처 확인 후 신규 메서드 분리로 결정). redis 세션 경로·`AuthController` 분기 미변경. **검증**: 수정 전 실제 우회 재현(preview 토큰·accessToken → `/auth/refresh` 200 + SYSTEM_ADMIN accessToken 발급)을 먼저 캡처한 뒤, 재기동 후 동일 요청이 전부 **401**로 바뀜을 확인. 정상 refreshToken은 200 유지, `LS_TOTP_ENABLED=true` 임시 오버라이드 재기동으로 **TOTP_PENDING tempToken → 401**까지 재현하고 DB `totp_secret` 기반 실제 TOTP 코드로 2차 인증을 통과시켜 **정상 2FA 플로우가 발급한 refreshToken은 `type=REFRESH`·refresh 200**임을 확인. 검증 후 오버라이드 제거·원래 local 프로필로 재기동해 회귀(로그인/인증API/refresh/logout/preview 발급) 정상 확인. **부수효과(의도됨)**: 기존 발급분 refreshToken은 `type` 클레임이 없어 다음 갱신 시 401 → 강제 재로그인(최대 7일 잔존분). **매트릭스 표 셀 변동 없음** — ④-B는 **#30(refresh 토큰 회전·재사용 탐지·서버측 저장소 전무, Critical)** 이 미해결로 남아 FAIL 유지. 집계 PASS 29 / FAIL 30 / N/A 1 그대로. 미해결 Critical 6건 → **5건**(#8·#11·#18·#30·#36) |
| 2026-08-08 | `#보안성상세분석 #8`(세션 에이전트 직접 오케스트레이션 — `java-security-reviewer`+`nextjs-security-reviewer` 블라인드 병렬 + 세션 에이전트 독립분석 3자 교차검증, security-review-dispatcher 미경유) 수행. `java-security-reviewer`: secret key가 구글 공식 테스트키임을 재확인(원 지적 사실 그대로) — 단 **#9(IP당 100회/분 Rate Limit)·#54(계정 5회 실패 시 비활성화)가 이미 실동작 중이라, 캡차 무력화만으로 무제한 브루트포스·비밀번호 크래킹은 불가능**함을 코드로 확인. 캡차 무력화가 실제로 보태는 위험은 "IP 하나로 분당 약 20개 관리자 계정을 스크립트로 대량 잠금(DoS)" 한 가지뿐이며, 이는 **`#12`(계정 잠금 자동해제 부재)와 동일 뿌리의 기존 추적 사안**이라 신규 위험이 아님. `nextjs-security-reviewer`: `next.config.ts`에는 reCAPTCHA 관련 설정이 전무하고 site key는 `.env.local`(`NEXT_PUBLIC_RECAPTCHA_SITE_KEY`) 단일 경로로만 주입되며 하드코딩 폴백 없음 — **구조적 결함 없음, 실키 발급 시 환경변수 교체만으로 전환 가능**. site key가 브라우저에 노출되는 것은 reCAPTCHA 설계상 정상이라 별도 이슈 아님. 세션 에이전트가 `RecaptchaService.java`/`LoginRateLimitFilter.java`(용량 100·분당 리필) 직접 Read로 두 결론 재검증. **결론**: 원 지적(테스트키=봇방어 무력화)은 사실이며 여전히 미해결이나, 이것만으로 열리는 새 공격로는 없고 실키 발급 대기라는 범위도 그대로다. 신규 발견 없음, 매트릭스 표 셀 변동 없음, 코드 수정 없음 — **사용자 확인 후 패스, 다음 Critical(#11)로 진행**. 미해결 Critical 5건 유지(#8·#11·#18·#30·#36) |
| 2026-08-08 | `#보안성상세분석 #11`(세션 에이전트 직접 오케스트레이션 — `java-security-reviewer`+`nextjs-security-reviewer` 블라인드 병렬 + 세션 에이전트 독립분석 3자 교차검증, security-review-dispatcher 미경유) 수행. 세션 에이전트가 `ContentsBatchController`/`SecurityConfig`/`ContentsBatchOrchestrator` 직접 Read로 permitAll 4건 재확인. `java-security-reviewer`: 익명 호출 1회만으로 즉시 데이터가 사라지는 건 아님(soft delete는 이번 회차 정상 처리분 기준)을 확인해 과장 배제 — 대신 **`ContentsWriter`/`CatalogContentsBatchService` 분석으로 신규 발견**: EAI가 소스 데이터를 적재하는 도중 공격자가 무인증 엔드포인트를 호출하면(race condition) 미완성 상태를 최종본으로 오인해 아직 안 올라온 진짜 콘텐츠를 soft delete하고 해당 IF 원본을 `if_result='P'`로 소비해버려, **이후 EAI가 정상 호출해도 재처리되지 않는 복구 불가 콘텐츠 유실**이 가능함을 코드로 확인. DoS는 제한적(`ix_batch_log_running`으로 소스당 동시 1건 직렬화, 스레드 폭주 없음)이나 CATALOG 배치의 반복 전수조회+N+1로 누적 DB 부하는 가능(중간 수준). `nextjs-security-reviewer`: 화면의 "배치 실행" 버튼은 정상적으로 Bearer 토큰을 붙이지만 백엔드가 검사하지 않아 무의미함을 프론트 코드로 재확인, 화면 자체는 `#40`과 동일하게 쿠키 기반 라우트 가드일 뿐이라 API 직접호출 앞에서 무력함. 조회용 5개는 `@PreAuthorize`로 보호돼 정보 노출 경로 없음(과장 배제). **매트릭스 표 셀 변동 없음**(①-B는 이미 FAIL). 코드 수정 없음, 미해결 Critical 5건 유지(#8·#11·#18·#30·#36) |
| 2026-08-08 | `#11` 분석 결과를 사용자가 확인 후 **패스**(수정 착수 없이 다음 Critical로 진행 결정). 세션 에이전트가 `CatalogContentsReader.markIfResult()`(`UPDATE ... WHERE ctlg_code=? AND if_result='N'`)를 직접 재확인해, 직전 회차 `#보안성상세분석`이 지적한 위험을 "조회(SELECT) 시점과 완료처리(UPDATE) 시점 사이의 시간차 동안 새로 도착한 같은 문서코드의 IF 행이 실제로는 처리되지 않았는데도 함께 완료 처리될 수 있는 조회-갱신 시차(TOCTOU) 구조"로 구체화·정정(EAI가 실제로 한 문서를 몇 번의 커밋으로 나눠 보내는지는 EAI 쪽 코드라 이 저장소에서 확인 불가 — 이 틈의 실제 발생 빈도는 검증 못 함, 가능성 수준으로만 제시). 코드 수정 없음, 매트릭스 표 셀 변동 없음. 미해결 Critical 5건 유지(#8·#11·#18·#30·#36) — 다음 진행 대상: **#30** |
| 2026-08-08 | `#보안성상세분석 #30`(세션 에이전트 직접 오케스트레이션 — `java-security-reviewer`+`nextjs-security-reviewer` 블라인드 병렬 + 세션 에이전트 독립분석 3자 교차검증, security-review-dispatcher 미경유) 수행 — 결과는 `#30` 항목 본문에 기록. 핵심: 회전·jti·서버저장소 전무는 재확인됐으나 "완화수단 0개"는 과장이었음을 정정(계정 비활성화는 즉시 유효한 차단 수단), 남은 진짜 문제는 차단 수단 부재가 아니라 **탐지 수단 부재**(`/auth/refresh`가 `login_log` 미기록)로 좁혀짐. `#36`(로그아웃 무력화)은 `#30`과 동일 근본원인(서버측 토큰 상태 전무)이라 별도 재분석 없이 같은 결론 적용, 마찬가지로 미해결. 사용자 검토 후 **패스**(수정 착수 없이 다음 Critical로 진행 결정, 심각도 등급은 유지하되 재론 시 위 근거 참고). 코드 수정 없음, 매트릭스 표 셀 변동 없음. 미해결 Critical 5건 유지(#8·#11·#18·#30·#36) — 다음 진행 대상: **#18** |
